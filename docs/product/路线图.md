# 当前状态与路线图

> 更新：2026-09-09。本文只维护当前能力、未完成事项与恢复条件；历史运行结论见评估记录。目标不等于已实现，文档整理不改变已有验收要求。

## 当前能力

- Web 工作台已具备需求确认、配置生成、增量改单、版本对比、分享和导出；ADK dev UI 用于调试。
- 默认 Harness v2：程序准备候选，Builder 最多三次选配，规则、预算和锁定由程序判定。
- 本地可选账号、匿名数据认领、Redis 会话与缓存已实现；公网账号能力仍有限制。
- 数据发布已具备字段证据、风险隔离、不可变 release、last-known-good 和本机任务；人工价格 CSV 与过期提示可用。
- 默认评估集 v1.5 共 50 题：原 40 题保留，新增 10 段需求确认前的多轮对话。固定模型基线 150/150、Pass^3=50/50，通过项为 69 次交付、24 次合理非交付、27 次单轮初筛、30 次多轮对话；旧清单和历史失败保留。评估是模型与程序的组合，不替代真人与产品发布验收。

## 当前待办

| 工作 | 状态与下一步 | 验证条件 |
|---|---|---|
| 对照与回归收尾 | 四组单变量对照已完成并重放；前三项产品修复回归 150/150，包含针对性澄清的最终回归正在运行 | 保存最终代码对应的完整三次重复、解释诊断及重放记录；不混用实验二进制与修复程序 |
| 解释质量与软偏好 | 固定八题异构 Judge 已完成诊断及协议验证，仍有无效引用与语义误判；空理由、静音证据不足单列 | 独立人工校准未完成，评分不接门禁；颜色以具体 SKU 证据为准，不能用检索文本或高硬分证明偏好满足 |
| 评估可信度 | v1.4 已迁移预算题契约，旧 L1-004 和历史失败保留；通过报告区分交付与合理非交付 | 分数变化须拆分验收契约、程序修复与模型波动；S3/S4 仍为中文近似规则，误判先查判卷系统 |
| 初筛追问质量 | 程序按真实配置状态选择草稿/改单协议，核验预算和分辨率来源，支持明确撤回与口径更正；非法草稿最多一次格式重试，v1.5 单轮 27/27、多轮 30/30 | 原文与重试用量全部保留；继续覆盖复杂表达，已有成绩不等于任意输入保证 |
| 目录预算可达性 | 办公新整机的平台预算下限已接入产品，L4-215 在新旧快照均正确非交付；不自动改预算 | 扩展已有件/改单证明前分别设计保守边界与反例；当前结论只限完整目录及快照，不代表全市场无解 |
| 价格复核 | 09-08 批次仅导入独立评估库，产品库未切换；44 个 SKU 价格变动超过 25% | 数据任务核验大幅变动的具体商品，保留沿用日期和来源；新价格不自动等于更可靠，不能把模型波动当作数据效果 |
| 真人评审 | 最近记录仍为 0/3，缺少独立参与者 | 按[真人评审](../eval/真人评审.md)完成三类样本；开发者不得代评 |
| 关键缺陷与发布验收 | 人工评审和最终回归发现项仍需处理 | P0/P1 缺陷关闭；延期 P2 有规避和证据；最终结果与被测版本对应 |
| 自动价格来源 | SerpApi/Baidu 实测未达门槛，来源和每日任务禁用；来源工作暂停 | 找到可用来源后重做 canary、精确匹配、零误匹配和真实周期验证，不能降低标准 |
| 评估反馈闭环 | 工作台负反馈、运行证据冻结、本机待复核候选导出已实现；正式题库仍由人工审核 | 通过开发者自己的真实使用积累问题、复核归因，再形成少量有依据的新题；合成多轮和对照实验不冒充用户反馈 |

## 数据维护目标

以下是后续目标，不是当前数据覆盖声明。数据规则以[数据获取与发布规则](../data/数据获取与发布规则.md)为准。

- 扩展八类目录至每类至少 50 个，分批 10–20 个引入；`active_core` 的错误级字段证据完整率 100%，不能为数量指标放松门槛。
- 核心池价格覆盖率 100%，平均年龄不超过 7 天、最大不超过 14 天；自动价格来源激活前保留已有 last-known-good 与过期提示。
- 自动价格来源恢复时保留 12 SKU canary、零误匹配、双商家等门禁；96/64 目录调整和 `PCBuilderData-PriceDaily` 均不得提前启用。完成两个真实 14 天轮转周期后再评估稳定性。
- 性能分先核验许可与再分发范围，再做版本化导入；CPU/GPU 覆盖率目标 80%，缺失显示 unknown。
- 按文本、provider、model、dimensions 指纹生成增量 embedding；模型变更需处理已有向量的一致性。
- 完成回滚恢复、调度告警和连续四周运行验证；稳态维护目标平均每周不超过 30 分钟，常规定时任务零模型调用。

## 后续范围

多平台、跨会话偏好画像和社区反馈按实际需求排期。公网部署、邮件验证、找回密码等先补运行前置条件；当前不承诺实时价格、库存、24 小时采集或生产 SLA。京东联盟路线不在实施计划内。

## 维护方式

每项只记录状态、下一步和完成证据；完成后从待办移除。质量基线放入 `docs/eval/`，运行步骤放入 `docs/ops/`，设计决策放入 `docs/tech/`。历史发布标签不再作为路线图分组，但未完成的真人门禁不能被文档整理视为已通过。
