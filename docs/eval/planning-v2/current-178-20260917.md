# current-178-20260917：快照 11 当前目录基线

2026-09-17 规划评估基线从快照 9（123 件，`current-123-20260915-r2`）更新到快照 11：178 行目录 = 126 件 active_core（全部有价）+ 52 件退库（无价），parts release `4dc0da59-f63b-5adc-a3b0-cd30394b7bd9`，冻结点 tag `release-candidate-20260917-r2`（HEAD 5f12d54），备份 `var/backups/rc-20260917-r2/`。旧 123 基线原样保留，显式传入其 suite 仍可运行。

## 基线构成

| 套件 | 模式 | 内容 |
| --- | --- | --- |
| `mechanisms` | 人工 oracle（10 case） | c1–c4 沿用 r2（升级/降预算、退库换新、退库内存、静音 unknown）；c5 改造为 ITX SFX 电源规则；c6 改造为办公核显；c7 重建为散热容量 unknown；另含 3 个行为 case（build_cases） |
| `screening-recorded` | 录制回放 | 20 题，原回答与断言不变，仅换 catalog；replay 20/20 |
| `thermal-recorded` | 录制回放 | 1 题 Builder 失败录制，仅换 catalog；**replay 已知失败（L1-006 保留 0/1）**，与 r2 录制在当前代码下的既有结论一致（见[收尾记录](业务收尾有限验收-20260915.md)）：录制断言与 2026-09-16/17 的数据补齐（主板 memory_speed_max_mts 已全量）及产品修复（已有件 issue 聚合、内部码剥离）已脱节；按"不重写历史录制"政策原样保留，当前行为以规则单测为准 |
| `cpu-recorded` | 录制回放 | LIVE123-CPU 首题录制，仅换 catalog；replay 1/1 |

`index.json`/`coverage.json`/`budget.json` 由 `scripts/eval/planning/index_current_178.py` 生成；无 GPU 下限 3060.3 元（最便宜核显 CPU = 4600G 657.8 元）。

## 新增能力点

- **ITX SFX 电源**（c5）：nr200p（supported_psu_form_factors=[sfx,sfx_l]、限长 130mm）+ ATX 电源 → FORM_FACTOR_SUPPORT 错误级 fail（issue 含"电源形态"）；换 `psu-cm-v850sfx-gold-white`（sfx/100mm）后 ready、validation=pass。规则实现 `internal/rules/clearance.go` formFactorSupportRule。
- **办公核显**（c6）：4000 元办公、`cpu-r5-4600g`（has_igpu=true）+ gpu=null → DISPLAY_OUTPUT 不再拦截，validation=pass；降预算到 3000 元后如实给出超支问题。真实模型冒烟会话 c2323e57（docs/eval/real-trial-20260917.md）已验证该场景可通过。

## c7 重建说明

r2 的 c7 前提是"主板内存频率上限未知"；2026-09-16 数据更新已补齐全部主板 memory_speed_max_mts（快照 11 无一缺失），该前提失效。c7 重建为等价的 unknown 保留机制场景：`cooler-noctua-nh-u12s`（cooling_capacity_w unknown）→ COOLER_THERMAL_CAPACITY unknown 如实保留（issue 含"散热"）→ 换 `cooler-deepcool-ag400`（220W）后 ready、validation=pass。这与散热 capacity 暂缓缺口（见下）一致，验证 unknown 不被默认值替代。

## 暂缓缺口（如实保留，不放宽规则）

见 docs/data/数据获取与发布规则.md §5.4 与 docs/eval/real-trial-20260917.md：

1. RX 6600/7600：三轮买手复核无干净 A 级行，暂缓。
2. 5600G/5700G：官方页未公布 Supporting Chipsets，暂缓、不猜测补位。
3. 散热 cooling_capacity_w：10 款复核厂商均未公布，保持 unknown。

上述三条已写入套件 provenance limitations 与 coverage limits；AMD 低预算场景不断言选中缺失 SKU，散热场景只断言 unknown 如实保留。

## 冻结与复现

- 冻结：`python scripts/eval/planning/freeze_current_178.py`（从 `var/backups/rc-20260917-r2/pcbuilder-rc2.dump` 恢复到一次性 `pgvector/pgvector:pg16` 容器 `peval-freeze-178`，只读导出，断言 latest_id=11、126 件全有价、release=4dc0da59）。
- 构建：`python scripts/eval/planning/build_current_178.py && python scripts/eval/planning/index_current_178.py`（OUT 已存在即拒绝，防覆盖）。
- 离线回归：`python scripts/eval/planning/run.py`（默认 suite 已指向本基线 mechanisms；`--go-tests ./internal/planningeval` 与 replay 互斥，需分开跑）。

### 2026-09-17 回归结果（离线，零模型调用、零联网）

- mechanisms：10/10 通过（`artifacts/planning-replay-178-mech-r2`）。
- screening-recorded：20/20；cpu-recorded：1/1。
- thermal-recorded：0/1，既有已知失败（见上表）。
- `--go-tests ./internal/planningeval`：ok；`go test ./internal/rules ./internal/planningeval`：ok。

## 真实模型冒烟（live-smoke-178-20260917）

用户授权的单批冒烟：2 场景（`SM178-OFFICE-IGPU`、`SM178-ITX-SFX`），共享 16 次调用硬上限，每场景一次、不重跑追分，网页与搜索离线。套件由 `scripts/eval/planning/build_live_smoke_178.py` 从新基线 mechanisms 派生（剥离 oracle，仅保留业务交付断言与用户点名项）。

结果（`artifacts/live-smoke-178-r1`，model pin `baseline-20260915.json`）：

| 场景 | 结果 | 观察 |
| --- | --- | --- |
| SM178-OFFICE-IGPU | 通过（delivered、无 issue、validation=pass） | 真实模型自主选中 `cpu-r5-4600g`、gpu=null、总预算内——办公核显能力点真实验证成立 |
| SM178-ITX-SFX | 未交付（0/1） | screening 把用户点名新购的"酷冷至尊V SFX Gold 850W 白色版"误分类为已有配件（issue：已有配件尚未对应到候选中的准确型号），未映射目录 SKU，方案停在待解决 |

消耗：actual_model_requests 14/16，tokens 248,670（约 12 分钟量级），external_requests 0。

**登记的模型能力问题（不改提示词、不追分）**：用户以"指定/要求 X"措辞点名的新购配件被 screening 归入已有/owned 语义，未进入购买选型；与 real-trial-20260917 场景 5（已有件）的语义边界问题同类。ITX SFX 规则本身的机制正确性已由 mechanisms c5 离线覆盖；真实模型下该规则路径尚未被端到端触发。

## 真实模型全量 mechanisms（r1 批次，2026-09-17）

用户授权的全量批次：10 场景共享 120 次调用硬上限，每场景一次、不重跑追分，网页与语义检索离线。套件由 `scripts/eval/planning/prepare_current_live_178.py` 从 mechanisms 派生（剥离 oracle）。

结果（`artifacts/live-178-mech-r1`，suite sha256 `cdd12232…`，执行套件完整副本保存在该目录；model pin 同冒烟）：

- **0/10 通过**；actual_model_requests **96/120**（screening 20 + builder 76），tokens 1,982,798，约 14 分钟，external_requests 0；工具调用 110 次（evaluate 24、search_local_batch 17、search_local 6、read_evidence 5、search_web 4、register_candidate 2）。剩余 24 次额度因场景失败早停未耗尽。
- 步骤分类：behavior_failure 27、delivered 3、pending_with_evidence 2、read_or_retry 5、technical_fault 4。

**失败分解（327 项断言，245 项通过；82 项失败）**：

| 类别 | 数量 | 说明 |
| --- | --- | --- |
| 套件设计错误（评估方责任） | 17 | r1 派生时给所有 message/confirm 步强加 `require_tools=["evaluate"]`；首条消息只跑 screening（无工具调用），该断言不可能通过。authored 套件首步期望本就是 `versions:0, builder_calls:0`。生成器已修正为仅保留 authored 工具要求 |
| confirm 时会话仍处于 collecting（technical_fault 4 + 连锁失败约 12） | ~16 | 模型未在 authored 步数内完成需求收集，confirm 动作被后端拒绝（"会话阶段不允许当前操作"）；主要发生在 B2 行为场景与 c7 |
| 真实模型行为失败 | ~49 | 见下 |

登记的真实模型行为问题（一次尝试，不代表成功率；不改提示词、不追分）：

1. **超预算交付**（c1、c2）：预算 7500/7000，模型分别交付 7682/7673 元的 proposal 并附"需用户确认取舍"的未解决 issue，而非自行调整到预算内（如降内存/换 SKU）。
2. **已有配件无法匹配目录**（c3、与冒烟 ITX-SFX 同源）：G.Skill 32GB 已有内存在目录中无精确 SKU 时，模型以 16GB 占位交付且超预算，owned_parts 语义处理失败。
3. **需求收集节奏**（B2-001/002/003、c7）：多轮 free-text 字段（workload/offline/avoid_brand/owned_cpu/owned_board）未被 screening 提取为 active；会话长时间停在 collecting，final_outcome 为空。screening 模型（deepseek-v4-flash-0731）对自由文本字段的提取能力是主要短板。
4. **冲突处理**（c4）：noise_pref 被记为 conflict 而非 uncertain。
5. **正向信号**：c6（办公核显）除被强加的工具断言外全部通过——4600G 核显路径在真实模型下端到端成立，与冒烟结论一致；c5 交付了合规 ITX 构建但规划进度断言未过。

**批次状态**：r1 已消耗，按"单批不重跑"政策不再自动补批。r1 的 17 项结构性假失败源于派生脚本缺陷，修正后套件版本为 `current-178-live-mechanisms-20260917-r2`（`internal/planningeval/testdata/current-178-live-20260917/`，已通过零调用校验），如需以修正套件重新执行需用户另行授权与调用预算。

### r1 失败项的提示词修复（2026-09-17，已授权）

依据 r1 与冒烟的失败现场登记并修复（提示词改动均通过离线回归与 `go test ./...`；真实模型效果待下次授权批次验证）：

1. **owned/指定购买语义**（`screening_requirement_state.go` 增量协议 + `screening_new_build_prompt.go` 草稿协议）：明确"指定/要用/必须配某型号"是购买要求，必须存独立 free.* 约束条目（草稿协议写 notes），绝不写入 existing_parts/owned_parts。现场依据：冒烟 SM178-ITX-SFX 模型把"电源指定酷冷至尊V SFX Gold 850W 白色版"写成 owned_parts（live 输出留档于 artifacts/live-smoke-178-r1）。
2. **负向品牌约束**（增量协议 remove 语义处）：补充示例"不要英伟达显卡"应存 `free.no_nvidia_gpu` kind=constraint，不写 brand_pref.gpu=amd（B2-001 现场违反）。另登记：B2 系列部分 free.* 命名差异（如 live 用 `free.local_whisper` 覆盖 workload/offline 语义）属 authored 期望对自由编号过度锁定，非模型能力缺失，不改套件、在此留档。
3. **value/strength 混淆**（增量协议 strength 说明处）：明确 value 与 strength 是不同字段，枚举字段 value 只能取枚举值。现场依据：c4 第 3 步模型把 "prefer" 当 noise_pref 的 value（应为 silent 枚举），非法值触发程序侧 conflict。
4. **超预算交付**（`internal/planning/runner.go` builder 指令）：总价超出预算硬上限时先自行调整未锁定配件压回预算内再交付，不把可自行决定的降配挂起等用户确认；只有牺牲硬性要求或已有件才留待解决。现场依据：c1/c2 分别交付 7682/7673 元超上限方案并附"需用户确认取舍"。

已有配件无法精确匹配目录 SKU 时的处理（c3）属 builder 检索/评估能力问题，本轮未改，维持登记。

### 修复效果验证（2026-09-17，用户授权第二批复跑）

冒烟复跑（`artifacts/live-smoke-178-r2`，16/16 次）：0/2，但**修复 1 在 screening 层验证成立**——"电源指定酷冷至尊V SFX Gold 850W 白色版"被输出为 `free.psu_model` 约束条目（r1 为 owned_parts），全链路无 owned 误分类。两个失败均在后续步：SM178-ITX-SFX confirm 步撞上 503 上游服务不可用（供应商故障）；SM178-OFFICE-IGPU 的 builder 耗尽 24 轮额度后以散热 capacity unknown 未解决项收尾（r1 该场景通过，属运行间波动）。

全量 r2（`artifacts/live-178-mech-r2`，套件 current-178-live-mechanisms-20260917-r2）：**1/10**（r1 0/10），112/120 次（screening 20 + builder 92），tokens 2,657,372，约 25 分钟，external 0。断言 320 项通过 250（r1 327 通过 245；r2 断言面已剔除结构性假失败）。与 r1 对比：

| 修复项 | 验证结果 |
| --- | --- |
| require_tools 结构性假失败 | 消除（r1 的 17 项不复现） |
| owned/指定购买语义 | 成立：全批无一处把点名新购写成 owned_parts；C123-002 整案例通过（r1 交付 7673 超上限） |
| 超预算交付 | 部分成立：C123-002 通过；但 c1 仍超 6500 上限交付、c6 以 3110.30>3000 交付并声称"目录无更低可换"（未核实为真） |
| value/strength 混淆 | 部分成立：c4 不再产生 conflict，但模型把"静音改成尽量"记成 value=normal（应为 silent+prefer），新错误形态 |

r2 新登记的模型问题（一次尝试，不追分）：

1. **builder 轮次额度耗尽**（c1、冒烟 OFFICE）：24 轮上限内未完成修正，以"本轮处理额度已用完"的未解决 proposal 收尾。
2. **目录事实误报**（c5）：声称"目录无 AM4 ITX 主板/无 AM5 核显 CPU/无 SFX 电源"，而 b650i-edge 与 v850sfx 均在目录中——检索覆盖或断言准确性问题；同时把 AM4 CPU 与 AM5 主板送进 evaluate。
3. **confirm 时会话仍 collecting**（B2×3，与 r1 同）：需求收集节奏问题未修复。
4. **B2 free.* 命名差异**（与 r1 同）：authored 期望锁定自由编号，已在上文留档为评估侧过度约束，不计为模型回归。

批次状态：r2 已消耗（剩余 8 次不足以重跑）；后续修复优先级建议：builder 轮次内完成修正与目录检索覆盖（c5）、noise_pref 枚举值语义、预算内自纠的稳定性。

### c5 目录误报根因与检索修复（2026-09-18，零模型调用）

对 r2 报告 C123-005 全部 search 调用做离线复核，修正此前一处误判：step2 批量子查询并非返回空数组（先前解析把 `planning_action` 响应口径看漏），每个子查询都返回了 8 个候选——但全部是该品类价格最低的 8 件，与查询词无关。根因确认：

1. **price_asc 排序下查询词失效**：`search_local` 排序为"价格优先、评分仅同价并列"（刻意设计，防平台过滤，见 legacy-retrieval-recheck 测试）。模型用 `order_by=price_asc` + 规格词（"SFX ITX"、"SFX SFX-L 小1U"）检索，返回的永远是品类最便宜的 8 件；v850sfx 价格 999 元被 `truncated=8` 截在页外。模型随后断言"目录无 SFX/SFX-L 电源"，而该商品在目录中。主板查询"ITX AM4"同理只见 mATX 板。step3 改用默认 relevance 按完整型号查询时 v850sfx 即排第 1，证明 relevance 路径正常。
2. **修复**（`internal/planning/runner.go`，不改价格排序语义）：价格排序响应新增 `query_matched_count`（本品类 query 命中总数）与 `keyword_hits`（命中候选，最多 8 个，按价格序）；relevance 路径不变。工具说明同步增加：命中者未进当前页不代表目录没有；`query_matched_count=0` 时换通用规格词重查，仍为 0 才可断言该品类缺少该规格。

新增回归测试 `TestKeywordHitsExposeMatchesTruncatedByPriceOrder`（c5 形态：9 件 ATX + 1 件 SFX，price_asc 页保持不变且 keyword_hits 暴露 SFX 命中；零命中不附 key）。`go test ./internal/... ./cmd/...` 全过，离线 replay 零模型调用不受影响。真实模型验证需下批授权。

### builder 预算自纠辅助与轮次收敛（2026-09-18，零模型调用）

针对 r2 新登记问题 1（24 轮额度耗尽）与"超预算交付"部分成立，两项改动（`internal/planning/runner.go`）：

1. **evaluate 超预算反馈附 `budget_alternatives`**：报价超预算硬上限时，反馈自动附各品类最便宜的有报价候选（每类最多 5 件、按价格排序、排除已选项、不做兼容核验，附说明文字）。模型无需额外检索即可判断能否自行替换压回预算；r2 中 c6 曾在超预算 110.30 元时直接交付并声称无更低可换，该数据此前需要多次 search_local 往返才能核实。触发条件：`budget_cny` 为 active must 且报价（PurchaseTotalCNY，缺省用 TotalCNY）超过上限。
2. **首次 evaluate 时机**：工具说明由"尽早检查"改为"首次 evaluate 应在工具额度过半前完成，之后优先用反馈修正"。现场依据：r1 C123-001 出现 `[search_local evaluate search_local×4 evaluate]` 循环，额度在反复评估中耗尽。

新增回归测试 `TestBudgetAlternativesAttachCheapestOnOverspend`（超预算附最便宜候选、已选项排除、缺价品类省略、预算内/无预算不附）。`go test ./internal/... ./cmd/...` 全过。真实模型验证需下批授权。

### noise_pref 示例与交付质量评分口径（2026-09-18）

1. **noise_pref 转换示例**（`internal/agents/pipeline/screening_requirement_state.go`）：strength 条目追加示例"静音改成尽量安静"→ `set noise_pref value=silent strength=prefer`。现场依据：r2 c4 模型记成 value=normal（应 silent+prefer），错误换了形态但仍是枚举/强度混淆。
2. **交付质量评分口径**（`scripts/eval/planning/delivery_quality.py`，零模型调用）：从既有 report.json 重算硬性门（事实正确、预算内、兼容错误 0、无缺价），失败 check 按名分类，机制/评估侧 check（require_tools、state:*、version_count 等）不计入。对 r1/r2 报告重算基线：r1 硬过 3/10、r2 硬过 4/10（机制口径为 0/10、1/10）。可用标准 27/30 需三批各 10 case 达标；关键能力点（办公核显、ITX+SFX、已有件替换）按 case 逐批核对。

## 范围与边界

离线回归全部零模型调用、零联网；provenance 记录 source_commit、套件与生成器 sha256。真实模型已执行两批授权：冒烟 r1（1/2）、全量 r1（0/10，含 17 项评估方结构性假失败）、修复后复跑冒烟 r2（0/2，screening 层验证 owned 修复成立）与全量 r2（1/10）；再次执行需另行授权与 `--max-calls` 上限后登记。
