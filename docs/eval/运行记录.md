# 评估运行记录

> 只追加,不改写历史。每轮记录:时间、快照、模型口径、指标、逐用例结果、失败归因、产物路径。原始轨迹在对应 `artifacts/eval/<时间戳>/`(不入库)。

---

## 2026-09-06 22:31 · 首跑(环境故障样本)

- **口径**:快照 2026-07-28;builder=bailian/qwen3.7-plus-2026-05-26(链机制引入前);评估集 v0(15 条 L1)
- **结果**:Pass@1 = **0%**(0/15,全为 RUN 错误,attempts 均为 0)
- **归因**:每条 ~150–340ms 即失败,错误统一为 `upstream kind=authentication status=403 code=PERMISSION_DENIED`。排查确认:**DashScope 账号聊天模型免费额度耗尽**(账号处于 free-tier-only 模式;直连 `/chat/completions` 返回 `insufficient_quota` + "Free quota exhausted"),embedding 模型计费独立故不受影响。403 被分类为 authentication 是 `upstream.Classify` 对 `/responses` 端点错误形态(码不含 quota 字样)的已知盲区。
- **结论**:环境级故障在 15 个用例上 3 分钟内稳定暴露,单条"用例 A"做不到;该轮作为评估体系有效性的环境故障样本留档。
- **产物**:`artifacts/eval/20260906-223112/`

## 2026-09-06 23:15 · v0 首次有效基线

- **口径**:快照 2026-07-28;builder=bailian/chain:qwen3.8-27b(11 候选额度链,当时 qwen3.7-plus 链外模型免费额度耗尽);评估集 v0(15 条 L1);`-seeds 1`
- **结果**:Pass@1 = **86.7%**(13/15),data-error 0;replay 重放逐用例结论一致(可复现性 ✓)
- **逐用例**:13 条 pass(其中 8 条经 1 次定向修复轮达成);2 条失败:
  1. **L1-004(3K 入门家用办公)**:3 轮后 overall=fail,失败规则 `DISPLAY_OUTPUT` → 归因 **B6**(校验反馈未定向修复):3K 档位下"显示输出(核显或独显)"约束与目录候选/预算组合未走通,待查目录是否有低价核显 CPU 及修复环策略。
  2. **L1-011(3K 已有 CPU+内存)**:3 轮 BuildDraft schema 反馈后仍"cpu 的 SKU 不得为空" → 归因 **B7**(输出契约违约):弱档模型把 `existing_parts` 误解为"不用选该品类",提示词鲁棒性改进的直接靶子。
- **观察**:平均耗时 ~8s/用例(attempts 1–3);8 条经修复轮达成说明定向修复环在工作。
- **产物**:`artifacts/eval/20260906-231523/`

## 2026-09-06 23:45 · v1 全集首跑(seeds=1)

- **口径**:快照 2026-07-28;builder 链 qwen3.8-27b(11 候选);screening 同链;评估集 v1(15 L1 + 10 L2 + 5 L3-screening = 30 条);`-seeds 1`
- **结果**:Pass@1 = **86.7%**(26/30),data-error 0,veto 0 次
- **逐用例失败与归因**:
  1. **L1-004(3K 家用机)**:B6 — `DISPLAY_OUTPUT` 规则 3 轮未修复(v0 已知,复现);
  2. **L1-011(已有 CPU+内存)**:B7 — schema 违规复现(existing_parts 被误解为"不选");
  3. **L2-109(基线C 换 AMD 旗舰卡)**:B6 — 换上 9070XT(¥5649)后总价 ¥21192 低于 25000×0.9 下限,3 轮"升预算"失败;修复环只会降配、升配路径候选不足,是定向修复策略的真实盲区;
  4. **L3-101(品牌推断陷阱)**:S1 — **评估集自身期望设计错误**(输入未给分辨率,模型按提示词"游戏必须确认分辨率"追问是正确行为),修正 fixture 输入补"2K 显示器"后转绿;归入"先查评测系统本身"案例。
- **观察**:25 条 build 全部跑通改单路径(锁品 类/调预算/换件 hint 混合),A8 锁定复验零触发;5 条 L3 中 4 条直接通过。
- **产物**:`artifacts/eval/20260906-234531/`

## 2026-09-06 23:57 · v1 Pass^3 发布门禁

- **口径**:同上;`-seeds 3`(30 用例 × 3 seed = 90 次执行)
- **结果**:记录级 Pass@1 = **90.0%**(81/90);用例级 **Pass^3 = 90.0%(27/30)**;**veto 触发 0 次**;replay 重放逐用例一致 ✓
- **逐用例**:L1-004(3 seed 全败,B6)、L1-011(3 seed 全败,B7)、L2-109(3 seed 全败,B6,失败金额完全一致 ¥21192.00)——三条失败均为**确定性复现的系统性问题**,非采样噪声;其余 27 条(含全部 L2 锁定/预算/换件组合与全部 L3 行为陷阱)三种子稳定全过。
- **结论**:按 Agent 的评估方法论口径,当前系统的可靠性瓶颈集中在 3 个已归因问题:①3K 档显示输出约束的组合可达性(L1-004);②existing_parts 的输出契约鲁棒性(L1-011);③换件后"升预算"修复路径(L2-109)。三者都是明确的改进靶子,修复后重跑本门禁即可验证。
- **产物**:`artifacts/eval/20260906-235754/`

## 2026-09-07 15:59 · v1.1 固定模型基线（30 题 × 3 次）

- **口径**：评估集 v1.1，清单 SHA256 `effd97d01d23497f8032cf12befdb1f329258742590e3497dcae1e2ecd6289c8`；价格快照 2026-07-28，当前启用的 active_core 零件 160 条。builder=`bailian/qwen3.8-max-0902`，screening=`bailian/deepseek-v4-flash-0731`，embedding=`bailian/qwen3.7-text-embedding`；三角色配置均无切换链，两个聊天角色 `reasoning_effort=none`、百炼 session cache 开启。`-seeds 3` 表示三次独立会话/执行，不是向模型传入三个随机种子。
- **环境预检**：启动原先停止的项目 PostgreSQL 容器，确认旧快照存在。screening 和 embedding 最小检查通过；builder 首次检查返回 400 `InvalidParameter`。定位到本机 `.env` 未配置 `BUILDER_REASONING_EFFORT`，非默认模型回退到 `low`；供应商拒绝思考模式下的强制工具调用。显式补 `BUILDER_REASONING_EFFORT=none` 后 builder 工具调用检查通过，再启动正式评估。预检不计入下述 90 次分母。
- **结果**：记录级 **Pass@1 = 83.3%（75/90）**；用例级 **Pass^3 = 80.0%（24/30）**；data-error 0、RUN 错误 0、veto 触发 0。单次用例耗时合计约 547.8 秒。门禁未全绿（run 退出 1），但这是一轮完整、有效的固定模型基线。

| 分组 | 执行通过 | 三次全过的用例 |
|---|---:|---:|
| L1 整机 | 36/45 | 12/15 |
| L2 改单 | 26/30 | 8/10 |
| L3 初筛 | 13/15 | 4/5 |

| 用例 | 三次结果 | 失败证据与归因 |
|---|---|---|
| L1-003：8K Intel + NVIDIA | 失败 / 失败 / 失败 | A2 → B6；三轮后仍为 ¥5952，低于预算下限 ¥7200；交付未成功，最终规则状态 review |
| L1-004：3K 家用机 | 失败 / 失败 / 失败 | A2 → B6；三轮后均为 ¥4013，高于预算上限 ¥3300；最终规则已 pass，本轮直接阻塞点为预算 |
| L1-011：已有 CPU 和内存 | 失败 / 失败 / 失败 | A2 → B6；三轮后均为 ¥4013，高于预算上限 ¥3300；本轮没有复现旧的空 SKU/schema 错误，最终规则已 pass |
| L2-109：换 AMD 显卡 | 失败 / 失败 / 失败 | A2 → B6；三轮后均为 ¥21192，低于预算下限 ¥22500；原有升配修复问题仍未解决 |
| L2-110：降预算 3000 | 失败 / 通过 / 通过 | seed 1 三轮后 ¥19242，低于新预算 ¥22000 的下限 ¥19800；A2 → B6；存在重复执行间波动 |
| L3-103：缺分辨率 | 通过 / 失败 / 失败 | seed 1 实际追问分辨率，S3 通过；seed 2/3 直接返回缺少 resolution 的 gaming 需求单，触发 S1，既未追问又不满足 schema；领域归因码尚未映射，不能当作无失败 |

L3-103 的两次失败原文均为 `{"schema_version":1,"budget_cny":8000,"use_case":{"type":"gaming"}}`。L3-102 三次均通过预算追问断言。其余四个初筛用例三次全过；缺分辨率题的失败可从留存原文直接复核，不再只依赖首跑分数。

- **重放验证**：90 个 `(case_id, seed)` 唯一且齐全；15 条 screening 记录均有回复原文。使用保存的 `cases.json` 和 `results.jsonl` 零模型重放，完整 Verdict（含失败明细、veto）与归因全部一致，replay 退出 0，无历史 screening 跳过。replay 退出 0 代表复现一致，不代表门禁通过；报告现为 replay 版本，原始结果和 meta 保留。
- **比较边界**：旧 v1 的 90% 使用额度链，本轮使用新固定模型且两道追问题期望已加严。不能把差值直接归因为单一模型能力下降。四个用例三次全败、两个用例有波动，这只是本轮重复结果，不宣称已证明模型输出确定性。veto=0 也仅覆盖实际执行的成功交付断言，未交付的预算失败由 A2 记录。
- **后续靶子**：先排查预算上下界的候选与修复路径（五个 build 失败用例），并加强缺分辨率时的初筛行为；改动后继续用同一 v1.1 和固定模型重跑。保留本轮为修改前基线，不为分数改低期望。
- **产物**：`artifacts/eval/20260907-155905-3494739335/`（`cases.json`、`meta.json`、`results.jsonl`、`report.md`）；控制台日志 `artifacts/eval/v1.1-baseline-hp9NN5CL.log`。首跑工作区 commit=`7a574945fdd40dd6fb3fd0f5311bea0ac78ca657`，dirty=true；实际程序 SHA256 见 meta，commit 不代表包含所有未提交改动。

## 2026-09-07 21:14 · 预算搜索与初筛修复首轮复测

- **固定口径**：仍为同一 v1.1 清单及 30 题 × 3 次，快照 2026-07-28；builder=`qwen3.8-max-0902`、screening=`deepseek-v4-flash-0731`、embedding=`qwen3.7-text-embedding`，空切换链，聊天推理参数 none。不改题目、价格或判分条件。
- **代码修复**：保留低价核显、各内存代数最低价和低价已知散热能力候选；原预算定向搜索无完整可行解时，尝试其他未锁定品类的有界组合，包括 SSD 1/2 块，继续通过真实 validator 校验；核验修复输出没有改变未开放品类。初筛提示词改为先判断信息齐全，再决定追问或输出 JSON，取消无依据按游戏推断分辨率的指令。实现见 Harness v2 设计（文档已清理）。
- **结果**：Pass@1=**92.2%（83/90）**，Pass^3=**90.0%（27/30）**；RUN 错误 0、data-error 0、veto 0。完整断言及归因重放一致，replay 退出 0；run 退出 1，门禁仍未全绿。
- **修复命中**：L1-003、L2-109、L2-110、L3-103 均三次全过。初筛 5 题共 15 次全过；原缺分辨率题不再输出缺字段需求单。
- **回归发现**：L2-105 第一次失败，后两次通过。失败明细是“预算改单改动了 3 个品类，超过最多两类限制”；检查确认候选分位裁剪未保留仍合规的基版本主板/内存，导致改单被迫改变零件。后续补上基版本保留及保护逻辑，再完整跑一轮；不将两轮成功记录拼接。
- **剩余数据边界**：L1-004/011 均三次失败。对当前 160 条 active_core 及指定价格批次做只读下界检查：核显路径按 CPU socket 匹配主板、内存代数匹配内存，其余必选品类取全库最低价，暂不要求芯片组、功耗、散热等其他规则，AM5 乐观下界 **¥3366**，LGA1851 **¥4066**，LGA1700 **¥4076.50**；独显路径即使各品类独立取最低价也需 **¥4325**。这些宽松下界均超过题目 ¥3300 上限，故更严格的兼容整机也不可达。`existing_parts` 目前仍沿用全机选件与全机报价口径，L1-011 也受此限制。保留原期望和失败分母，没有改成 data-error 来抬高分数；解决它们需要另行补真实低价数据，或在新版本明确预算/已有件报价契约。
- **产物**：`artifacts/eval/20260907-211417-1948772304/`；控制台日志 `artifacts/eval/v1.1-repair-DOFL9d86.log`；零模型候选诊断摘要 `artifacts/eval/repair-diagnosis.log`。首跑与重判程序标识分别保存在 meta/report。

## 2026-09-07 21:26 · 基版本候选保留后的完整复测

- **口径不变**：对比 15:59 固定模型基线，`suite_version`、`suite_sha256`、`requested_seeds`、`snapshot_date` 和三个角色的完整脱敏模型配置均一致。依然是 v1.1、原 30 题 × 3 次、2026-07-28 快照；本轮是代码/提示词修复验证，没有修改题目或数据。
- **补修内容**：在每类硬过滤后，保留仍符合本轮条件的基版本 SKU，并保护其不被候选裁剪；按 CPU→主板→内存依赖顺序处理。新增测试确认这不会把不符合换品牌要求的旧 GPU 带回候选包。完整重跑 90 次，未拼接上一轮记录。
- **最终结果**：记录级 **Pass@1=93.3%（84/90）**，用例级 **Pass^3=93.3%（28/30）**；较 15:59 基线的 80% 提升 13.3 个百分点。RUN 错误 0、data-error 0、veto 0；用例执行耗时合计 508.64 秒。由于两题仍失败，run 退出 1，不能称为门禁全绿。

| 分组 | 执行通过 | 三次全过的用例 |
|---|---:|---:|
| L1 整机 | 39/45 | 13/15 |
| L2 改单 | 30/30 | 10/10 |
| L3 初筛 | 15/15 | 5/5 |

- **通过的修复靶子**：L1-003、L2-109、L2-110、L3-103 均三次全过；上一轮出现回归的 L2-105 本轮也三次全过。seed 1 示例：L1-003 总价 ¥7769.90；L2-105 总价 ¥13801；L2-109 总价 ¥22911，SSD 明确为同一 SKU 两块，经过插槽和报价验证；L2-110 总价 ¥22190。
- **如实保留的失败**：L1-004 三次规则均 pass，但总价分别 ¥3370.90、¥3561.90、¥3561.90，仍高于 ¥3300 上限。L1-011 三次最终直接失败项为 `MEMORY_GENERATION`，不能误写成“三次只因预算失败”；其现行全机报价口径同时受上一节已验证的价格下界约束，不存在预算与兼容性都满足的解。两题均按 A2/B6 计失败，未剔除分母。需要在后续明确已有件报价契约、补充真实低价目录或另建合理预算的版本，不能靠编造 SKU、价格或放宽本轮断言转绿。
- **2026-09-08 收尾验证**：90 个 `(case_id, seed)` 唯一且齐全；原始元数据保留为 run。从本轮保存的完整题目和响应重放，所有可重判记录的完整 Verdict 与归因一致，replay 退出 0，未跳过 screening。全项目 `go test ./...` 及涉及包的 `go vet` 通过；新测试覆盖 SSD 数量/插槽、锁定、预算改单两类上限、未开放品类改动、取消执行、低价候选与基版本保留。
- **产物**：`artifacts/eval/20260907-212656-2722694623/`；日志 `artifacts/eval/v1.1-repair-final-hkuQKH9Y.log`。首跑程序 SHA256=`3bf0b83adb2b31844b36e089c9bfbce294876a3e863046b8140eb70b86950ca5`；工作区 commit 仍为 `7a574945fdd40dd6fb3fd0f5311bea0ac78ca657` 且 dirty=true，完整代码口径以 meta 为准。报告现为重放报告，原始结果未改写。

## 2026-09-08 10:35 · v1.2 已有件与非交付首轮

- **口径**：原 v1.1 的 30 题保持不变，新增 L4-201～208，共 38 题 × 3 次；清单 SHA256=`1c3a3fb51874bf4a9379104f93652531132a594f284831d93390d0b85f2b1a06`。Builder=`qwen3.8-max-0902`、Screening=`deepseek-v4-flash-0731`、Embedding=`qwen3.7-text-embedding`，链为空；固定价格快照仍为 2026-07-28。本轮未采集、修改商品或报价。
- **实现**：已有件准确型号绑定、锁定与采购预算；缺信息先追问；当前完整目录的独立品类最低价下界；结构化非交付 Decision；A9 已有件/采购报价与 N1 类型/原因/证据断言。接口 error 一律计失败，不通过文案排除分母。
- **首跑结果**：Pass@1=102/114（89.5%），Pass^3=33/38（86.8%）；veto=0、data-error=0、RUN 错误=0。L4 新题 7/8 用例三次全过，L4-208 有一次判卷误判。
- **保留的历史失败**：L1-004 仍超过预算；L1-010/011 没有提供已有件型号和预算口径，新执行器先追问，原 pass 期望因此继续计失败。不能把 L1-010 写成模型突然失去选件能力，或在新版本总分里隐藏这两道契约不一致的历史诊断题。
- **本轮发现**：L3-101 两次额外追问可选游戏名称/是否含显示器，S1 失败，是真实初筛行为问题。L4-208/seed=2 实际明确询问“新增购买费用，还是整台电脑的参考总价”，S3 因按逗号切句且未覆盖“整台电脑”而误判；先修判卷并加入原话回归测试，再补提示词明确可选游戏名与无已有件时不追问预算口径。
- **产物**：`artifacts/eval/20260908-103548-3399842394/`，日志 `artifacts/eval/v1.2-boundaries-live.log`；首跑 binary SHA256=`79cafde60fa918070f71e3f241829c718d9aa5fe5ce1fba247f12c15e56c6137`。
- **修正判卷后的重判**：仅 L4-208/seed=2 从失败变为通过；replay 退出 1，正确检测到判卷结论变化，不能称本轮首跑与新判卷完全一致。原 `results.jsonl` 和首跑元数据保留，`report.md` 已更新为重判报告；诊断日志 `artifacts/eval/v1.2-first-regrade.log`。重判不替代后续完整真实复测。

## 2026-09-08 10:46 · v1.2 第二轮完整复测

- **口径**：同一 v1.2、38 题 × 3 次、相同固定模型和价格快照。新增可选信息无需追问的提示词，并修正上一轮“整台电脑”的 S3 识别。
- **原始结果**：Pass@1=101/114（88.6%），Pass^3=32/38（84.2%）；veto=0、data-error=0、RUN 错误=0。L4 的六道 build 题全部三次通过；L4-208 三次通过。L1-004/010/011 的失败原因与上一轮一致。
- **真实行为问题**：L3-101 两次追问可选游戏名称/显示器预算；L3-104 一次把“玩黑神话”的明确游戏用途当作不明确，要求再次确认是否纯游戏。说明上一轮提示词修改不足，不能只根据写入了规则宣称修好。
- **判卷误判**：L4-207/seed=3 已说“请分别提供具体的完整型号和数量”，S3 未覆盖带副词的请求表达。修正后加入肯定、否定及无关问题的回归测试；零模型重判仅这一条由失败变为通过，重判 Pass^3=33/38（86.8%）。该回复同时存在重复询问已知预算、办公用途分辨率的问题；当前 S3 只验证是否问到所缺字段，不评估全部追问质量，不能把通过解读为回复完美。
- **产物**：`artifacts/eval/20260908-104642-230729384/`；首跑日志 `artifacts/eval/v1.2-boundaries-final.log`；判卷差异日志 `artifacts/eval/v1.2-second-regrade.log`。首跑 binary SHA256=`1cac69d8554c25e4072430ba8059bef6ce032ecc9348b315877f41c5059dd66b`。原结果与 meta 保留，报告更新为重判版；replay 退出 1 表示已检测到预期的判卷差异。

## 2026-09-08 12:34 · 初筛小范围诊断

- **范围**：从冻结 v1.2 原样抽取 7 道 screening 题，3 次重复共 21 次；诊断清单 `artifacts/eval/v1.2-screening-diagnostic.json`。它是定向检查，不冒充完整发布门禁。
- **修复**：提示词末尾补充有限的必填检查清单：明确游戏即已给用途、日常办公无需分辨率、显示器不属于已有主机配件、明确新增预算不重复确认。接口测试另行验证同一个 ADK runner 的三次独立请求均携带规则，且不混入前题聊天记录；该本地测试不代表核验了供应商服务端缓存。
- **结果**：L3 原五题 15 次全过；L4-208 三次全过；L4-207 一次实际说“请分别告诉我 CPU 和内存的完整型号”，仍被字面请求词误判。原始 Pass@1=20/21（95.2%），Pass^3=6/7（85.7%）。将“请分别/请逐一”按请求语义归一后，21 次均通过断言；仍保留主题词与请求词同一短句、否定句排除，未修改题目期望。当前断言不检查所有重复追问，完整质量仍有加强空间。
- **产物**：`artifacts/eval/20260908-123448-4116342254/`、`artifacts/eval/v1.2-screening-diagnostic.log`。零模型重判仅 L4-207/seed=1 发生预期差异，replay 退出 1，日志 `artifacts/eval/v1.2-screening-regrade.log`；原始结果和元数据保持不变。随后以同一正式 v1.2 完整复测，不拼接历轮记录。

## 2026-09-08 12:36 · v1.2 最终完整回归与判卷复核

- **固定口径**：正式 v1.2 的 38 题 × 3 次，114 个 `(case_id, seed)` 唯一且齐全，21 条 screening 回复全部留存。模型、空切换链及 2026-07-28 价格快照保持不变；四轮保存的完整目录内容核验一致。执行耗时合计 520.773 秒。
- **原始结果**：Pass@1=104/114（91.2%），Pass^3=34/38（89.5%）。L3 原五题 15 次全过；新增 L4 中仅 L4-207/seed=2 因判卷未识别列表形式而失败。run 退出 1。
- **最后一处判卷修正**：原回复先说“请补充以下信息”，随后编号列表明确要求“CPU 和内存的完整型号与数量”。S3 原先只在同一短句内识别请求，遗漏这种结构。现在只向明确请求引导后紧邻的编号/项目列表传播请求含义，普通段落结束作用域；测试覆盖真实原话、Markdown 列表、否定句、已知信息陈述和无关问题，未放宽题目期望。
- **当前判卷重判结果**：Pass@1=**105/114（92.1%）**，Pass^3=**35/38（92.1%）**，veto=0、data-error=0、RUN 错误=0。只有 L4-207/seed=2 从失败变为通过，其余 113 条完整 Verdict 与归因均一致。replay 退出 1 表示检测到这一预期差异，不能宣称首跑与新判卷完全一致。未改写原始结果或首跑 meta；报告是重判版本，没有拼接不同轮次模型输出。

| 分组（当前规则重判） | 执行通过 | 三次全过的用例 |
|---|---:|---:|
| L1 整机与历史诊断题 | 36/45 | 12/15 |
| L2 改单 | 30/30 | 10/10 |
| L3 初筛 | 15/15 | 5/5 |
| L4 已有件与非交付边界 | 24/24 | 8/8 |

- **剩余边界**：L1-004 三次预算仍未达标，不能把本轮有限搜索停止说成整个市场无解。L1-010/011 的旧输入缺准确已有件型号和预算口径，新执行器三次都先追问；旧 pass 期望仍记失败。发布门禁范围需要在新版本中明确迁移这类旧契约题，不能临时减掉分母。已有件初筛仍会重复确认用户已明确的新增预算；S3 当前只验证所缺字段被问到，不代表全部追问质量过关，后续应另补负向行为断言。
- **程序验证**：全项目 `go test ./...` 通过；随后补强的 S3 和非交付 A8/A9 测试及相关 `go vet` 通过。`search_exhausted` 也检查已有件、采购报价和硬锁，不能用“未交付”掩盖越权替换；用本轮真实轨迹重判未引入额外差异。Web 全量 11 文件/27 测试及类型检查通过，新增的“型号已填但预算口径缺失仍不可提交”检查也通过。v1.1/v1.2 清单哈希、涉及档案的 UTF-8/LF 和本地链接均核验通过。
- **产物**：`artifacts/eval/20260908-123651-3156889149/`；首跑日志 `artifacts/eval/v1.2-boundaries-release.log`；判卷差异日志 `artifacts/eval/v1.2-final-regrade.log`。首跑 binary SHA256=`258adbc7892ee22fec733ed80e1419fdb3e8a1a8ba7f53195ed97b8a8c3a914b`，重判 binary SHA256=`01670a068389fbf206d0e3e6f9f0f6bb59d155356b04d2541a14101ff223485c`；工作区仍 dirty，代码身份以保存的程序指纹为准。

## 2026-09-08 13:40 · v1.3 初筛负向断言基线

- **题目迁移**：正式 v1.3 冻结 40 题：L1-010/011 原输入以 L4-209/210 迁移为缺信息先追问；L4-207/208 原输入以 L4-211/212 加严重复追问检查，再新增 L4-213/214。其他 34 题原样保留，包括 L1-004。旧文件、v1.2 清单和历史成绩保持不变，迁移表见 [v1.3](versions/v1.3.md)。
- **本轮范围**：正式清单中的 9 道 screening 题原样抽取为 `v1.3-screening-diagnostic`，共 27 次，作为提示词修复前基线。Builder=`qwen3.8-max-0902`、Screening=`deepseek-v4-flash-0731`、Embedding=`qwen3.7-text-embedding`，空链，价格快照仍为 2026-07-28。定向诊断不作为完整门禁。
- **原始结果**：Pass@1=19/27（70.4%），Pass^3=5/9（55.6%）。S4 要求不能追问已知/不适用字段；S3 的“问到缺失项”仍独立检查。
- **先复核判卷**：3 次为新判卷误伤：L4-211/seed=1 在问型号时说明用途，被裸“购买”词误认为问预算口径；L4-212/seed=3 的口径选择被逗号拆开，被误判为索要金额；L4-214/seed=1 的“新增购买总预算是多少元”实际只问金额。修正规则并加入原话与反例后，仅这 3 条变为通过，重判 Pass@1=22/27（81.5%）、Pass^3=6/9（66.7%）。replay 退出 1 正确指出判卷变化，原记录不改写。
- **真实失败**：L4-211 一次只输出“需要追问”的内部分析；L4-212 一次要求重新确认明确的 CPU 型号；L4-213 三次重复确认已明确的新增预算。没有通过删掉负向条件让这些输出转绿。
- **产物**：`artifacts/eval/20260908-134020-2460123357/`；日志 `artifacts/eval/v1.3-screening-before.log`、`artifacts/eval/v1.3-screening-before-regrade.log`。原始结果和 meta 保留，报告更新为重判版。

## 2026-09-08 13:44 · 初筛提示词修复第一次检查

- **改动**：将“必须确认”改为提取并采纳已明确字段；禁止在正确问题后追加“对吗”式确认；用少量示例区分只缺型号、只缺预算口径、只缺金额，要求直接提问而不是输出内部待办。题目和固定模型不变，仍为 9 题 × 3 次。
- **原始结果**：Pass@1=23/27（85.2%），Pass^3=6/9（66.7%）。其中 L4-214/seed=2 的“请提供新增购买配件的预算金额”再次被判卷误认为询问口径；补充命令式金额请求识别和正反例，重判仅此条变化，Pass@1=24/27（88.9%）、Pass^3=7/9（77.8%）。replay 退出 1，原结果保留。
- **真实回归**：L3-104 一次在普通游戏需求已齐全时询问是否有旧配件、预算是否都用于主机；L4-212 两次认为 AMD Ryzen 5 7600 还需要额外型号信息，其中一次没有问应问的预算口径。后续明确无已有件时直接按新装机处理，完整型号不要求店铺 SKU、包装版本、赠品或额外后缀。
- **产物**：`artifacts/eval/20260908-134400-1030341484/`；日志 `artifacts/eval/v1.3-screening-after.log`、`artifacts/eval/v1.3-screening-after-regrade.log`。

## 2026-09-08 13:48 · 初筛修复定向复测

- **同题同模型复测**：9 题 × 3 次；原始 Pass@1=25/27（92.6%），Pass^3=7/9（77.8%）；veto=0、data-error=0、RUN 错误=0。与修正判卷后的提示词前基线 22/27、6/9 比较，记录级多通过 3 次、稳定用例多 1 条，仅作为小样本方向性证据。
- **仍有真实失败**：L4-211/seed=1 仍输出“需要确认型号、信息齐全可输出需求单”的内部分析，没有实际向用户索要型号；L4-212/seed=2 追问用户已说“一颗”的 CPU 数量。S3/S4 如实计失败。其余原五道初筛题及 L4-213/214 全部通过，不宣称完全消除初筛波动。
- **重放**：27 条完整 Verdict 和归因均与首跑一致，replay 退出 0，无跳过；run 退出 1 表示仍有失败。随后在同一正式 v1.3 跑完整 40 题 × 3 次，不拼接这轮输出。
- **产物**：`artifacts/eval/20260908-134804-1791488492/`；日志 `artifacts/eval/v1.3-screening-final.log`、`artifacts/eval/v1.3-screening-final-replay.log`。

## 2026-09-08 13:49 · v1.3 完整回归

- **固定口径**：正式 v1.3，40 题 × 3 次，共 120 次；清单 SHA256=`a0b94795457ff5fe83effc389826a2336df1888124b353bda32965188945adc5`。Builder=`qwen3.8-max-0902`、Screening=`deepseek-v4-flash-0731`、Embedding=`qwen3.7-text-embedding`；链为空，聊天 reasoning=none。2026-07-28 价格快照及保存的完整目录与前几轮一致，未改商品或价格。
- **原始结果**：Pass@1=**114/120（95.0%）**，Pass^3=**37/40（92.5%）**；veto=0、data-error=0、RUN 错误=0。120 个 `(case_id, seed)` 唯一且齐全，27 条 screening 原文完整留存，执行耗时合计 590.701 秒。run 退出 1，门禁未全绿。

| 分组 | 执行通过 | 三次全过的用例 |
|---|---:|---:|
| L1 整机 | 36/39 | 12/13 |
| L2 改单 | 30/30 | 10/10 |
| L3 初筛 | 15/15 | 5/5 |
| L4 边界与迁移题 | 33/36 | 10/12 |

- **迁移验收**：L4-209/210 各三次都返回缺已有件信息的 clarify，与新契约一致；这是期望迁移后的正确行为，不能当作模型突然修好了两道旧题。测试确认迁移前后 Requirement 完全一致、旧期望仍为 pass、L1-004 没有被替换或移除。L4-211/214 本轮各三次通过，原五道初筛题全部通过。
- **真实剩余失败**：L1-004 三次均在三轮选配后为 ¥3561.90，高于 ¥3300 上限。L4-212/seed=1、3 在用户未明确预算口径时直接输出 JSON，并自行填写 `budget_basis=new_purchase`，应追问而未追问，S1 失败。L4-213/seed=3 在用户没给已有显卡型号时输出缺少该型号的需求单，触发 S1/S3。这三次初筛失败不能通过加严“不要重复问”就视为修好；还要保证缺失字段确实被询问，不能反过来鼓励模型擅自补齐。
- **重放与验证**：当前代码零模型重放全部 120 条，完整 Verdict 和归因与首跑一致，replay 退出 0，无跳过，也未修改判卷来改变本轮结论。全项目 `go test ./...`、相关 `go vet` 通过；新测试覆盖 S4 正反例、原话误判、预算金额/口径区分、未知/重复/冲突字段、旧题不追溯加严及迁移输入保真。v1.1/v1.2/v1.3 哈希检查通过，涉及源码和文档 UTF-8/LF、本地链接及 diff 空白检查通过。本轮没有 Web 代码变更，不另报新的 Web 实测成绩。
- **产物**：`artifacts/eval/20260908-134941-411919689/`；日志 `artifacts/eval/v1.3-full.log`、`artifacts/eval/v1.3-full-replay.log`。首跑 binary SHA256=`e829ca44d478d30ced58d699941bb364521f20ce9bf204ca6ec83028fb58bb89`。原始 `results.jsonl` 和 meta 保留，报告更新为重放版；dirty 工作区身份及固定配置详见 meta。
- **后续焦点**：旧契约迁移与 S4 检查已完成。下一步应针对“缺预算口径却自行填值”和“缺已有件型号却输出需求单”加程序约束或结构化缺失字段处理，继续使用冻结 v1.3 验证；不再仅通过增加提示词句子或改期望追求全绿。数据采集仍在本对话范围之外。

## 2026-09-08 22:55 · 初筛程序约束定向复测

- **改动**：初筛共用模型包装层在输出事件及 OutputKey 写入前核验已声明的已有件型号和预算口径；无原话支持时输出字段级追问。产品上下文单独传入真实用户消息，不把助手举例当证据。评估额外保存拦截前 `model_text` 和 `missing_fields`，评分仍针对最终可见回复。提示词、题目、判卷和数据均未修改。
- **口径**：从冻结 v1.3 原样抽取 9 道 screening 题，独立清单 `v1.3-screening-guard-diagnostic`，共 27 次。三个角色的完整脱敏模型配置、2026-07-28 快照和完整目录与 13:49 基线逐项一致；Builder/Screening 仍为固定模型、空链、reasoning=none。
- **结果**：Pass@1=27/27，Pass^3=9/9，均为 100%；RUN、data-error、veto 均为 0；27 条原始模型文本完整留存，重放逐条一致。**本轮程序拦截为 0 次**，模型直接输出了正确行为，不能把全绿归因为程序修复，也不代表独立模型能力已稳定达到 100%。模型错误输入的程序拦截由纯函数及真实 ADK 假模型集成测试验证，后续完整回归另行记录。
- **产物**：`artifacts/eval/20260908-225527-3173152317/`；清单和原样题目副本 `artifacts/eval/screening-guard-diagnostic/`；日志 `artifacts/eval/v1.3-screening-guard-diagnostic.log`。binary SHA256=`70641d2ffce697b41b7ba261982a8b152f74c24135a46345ed424913805581a7`。

## 2026-09-08 22:57 · 初筛约束完整回归中断记录

- 使用上一节程序开始 v1.3 的 40 题 × 3 次回归。随后边界检查补充两项行为：型号不应被强制要求包含数字（例如 Fractal Design Terra）；模型遗漏或填反用户已明确的预算口径时，应按原话修正，而不是再次追问已知信息。
- 为避免把旧程序结果作为最终版本的验收，主动停止该进程并另起完整回归。已执行的控制台记录保留，但未生成完整 `results.jsonl`，**本轮无有效总分，不计作完整门禁，不与其他轮次拼接**。L1-004 已执行的失败仍保留在日志中。
- **产物**：`artifacts/eval/20260908-225709-3047715439/`（启动时保存的题目和元数据）；日志 `artifacts/eval/v1.3-screening-guard-full.log`。最终程序另见下一轮记录。

## 2026-09-08 23:02 · 初筛 JSON 约束完整回归

- **口径**：冻结 v1.3，40 题 × 3 次，完整题目清单、模型脱敏配置、2026-07-28 价格快照及完整目录与 13:49 基线一致，120 个 `(case_id, seed)` 唯一且齐全。未使用新价格数据，未修改判卷。
- **结果**：Pass@1=116/120（96.7%），Pass^3=38/40（95.0%）；screening 26/27，RUN/data-error/veto 均为 0；用例耗时合计 512.339 秒。重放完整断言和归因一致，run 退出 1、replay 退出 0。27 条模型原文完整保存，程序拦截为 0 次；不能用这次分差证明约束导致了提升。
- **失败**：L1-004 三次均为 ¥3561.90，超过 ¥3300 上限；L4-212/seed=3 回复“请提供已有内存的完整型号。”，实际用户只说已有明确型号 CPU，缺的是预算口径，触发 S3/S4。该错误是自然语言追问，JSON 约束不会接管，因此本轮不能宣称初筛修复完成。
- **额外审计**：运行期间补充了“不包括已有”“不计入已有”等否定口径表达的单元测试与规则。使用最终规则对保存的 27 条模型原文做零模型审计，最终文本及缺失字段逐条相同；不把新处理结果写回首跑记录。后续将已有件流程改为模型提取草稿、程序生成缺失字段追问，再独立复测。
- **产物**：`artifacts/eval/20260908-230205-2014828555/`；日志 `artifacts/eval/v1.3-screening-guard-final.log`；首跑 binary SHA256=`ec8ae378cd60ced74f40e749747846f33c22a0c7cd1709eb864ccb42656a7eb1`。当前程序审计命令见 Runner 设计。

## 2026-09-08 23:13 · 已有件结构化草稿定向复测

- **修复**：已有主机配件场景由模型提取内部 JSON 草稿，程序按已声明的品类、型号原话、费用口径和必填金额/用途/游戏分辨率生成追问。已明确的口径被遗漏或填反时按用户原话修正，既不擅自补齐未知项，也不反复确认已知口径。无已有件场景及 ChangeRequest 保留原协议；公共 schema、题目与判卷不变。
- **口径与结果**：同一 9 题诊断清单、旧快照和固定模型，27 次全部通过，Pass^3=9/9；RUN/data-error/veto 均为 0，重放一致。4 道已有件初筛题共 12 次均由程序依据草稿生成追问：L4-211 问 CPU/内存型号，L4-212 问预算口径，L4-213 问显卡型号，L4-214 问金额。原文和缺失字段均保存，不能把内部草稿当成已经对外发出的不合格需求单，也不能把最终成绩称为裸模型正确率。
- **产物**：`artifacts/eval/20260908-231339-320076954/`；日志 `artifacts/eval/v1.3-owned-draft-diagnostic.log`；binary SHA256=`dd397f6b6bf1850152aa2cb4dff87af043e6b80d1380a62732ddd8bb05482089`。本轮只是定向诊断，完整门禁见下一节。

## 2026-09-08 23:15 · 已有件结构化草稿完整回归

- **固定口径**：正式 v1.3，40 题 × 3 次；清单 SHA256=`a0b94795457ff5fe83effc389826a2336df1888124b353bda32965188945adc5`。Builder=`qwen3.8-max-0902`、Screening=`deepseek-v4-flash-0731`、Embedding=`qwen3.7-text-embedding`；完整脱敏模型配置与 13:49 基线一致，聊天 reasoning=none、切换链为空。逐条核对保存的目录和价格快照均与原 2026-07-28 基线一致，没有使用 9 月 8 日的新价格。
- **结果**：Pass@1=**117/120（97.5%）**，Pass^3=**39/40（97.5%）**；初筛 **27/27 全过**，RUN/data-error/veto 均为 0，执行耗时合计 506.448 秒。相对 13:49 的 114/120、37/40，原先缺口径/缺型号的两道初筛题本轮均三次通过。样本仍小，不据此承诺任意表达都已稳定。
- **程序作用**：12 次已有件初筛中，11 次由程序根据草稿生成缺失字段追问，另 1 次模型直接输出了正确追问；其余 15 次普通初筛通过原路径。保留 `model_text`、最终 `text` 和 `missing_fields`，能够区分提取与程序生成的结果。自然语言输出仍可能绕过草稿协议，复杂表达和提取遗漏仍是后续覆盖方向。
- **唯一失败用例**：L1-004 三次均未进入 ¥3300 预算上限，价格分别为 ¥3370.90、¥3561.90、¥3561.90；规则均 pass，最终按 A2/B6 计失败，未保存版本。没有改期望、移除该题、放宽预算或改成 data-error。因此 **run 仍为非全绿门禁**，不能宣称发布验收完成。
- **2026-09-09 收尾核验**：120 个 `(case_id, seed)` 唯一且齐全，27 条模型原文完整；正常 replay 对完整 Verdict 和归因逐条重放一致，退出 0。另用当前初筛程序对保存的 27 条模型原文做零模型审计，最终文本和缺失字段也全部一致。全项目 Go 测试通过，补充测试覆盖 ADK 可见事件/OutputKey、多轮补全、助手示例排除、截断来源、费用否定表达、无数字型号及 trace 往返保存。本轮没有 Web 源码或数据库写入，不另报 Web Live 成绩。
- **产物**：`artifacts/eval/20260908-231528-2436427345/`；日志 `artifacts/eval/v1.3-owned-draft-full.log`。首跑与上一节使用同一 binary SHA256=`dd397f6b6bf1850152aa2cb4dff87af043e6b80d1380a62732ddd8bb05482089`；首跑工作区 commit=`d619a31`、dirty=true，具体程序与脱敏配置见 meta。未拼接其他轮次的成功记录。
- **下一步**：保持当前题目、代码和模型，单独验证 2026-09-08 价格快照入库并做新旧数据对照，重点看 L1-004 和其他预算题。新价格不代表更便宜，结果变化归因于数据；旧门禁记录保留。之后再进入负反馈回流和少量真实失败变体题建设。

## 2026-09-09 09:18 · 新旧价格快照对照

- **固定条件**：旧对照为上一节 `20260908-231528-2436427345`，新轮 `20260909-091823-760092368`；均为 v1.3、40 题 × 3 次。完整题目副本、清单 SHA256、三角色模型全部配置和实际 runner binary SHA256=`dd397f6b6bf1850152aa2cb4dff87af043e6b80d1380a62732ddd8bb05482089` 完全相同。运行前逐条比对旧轮保存目录与当前数据库，零件规格、目录状态和全部向量也与复制后的评估库一致。新旧轮目录逐条比对仅 `PriceCNY` 不同；快照从 07-28 改为 09-08。编号代表独立重复，未传入模型随机种子，不能将每个输出差异都归因于价格。
- **入库**：发现原 `cmd/importprices` 拒绝混合采集日期，补充显式 `-snapshot-date`，将批次日期与每行观察日期分开。早于批次的行须匹配数据库中同 SKU、金额、来源、观察日期的历史价格，否则整批回滚；同文件改批次日期也拒绝。将当前库复制为本机独立库 `pcbuilder_eval_20260909_prices` 后导入，产品库仍只有 2026-07-28。新批次 160 行，102 条买手来源观察和 58 条沿用；58 条源快照、日期、来源和金额全部匹配原记录。仍为 `bootstrap`，不视为通过严格观察发布流程。
- **数据身份**：价格 CSV SHA256=`2d89751dd79674ab40b069c97cf495d1e789894733f20d79af97f2a5eeb86d67`，与归档 manifest 一致；offers 复核记录哈希也一致。本轮未修改 CSV、offers、规格或来源裁定，未重新联网证明聚合价格。

| 指标 | 旧价格 2026-07-28 | 新价格 2026-09-08 |
|---|---:|---:|
| Pass@1 | 117/120（97.5%） | 116/120（96.7%） |
| Pass^3 | 39/40（97.5%） | 38/40（95.0%） |
| build | 90/93 | 90/93 |
| screening | 27/27 | 26/27 |
| RUN / data-error / veto | 0 / 0 / 0 | 0 / 0 / 0 |

- **构建结果**：所有 93 次构建的通过/失败状态与旧轮相同；L1-004 新轮三次均报 ¥4832.00，仍为规则 pass、预算不达标、`search_exhausted/no_verified_solution`，A2/B6 失败。改单及已有件构建边界均通过。没有修改该题的成功期望或排除分母。
- **新增初筛失败**：L3-101/seed=2。用户原话“我想要一台安静的电脑打游戏,预算 8000,显示器是 2K 的”，模型未输出需求单，而是声称还需确认游戏用途、分辨率和已有配件情况，触发 S1。原文完整保存，属于真实多问而非判卷误读；该初筛路径不读取价格，因此不能把这次退步归因于新快照。其余两个重复通过，仅凭本轮也不判断为稳定故障。已有件初筛 12/12 通过，11 次程序生成追问；普通初筛不在已有件草稿接管范围内。
- **价格变化**：160 个 SKU 中 41 个降价、58 个涨价、61 个价格不变（102 个新观察中有 3 个金额不变）。44 个 SKU 的绝对变动超过 25%，作为复核候选，不直接判为错误。例：`mem-kingston-beast-16-5200` 从 ¥469 到 ¥2379，归档 accept 行指向同型号 16GB(8GB×2) 套装，来源为聚合结果；仍需详情页复核才能判断是否真实涨价或采集问题，不能自动删价或以旧价替换。
- **固定方案重计价**：保持旧 L1-004 的 SKU 与数量不变，仅替换价格，三份报价由 ¥3370.90/¥3561.90/¥3561.90 变为 ¥5913.00/¥5606.70/¥5606.70。这是确定性的价格影响诊断，不是重新生成方案的评估成绩。
- **目录预算下限证据**：对办公整机、无已有件、一块 SSD 的本题，枚举完整目录中满足 CPU 插槽/芯片组和内存代际的 1500 组平台组合；无核显者加最低独显价，其余电源、SSD、机箱、散热取各自最低价。忽略其余兼容约束只会让价格下限更乐观，得到旧轮 ¥3366.00、新轮 ¥3458.50，均高于 ¥3300。这证明在本题契约及保存的当前目录价格下无解，不是全市场无解，也不是一份通过全部规则的最便宜装机方案。该诊断暂未接入产品 `AssessCatalog`，当前程序仍只做各品类独立下限检查，不能宣称已经能自动输出这份更强的证明。
- **验证**：新轮 120 个记录唯一齐全、耗时合计 551.147 秒，run 退出 1；完整 replay 断言和归因一致、退出 0，另有 27 条初筛模型原文的程序核验审计全部一致。全项目 Go 测试、vet、lint 通过；导入器真实 PostgreSQL 集成测试覆盖旧价日期与源快照、新价日期、伪造金额/来源/日期回滚及幂等冲突。未改产品价格指针或保存装机版本。
- **产物与复算**：新轮 `artifacts/eval/20260909-091823-760092368/`，日志 `artifacts/eval/v1.3-new-price-full.log`；对照依据在 `artifacts/eval/price-comparison-20260909/` 的 `preflight.json`、`price-analysis.json`、`run-comparison.json`。本机可运行同目录 `analyze.py` 复算价格变化/下限，`compare_runs.py` 核对两轮题目、模型、程序、目录及逐题状态；二者零模型调用，不替代正式 replay、不覆盖首跑分数。
- **下一步**：优先修复普通初筛在信息齐全时仍追问的缺口，保留 L3-101 原话并补行为反例；随后将保守的平台预算下限证明接入产品，并按新版本用例检验诚实非交付，旧 v1.3 期望与历史失败不改写。44 条价格大幅变动交由数据任务单独核验，之后再进入负反馈回流，不通过重跑挑选全绿成绩。

## 2026-09-09 09:51 · v1.4 预算自适应契约与完整回归

- **用户确认的契约**：能够在预算内交付则交付，能证明当前目录预算不可达则解释限制并给出下一步，合理非交付可以通过。没有放宽预算、SKU/锁定/兼容校验，不接受无证据拒绝。旧 L1-004 保留 `pass`，新 ID L4-215 使用 `budget_adaptive`；输入与预算不变，其余 39 题原样保留。v1.4 清单 SHA256=`57407c9785ae964667f47cea9a6a37c9db0ae31c824dfa54b77ebd3c89f8d730`，默认 runner 已切换该版本。
- **程序处理**：新增办公新整机平台预算下限证明，基于完整目录的 CPU/主板/内存关系和显示输出，其他品类取独立最低价。未知价格、规格解析失败或过大枚举时放弃证明，未知规格按可能兼容处理。适用范围不含已有件、基版本和改单，原独立品类证明保留。证据足够时返回 `catalog_infeasible/platform_budget_lower_bound`，说明该金额不是交付报价，建议调整预算后重验、补充已有件信息或可核验商品；不自动提高预算、不保存配置、不调用 Builder 或 Embedding。
- **定向验证**：先用旧价格快照运行 L1-004 与 L4-215 各 3 次，二者都得到 ¥3366.00 下限；旧题 0/3、新题 3/3，均 0 次选配。诊断集为 3/6，run 退出 1，replay 退出 0，正好说明旧合同没有被追溯改判。产物 `artifacts/eval/20260909-095139-3640641123/`，日志 `artifacts/eval/v1.4-old-budget-diagnostic.log`；这是 2 题诊断，不冒充旧价全集。
- **完整运行条件**：v1.4 正式 40 题 × 3 次，价格固定 2026-09-08，独立评估库 `pcbuilder_eval_20260909_prices`。Builder=`qwen3.8-max-0902`、Screening=`deepseek-v4-flash-0731`、Embedding=`qwen3.7-text-embedding`，三角色完整配置和目录价格与上一轮新价格对照相同，切换链为空。题目迁移与程序变化分别记录，不混用 v1.3 分数。
- **结果**：任务处理正确率 Pass@1=**120/120（100%）**，Pass^3=**40/40（100%）**；veto/RUN/data-error 均为 0，耗时合计 522.172 秒。通过项为 **69 次成功交付、24 次按各题契约验证的合理非交付、27 次初筛正确**，不是 120 次交付。
- **非交付明细**：实际 24 次 = 12 次缺已有件信息追问（L4-201/202/209/210）+ 3 次未知已有型号（L4-204）+ 3 次原 L4-205 有界搜索停止 + 3 次 L4-203 独立预算证明 + 3 次 L4-215 平台证明。每项均按自身 fixture 验证；新 L4-215 不接受搜索耗尽替代预算证明。
- **分差解释**：相对上一轮新价格 116/120，多出的 3 次是 L1-004→L4-215 契约迁移与可复核程序证明，另 1 次是 L3-101 本轮恢复正确输出。初筛代码与模型配置未改，不能把这一次恢复当成修复；该偶发多问问题仍保留。已有件初筛 12 次均由程序根据模型草稿生成追问。
- **预算题实际输出**：L4-215 三次均为 `Succeeded=false`、Attempts=0、下限 ¥3458.50 > 上限 ¥3300.00，解释仅限当前目录，明确增加到下限也不保证配成。N1 从保存的完整目录、需求及程序说明重建证据，篡改金额、范围、说明、选配次数或缺失目录均不能通过；合格交付分支仍检查预算 veto。
- **验证**：120 个记录唯一完整，正式全集和旧价定向诊断 replay 的完整 Verdict/归因全部一致；27 条初筛最终回复及缺失字段通过原文核验审计。全项目 Go 测试、vet、lint 通过；新测试覆盖低价兼容路径、未知价格/规格、上限等值不拒绝、零模型路径、预算 veto、伪造非交付与报告分项。将旧 v1.3 两轮产物复制到 `artifacts/eval/v1.4-replay-compat/` 后用新程序重放，仍分别为 117/120、116/120，原目录结果未改写。
- **产物与边界**：完整运行 `artifacts/eval/20260909-095140-1050334509/`，日志 `artifacts/eval/v1.4-new-price-full.log`；实际 runner SHA256=`a7d18fce49caf9a82c2d28b24aefdf84706e5c4c3b6b5201bf6f58c6806028fe`，工作区 commit=`d619a31`、dirty=true。产品价格库和采集文件未改；单轮评估全过不代表普通初筛任意输入稳定，也不替代真人评审和 Web Live 发布验收。

## 2026-09-09：普通初筛草稿协议与预算金额来源修复

- **范围与条件**：仍为 v1.4 正式 40 题 × 3 次，清单 SHA256=`57407c9785ae964667f47cea9a6a37c9db0ae31c824dfa54b77ebd3c89f8d730`。固定独立评估库的 2026-09-08 快照；Builder=`qwen3.8-max-0902`、Screening=`deepseek-v4-flash-0731`、Embedding=`qwen3.7-text-embedding`，额度链为空。没有改题、改判卷或更换价格。
- **中间失败保留**：先将所有新装机统一为内部需求草稿，程序决定缺失必填项。10:40 全集为 **119/120、Pass^3=39/40**，成功交付 69、合理非交付 24、初筛正确 26，veto/data-error=0。L3-101 三次通过；唯一失败是 L4-214 seed=3，用户明确没给金额，模型却填写 `budget_cny=8500`，现有检查只看金额大于零而放行。这是程序缺少来源核验，不是题目错误。产物 `artifacts/eval/20260909-104012-911772433/`，runner SHA256=`9d61ef6f6e9f2264adacf8f577739718011fe76b2fc7a6b050e7e883da7f06cd`，同一程序 replay 保持 119/120、完整 Verdict 与归因一致。
- **修复**：新装机 JSON 草稿中预算金额必须在有界用户消息的预算/金额表达中有依据，不能仅因模型填了正数就接受；同时保留已有件型号和预算口径检查。不新增模型调用。单元测试覆盖凭空填值、型号数字误认、数字/千分位/千/万/k/中文整数、缺字段以及已有字段不重复追问。
- **修复后完整回归**：10:53 首跑为 **120/120、Pass^3=40/40**，veto/RUN/data-error=0，耗时合计 509.635 秒；其中 **69 次成功交付、24 次合理非交付、27 次正确初筛**。L3-101 和 L4-214 均三次通过。15 次初筛由草稿检查生成追问（缺分辨率 3 次、已有件相关 12 次）；L3-102 的 3 次缺预算直接追问仍由模型原始文本给出，不把它算成程序拦截。
- **验证与产物**：`artifacts/eval/20260909-105354-2212735039/`，日志 `artifacts/eval/v1.4-grounded-full.log`；runner SHA256=`7535250ce2ab747327acc535e8178c8cc53a3909020abb2e7446dafea01f5281`，首跑 checkout=`d619a31`、dirty=true。相同二进制零模型 replay 的完整 Verdict/归因一致，27 条保存原文的程序核验审计一致。Go 测试、vet、项目锁定版本 lint、PostgreSQL 集成测试通过。中间失败记录保留，不以修复后的处理结果覆盖。
- **解释与边界**：这次收益来自草稿协议和程序核验，不是换模型，也不是预算题再改合同。金额识别是保守文本规则，复杂多轮、更正和单件价格仍可能有歧义；模型完全不遵守 JSON 草稿协议时仍走原文本路径。全集通过只说明这 40 题的本轮结果，不替代真人评审和 Web Live 发布验收。同期反馈功能的数据库/HTTP/浏览器测试另见反馈设计文档，不计入本次 eval 分数。

## 2026-09-09：v1.5 多轮诊断首跑与首轮修复

- **新题来源**：在 v1.4 的 40 题之外新增 10 段人工设计的合成对话，实测前冻结输入与逐轮期望，不标成真实用户反馈。旧 40 题内容与哈希全部保留。对话复用产品有界上下文、无配置阶段和字段解码，逐轮保存原文；通过率按整段所有轮次正确计算。
- **11:53 首跑**：10 段 × 3 次，**16/30（53.3%），Pass^3=4/10**，veto/data-error=0。产物 `artifacts/eval/20260909-115307-997987568/`，runner SHA256=`f439b6c0d865cf817e28678af4c7ed1949a85a700ee6618ad1cafb877a48214c`。这是新题诊断，不能拿 53.3% 与旧 40 题的 100% 直接算退化。
- **失败证据**：L5-301 两次自行填写未提供分辨率；L5-304 一次把普通办公改成 productivity；L5-305 三次因程序将前后口径视为冲突而重问；L5-307 三次和 L5-309 部分轮次把需求确认提示误判为已保存 v1，输出 ChangeRequest；L5-310 两次将撤回预算当改单或沿用旧金额。完整原文和每轮原因在 results.jsonl。
- **程序修复**：由真实运行类型传入是否已有配置版本，无版本的新需求使用独立草稿指令；已有配置仍保留改单协议。分辨率必须有用户来源，撤回的预算失效，明确的新采购口径覆盖前轮说明，同一条消息内冲突仍追问。
- **12:00 复测**：同一 10 段 × 3 次为 **29/30（96.7%），Pass^3=9/10**，veto/data-error=0。产物 `artifacts/eval/20260909-120049-1389403608/`。剩余 L5-301 seed=3 第三轮业务字段正确，但漏 schema_version。后续仅由程序给明确新需求补缺失的协议常量 1，显式其他版本不覆盖；业务字段仍按用户来源核验。
- **条件与边界**：两轮均固定 09-08 独立评估快照、原 Builder/Screening/Embedding 模型，无额度切换；题目和判卷未为修复而修改。多轮新题不执行装机生成，这两轮均不是 50 场景全集发布结论；正式全集回归另记。

## 2026-09-09：v1.5 全集回归暴露来源识别和输出协议问题

- **固定条件**：v1.5 正式 50 题 × 3 次，清单 SHA256=`5a3a4fdf5b6badc42a5369febb21c4c2eab009f1ddadc38b931a92f99ca19ad3`；原 v1.4 的 40 题内容和哈希全部保留。独立评估库、2026-09-08 价格快照、Qwen Builder、DeepSeek Screening 和 Qwen Embedding 均未更换，模型链为空。
- **12:04 全集**：**139/150（92.7%），Pass^3=45/50**；69 次交付、24 次合理非交付、18 次单轮初筛正确、28 次对话正确，veto/RUN/data-error=0。耗时合计 618.291 秒。产物 `artifacts/eval/20260909-120435-802325508/`，日志 `artifacts/eval/v1.5-full.log`，runner SHA256=`28fedbe7bf11aeeff0d20aa5e16333d728688393c7b2625001871794f8adbf6a`。
- **失败与修复**：L3-101/104/105 各三次，模型实际给对分辨率，程序来源识别却漏掉带空格及“2K 玩游戏”的写法，是新程序造成的旧题回归，已补回归测试并修正匹配。L5-308 一次 schema_version 为字符串 `"1"`，L5-309 一次 notes 为 `[]`；仅规范协议常量和空说明，显式其他版本或非空数组不覆盖。
- **12:17 全集复测**：**148/150（98.7%），Pass^3=48/50**；69 次交付、24 次合理非交付、26 次单轮初筛正确、29 次对话正确，veto/RUN/data-error=0。耗时合计 614.022 秒。产物 `artifacts/eval/20260909-121727-33582519/`，日志 `artifacts/eval/v1.5-full-verified.log`，runner SHA256=`1039a7985985892638b969a7e60661133a48f639ecef7bfcf8ade6388ae81793`。
- **余下失败**：L3-102 第一次输出带 `intent:new_build` 的空字段对象，绕过原需求草稿缺失项处理；L5-302 第一次第二轮将 `noise` 填入硬件品类 priority。属于模型协议错误，不改题、不放松枚举来通过。后续明确 priority 的合法值，并仅在程序确认无配置版本且 JSON 协议非法时增加最多一次格式重试；缺失信息已形成追问时不重试，第二次仍错则保留错误。每次原文另存 model_attempts，调用和 token 计入本轮。
- **用量记录起点**：12:17 首次完整记录 profile 和用量：attempt_limit=3、semantic=true；225 次逻辑模型调用均返回用量，输入 485515、输出 24631、总 token 510146；15 次 Embedding 调用。逻辑调用不含适配器内部 HTTP 重试，不含 Embedding token，不换算费用。早期未记录用量的轮次不补造。
- **重放验证**：11:53、12:00 两轮诊断及上述两轮全集，按保存题目、每轮真实上下文、有效载荷和完整断言/归因重放均一致（退出 0）；失败仍分别为 14、1、11、2 次。这里的多轮正确率是模型与程序共同处理的结果，不是模型原始协议正确率。

## 2026-09-09 12:30：v1.5 固定模型基线完成

- **结果**：正式 50 题 × 3 次，**150/150（100%），Pass^3=50/50**；69 次成功交付、24 次合理非交付、27 次单轮初筛正确、30 次多轮对话正确，veto/RUN/data-error=0。耗时合计 623.740 秒。原 v1.4 的 40 题及新增 10 段对话期望均未为分数而修改。
- **口径**：清单 SHA256=`5a3a4fdf5b6badc42a5369febb21c4c2eab009f1ddadc38b931a92f99ca19ad3`；独立库 2026-09-08 价格快照，Builder=`qwen3.8-max-0902`、Screening=`deepseek-v4-flash-0731`、Embedding=`qwen3.7-text-embedding`，模型链均为空。正常最多 3 次生成、语义检索开启。
- **实际格式重试**：L3-102 第一次重复触发一次重试，其余初筛轮次未触发。失败原文与重试原文都保存，没有把程序修复后的结果冒充一次原始模型全对。多轮对话复用产品“尚无配置”的状态与真实前轮回复，范围仍不包含 Builder 执行或浏览器完整流程。
- **用量与候选**：225 次逻辑模型调用均有用量，输入 489812、输出 25472、总 token 515284；15 次 Embedding 调用。72 条构建记录保存实际候选副本，其余提前非交付未准备候选，不能当作缺失检索结果计零分。未测量内部 HTTP 重试及 Embedding token，不换算费用。
- **验证**：完整 150 条记录的 Verdict/归因重放一致；57 段初筛执行（含 30 段多轮）的最后模型原文、用户来源、每轮核验输出与缺失字段通过零模型审计。格式重试限制、来源更正、原文留存、候选隔离和对照拒绝混杂配置的测试通过；全项目 Go 测试、vet、固定版本 lint 通过。
- **产物**：`artifacts/eval/20260909-123021-1048616481/`，日志 `artifacts/eval/v1.5-protocol-full.log`；实际评估二进制 `bin/eval-v1.5-protocol.exe`，SHA256=`7cfc87205628d606cfe0478f069a19446fa2cddefc9727797bfb0321f6d3f3fc`，首跑 checkout=`7e05bb1`、dirty=true。后续对照使用同一二进制，不能用工作区 commit 名替代实际程序哈希。
- **下一步**：默认评估集切换 v1.5；按已预定顺序进行单次生成、关闭语义检索、仅换 Builder、仅换 Screening 的配对实验。这次基线不证明机制收益或模型排名，真实对照另记。

## 2026-09-09 12:41：修复环单次生成对照

- **唯一变量**：对比 12:30 正常组 A，本轮 B 仅将 attempt_limit 从 3 改为 1；使用相同 v1.5 清单、完整目录/09-08 快照、三模型配置和二进制 SHA256=`7cfc87205628d606cfe0478f069a19446fa2cddefc9727797bfb0321f6d3f3fc`。每题三次，A 后 B，未随机化顺序；重复编号不是共享随机种子。
- **全集留档**：B 为 98/150（65.3%）、Pass^3=31/50，52 次失败，veto/RUN/data-error=0。18 次交付、24 次合理非交付、26 次单轮初筛正确、30 次多轮对话正确；总耗时 426.652 秒。另一次 L4-213 初筛失败属于未改变阶段，按预定口径不计入修复环效果。
- **装机阶段主结果**：A 为 93/93、Pass^3=31/31；B 为 **42/93（45.2%）、Pass^3=13/31**。成功交付 69→18，合理非交付均 24。逐次配对仅 A 通过 51、仅 B 通过 0、双方通过 42、双方失败 0；平均题目通过率 B−A=−54.84 个百分点，按 31 题聚类 bootstrap 95% 区间 [−70.97, −38.71]。包含 7 道程序提前非交付、两组均零模型的题，不能将它们当作修复收益。
- **失败成因**：B 的 51 次装机失败全部落在预算区间外，其中 **45 次低于下限、6 次高于上限**；兼容报告为 pass 39 次、review 6 次、fail 6 次。当前分差主要体现预算调整能力，不能直接说 51 次首轮配置全不兼容，也不能把满足预算下限等同于性能或用户价值提高。A 的成功交付中 19 次用一轮、50 次用两轮；两组独立生成，不能声称同一个首轮答案逐条得到了反事实复验。
- **装机阶段资源**：模型逻辑调用 128→72，Embedding 均 15；已知 token 429969→347960（输入 410468→337014，输出 19501→10946），所有模型调用都有用量。耗时 423.960→228.005 秒。单次组调用少 43.75%、token 少 19.07%、耗时少 46.22%，同时任务通过率显著下降；未换算费用，不自动更改生产策略。
- **新发现与边界**：单次组实际 Attempts=1、逻辑调用也为 1，但失败说明仍写死“三次选配后”，这是实验开关未同步文案的缺陷，结构化次数及判卷不受影响。L4-213 的原文缺少外层 JSON 结束符，内层对象被误当草稿，导致重问已知预算/用途；不是本轮修复开关造成。两项保留为实验后代码修复目标，后续组继续使用同一冻结二进制，不能中途换程序混算结果。
- **验证与产物**：B 产物 `artifacts/eval/20260909-124127-1013495092/`，日志 `artifacts/eval/v1.5-ablation-repair-single.log`；完整 replay 一致。配对工具复验清单、逐题快照、判卷和上下文，报告保存于 `artifacts/eval/20260909-repair-comparison/`；其中 19 道其他阶段题明确排除主指标，未隐藏原全集失败。

## 2026-09-09 12:50：关闭语义检索对照

- **唯一变量**：C 仅 semantic=false，attempt_limit 保持 3；与 A 同一 v1.5 清单、三次重复、完整目录/09-08 快照、模型和冻结二进制 `7cfc87205628d606cfe0478f069a19446fa2cddefc9727797bfb0321f6d3f3fc`。按 A→B→C 顺序执行，不中途修改程序或候选资料。
- **结果**：C 全集 **149/150、Pass^3=49/50**，veto/RUN/data-error=0，总耗时 644.838 秒。装机阶段 A/C 均为 **93/93、Pass^3=31/31**，69 次交付、24 次合理非交付；主指标差 0，31 题聚类 bootstrap 区间 [0,0] 只描述本题库同分，不能证明普遍等效。另一次 L4-214 seed=2 重问已知用途，属于未更换的初筛阶段，不计入检索收益。
- **调用与用量**：装机阶段模型调用 128→130、Embedding **15→0**；输入 token 410468→399042、输出 19501→19751、合计 429969→418793。耗时 423.960→438.091 秒。省下向量调用不等于整轮更快，生成次数与延迟仍波动；这次不据耗时变化自动调整生产策略。
- **确实触发的五题**：L1-008/013/015（安静）、L1-009（白色海景房）和 L2-108（安静改单）。每题三次的正常组均有一次 Embedding，关闭组零调用；其他题不调用。保存候选证明五题每次均有变化：前四题各少 5 个语义补入 SKU，L2-108 各少 3 个，均无新增 SKU；完整逐题原文与候选差异在配对 JSON。
- **实际选用差异**：四道静音题正常组均选中带低噪文字的 be quiet! 机箱与 Dark Rock 散热器；关闭组改用 Montech AIR 903 MAX 和 AG400/AK620 等核心候选。白色海景房题正常组选择 Lian Li O11 Dynamic EVO，关闭组为 Montech。上述差异可从实际选中 SKU 与本轮匹配文本逐字复核，不是根据分数推测。
- **证据限制与发现**：低噪文本没有统一实测条件/数值，不能证明实机更安静。L1-009 选中型号明确为 **O11 Dynamic EVO (Black)**，但检索文本同时描述系列的白色版本，最终理由只截取“偏好匹配”与参数，没有解释白色要求未满足。当前硬断言没有为此扣分，说明软偏好和解释质量覆盖不足；不得把 93/93 当作白色/静音已满足，也不把系列其他版本的描述当作选中 SKU 的颜色证据。该样本进入后续解释质量诊断。
- **验证与产物**：C 产物 `artifacts/eval/20260909-125022-3818495549/`，日志 `artifacts/eval/v1.5-ablation-semantic-off.log`；完整 replay 一致。配对工具复验通过，结果在 `artifacts/eval/20260909-semantic-comparison/`，主表只统计 31 道装机题，其他 19 题明确排除。生产语义检索暂保持开启；下一步优先验证更细的属性证据，而非根据硬断言同分删掉检索。

## 2026-09-09 13:02：仅替换 Builder 为 DeepSeek

- **唯一变量**：D 将 Builder 从 qwen3.8-max-0902 改为 deepseek-v4-flash-0731，Screening 保持 DeepSeek；其余模型参数、清单、快照、三次重复、三轮机制及冻结二进制与 A 相同。进程级环境覆盖，产品 `.env` 未改、额度链为空。报告工具已验证只有 Builder 模型名变化。
- **全集结果**：149/150、Pass^3=49/50，veto/RUN/data-error=0，总耗时 583.147 秒。唯一失败 L5-303 seed=2 第二轮未给需求单，发生在未改变的 Screening；不归因到 Builder 选型。
- **Builder 主结果**：A/D 装机均 **93/93、Pass^3=31/31**，69 次交付、24 次合理非交付。按题目聚类的分差区间 [0,0] 仅说明这轮同分，不能证明更广需求下等效。A 的交付为 19 次首轮成功、50 次第二轮成功；D 为 13 次首轮成功、56 次第二轮成功，另外均有 3 次搜索耗尽和 21 次提前非交付。
- **装机阶段资源**：模型调用 128→134，Embedding 均 15；已知输入 token 410468→396152，输出 19501→19599，总量 429969→415751（少约 3.3%）。耗时 423.960→387.730 秒（少约 8.5%）。本轮 DeepSeek 单次较快但需要的调用更多；运行顺序、缓存及上游波动没有随机化控制，不据此自动改生产模型，也不换算费用。
- **验证与产物**：D 原运行 `artifacts/eval/20260909-130258-3875612415/`，日志 `artifacts/eval/v1.5-model-builder-deepseek.log`；完整 replay 一致。配对结果 `artifacts/eval/20260909-builder-comparison/`，程序 SHA256 仍为 `7cfc87205628d606cfe0478f069a19446fa2cddefc9727797bfb0321f6d3f3fc`，19 道其他阶段题明确排除主指标。下一轮只更换 Screening，Builder 使用基线 Qwen。

## 2026-09-09 13:14：仅替换 Screening 为 Qwen

- **唯一变量**：E 仅把 Screening 改为 qwen3.8-max-0902，Builder 保持基线 Qwen；清单、快照、三次重复、正常机制、其余模型参数及冻结二进制均与 A 一致。产品配置未改，模型链为空。
- **结果**：全集 148/150、Pass^3=49/50，veto/RUN/data-error=0。初筛相关主指标 A/E 为 **57/57→55/57、Pass^3=19/19→18/19**；E 单轮 27/27、多轮 28/30。分差 −3.51 个百分点，按题目聚类的 95% bootstrap 区间为 [−10.53,0] 个百分点。其他 31 道装机题排除主指标，不能把装机成绩算作初筛收益。
- **具体失败**：L5-306 第一次、第二次重复的首轮，用户已说“日常办公的新电脑，预算还没确定”，Qwen 只输出 schema_version，遗漏已知办公用途。程序因此同时追问预算和用途，触发 S4；第二轮八千五百元能正确抽取。这里是完整合法草稿遗漏已知事实，与未闭合 JSON 不同，不能声称格式重试修复覆盖了此问题。
- **初筛资源**：逻辑调用 97→96，已知输入 token 79344→79695、输出 5971→3318、合计 85315→83013（少约 2.70%）；耗时 199.780→148.814 秒（少约 25.51%）。本轮速度和小样本分差不构成生产自动选型依据，继续使用 DeepSeek Screening。
- **验证与产物**：E 原运行 `artifacts/eval/20260909-131440-1808097164/`，日志 `artifacts/eval/v1.5-model-screening-qwen.log`；完整 replay 一致。配对报告 `artifacts/eval/20260909-screening-comparison/` 已验证唯一变量、完整题目和证据，保留逐题重复结果。

## 2026-09-09 13:25：解释诊断首跑，先暴露评分协议问题

- 固定 A 的第一次重复、八题清单和 v1 规则，异构 Judge=deepseek-v4-flash-0731，每题三次。L1-001、L4-206 无理由，六条记录由程序标缺失、记 0 档且不调用模型。
- 其余 18 次调用只有 **4 次有效、14 次 invalid_judgement**；无效评分主要有 Markdown 围栏和引用改写/重组，未进入均分。即使围栏内的分数看似合理也不能事后当作有效评分。18 次均有用量：输入 41268、输出 7972、合计 49240 token；耗时 95.578 秒。
- 原产物 `artifacts/evaljudge/20260909-132555-1408332717/`，日志 `artifacts/eval/judge-v1-baseline.log`；原始评分程序 `bin/evaljudge-v1.exe` 重放一致，包含全部错误。此结果首先说明评测系统输出协议不足，不据此评价产品解释质量。
- 修正方向：请求 JSON 输出类型，并展开事实原字符串，避免 JSON 再序列化一层；明确只截取连续短引用。严格 JSON 和逐字证据验证保持不变，评分规则与八题选择不变。使用新评分二进制重跑，首跑不覆盖；这次变化属于 Judge 协议修复，不是产品改进。

## 2026-09-09 13:31–13:38：解释诊断协议复验与评分局限

- **固定内容**：继续使用 A 的第一次重复、同一八题清单与 v1 评分规则，Builder Qwen、Judge DeepSeek。所有样本事实和解释均从原产物提取；六条空解释记录仍由程序判缺失。下表的有效率分母只包含实际 18 次评分调用，不是八题的产品通过率。

| 评分程序与产物 | 有效评分 | 无效评分 | 已知输入/输出/合计 token | 累计耗时 |
|---|---:|---:|---|---:|
| `evaljudge-v1.exe`；`20260909-132555-1408332717` | 4/18 | 14 | 41268 / 7972 / 49240 | 95.578 秒 |
| `evaljudge-v1-protocol.exe`；`20260909-133123-434120322` | 10/18 | 8 | 40026 / 7397 / 47423 | 91.668 秒 |
| `evaljudge-v1-schema.exe`；`20260909-133656-494345765` | 10/18 | 8 | 40026 / 6629 / 46655 | 89.788 秒 |

- **协议变化**：第二轮展开原始字符串并声明 JSON 输出类型；第三轮再声明包含三个维度、分数、理由及引用的结构 schema。评分标准未改；每轮各保留首跑、逐次错误，不混合挑选有效分数。结构 schema 是请求约束，实测上游仍会返回围栏、错误 JSON 或不存在的引用，因此不能称作可靠强制结构化输出。
- **可复核发现**：第二轮 L1-009 两次有效诊断的需求关联和下一步均为 0，指出 Black 机箱没有说明白色偏好未满足。第三轮静音 L1-008 三次有效诊断为依据表达平均 0.67/2、需求关联 1/2、下一步 1.33/2；L1-013 仅一次有效为 1/1/1，样本过少。该轮 L1-009 三次全部无效，不能搬用前轮分数填空。L1-001、L4-206 原解释为空，也不能靠补写理由消除诊断缺口。
- **Judge 语义误判**：L4-201 需求已写 `budget_basis=new_purchase`，原文却再次要求确认预算口径；第二轮三次、第三轮两次有效评分仍给三个维度 2 分。逐字引用合格并不意味着符合评分规则，这与 v1 锚点中的“重问已知口径应降低针对性”存在明确分歧。这里只是 AI 编程助手对照字段发现的规则冲突，**不是独立人工校准**，不能计算人工一致率或将评分接入发布门禁。
- **产品修复**：已有件澄清改为只询问实际缺失字段，不重复确认已知预算口径，不追问未缺失的数量；说明模板版本化，旧版文案仍精确重放。Black/白色冲突、尝试次数文案与截断 JSON 的修复另用新评估程序验证，不改 A–E 历史产物。
- **验证与产物**：上述目录均位于 `artifacts/evaljudge/`，三轮重放一致；当前源码还增加来源记录需求与冻结题目的一致性检查，并通过第三轮产物重放。第三轮评分程序 SHA256=`fcc745a7844ec2a05c8b2aeec8cf573b4e4d4d173507cee12960b6c16926e584`。协议/评分尝试与产品回归部分重叠，仅记录耗时，不作延迟收益结论。现阶段工具用途是暴露质量问题和保存可复核证据，独立人工校准及可靠自动评分仍待后续。

## 2026-09-09 13:33–13:44：前三项产品修复完整回归

- **修复范围**：失败次数文案使用实际尝试数；新需求外层 JSON 截断进入至多一次协议重试，不能捞内部对象产生无关追问；白色偏好命中 Black 型号时明确说明未满足，未知外观和噪声不写成已验证。该二进制尚未包含随后发现的已有件澄清文案修复，不能当作最终代码验收。
- **结果**：v1.5 50 题 × 3 次，**150/150、Pass^3=50/50**，69 次交付、24 次合理非交付、27 次单轮初筛、30 次多轮对话；veto/RUN/data-error=0。模型、三轮机制和 09-08 独立评估库快照与 A 一致，题目未改。
- **实际说明**：L1-009 仍选中 O11 Dynamic EVO (Black)，解释已明确“未满足白色机箱偏好”，要求核验对应白色 SKU；修复的是诚实说明，没有凭文案变成白色交付。L4-201 此轮仍为旧版重复口径追问，保留原记录，后续新版本单独验证。
- **资源与产物**：225 次模型调用均有用量、15 次 Embedding；已知输入 490546、输出 25653、合计 516199 token；耗时合计 676.485 秒。产物 `artifacts/eval/20260909-133320-666574533/`，日志 `artifacts/eval/v1.5-final-full.log`；二进制 `bin/eval-v1.5-final.exe`，SHA256=`db27f03a6f80ae3188b8312ed83cff3e43ee37c0cb4ea3c2fd7dea6eee29dcea`。与 Judge 部分并行，仅记录耗时，不做速度比较。
- **复验**：原二进制和包含新说明版本兼容逻辑的程序都 replay 一致；当前初筛核验审计重现全部 57 次初筛/多轮输出及缺失字段。当前 `evalcompare` 对 A/B、A/C、A/D、A/E 再次完整复验，四份 `comparison.json` 与原报告完全一致；新复验目录分别为 `artifacts/eval/20260909-{repair,semantic,builder,screening}-current-verification/`。

## 2026-09-09 13:44–13:55：包含针对性澄清的最终完整回归

- **最终代码结果**：v1.5 50 题 × 3 次，**150/150、Pass^3=50/50**；69 次交付、24 次合理非交付、27 次单轮初筛、30 次多轮对话，veto/RUN/data-error=0。固定三款模型、模型链为空、最多三轮选配、语义检索开启、09-08 独立库快照均与 A 相同；旧 40 题清单条目及哈希未变，10 段多轮题期望未变。
- **真实针对性说明**：L4-201 三次均只问“请补充已有CPU的完整型号。”；L4-202 三次均只确认缺失的预算口径，不重复问已知型号。新版 Decision 明确记录 `explanation_version=1`。L1-009 三次均明确 Black 型号未满足白色偏好，要求核验对应白色 SKU；不把说明改善当成机箱颜色已改变。
- **资源与边界**：224 次模型调用均有用量、15 次 Embedding；已知输入 489092、输出 25077、合计 514169 token；耗时合计 627.609 秒。本轮没有触发格式重试，截断外层 JSON 的错误与修复路径由真实失败原文的定向测试覆盖；不能把本轮无重试解释为已验证任意上游格式错误。通过率仍是模型与程序组合的结果。
- **产物与被测程序**：`artifacts/eval/20260909-134447-3770035618/`，日志 `artifacts/eval/v1.5-closeout-full.log`；二进制 `bin/eval-v1.5-closeout-v2.exe`，SHA256=`0b8f9ed004f4a3f88790d4330aac53989c59770a2b63e6ac3a38fbbe45cc3609`。首跑 checkout=`a7c5878`、dirty=true（当时尚有运行文档待提交）；对应产品修复已在 `c6d69f9`，解释诊断在 `a7c5878`。原 A–E 冻结二进制及产物未覆盖。
- **验证**：完整 replay 一致；保存原文再次执行当前初筛核验，57 次单轮/多轮回复及缺失字段全部重现。最终源码 `go test ./...`、`go vet ./...` 与固定版本 golangci-lint v2.12.2 均通过，lint 为 0 issues；日志分别为 `artifacts/eval/closeout-final-{tests,vet,lint}.log`。另有版本 0/1 澄清精确重放、伪造说明拒绝、Judge 原文/分数/需求篡改拒绝和颜色证据反例测试。

## 2026-09-09 13:55：最终版本解释诊断与本轮收尾

- **来源与口径**：对最终运行 `20260909-134447-3770035618` 的第一次重复，继续使用同一八题清单、v1 分档规则、JSON schema 请求和 DeepSeek Judge，每题三次。原规则与清单哈希不变；来源是包含数项修复的新运行，不将本轮与 A 的分差宣称为某一个修改的因果收益。
- **结果**：两题空解释共六条记录仍为 missing_explanation，零模型调用。其余 **18 次调用中 8 次有效、10 次无效**：七次围栏、两次错误 JSON、一次不存在的引用，全部原样保留，不进入均值。有效率不足以支持可靠自动评分，也不能通过只挑有效轮次宣布解释质量全面通过。
- **改进证据**：L1-009 两次有效诊断均给 2/2/2，引用明确的 Black 型号、白色偏好未满足和核验对应 SKU 的下一步；另一次无效。L4-201 三次有效均为 2/2/2，原文确实只要求已有 CPU 完整型号。其改善首先由冻结需求、缺失字段及实际文案证明；由于修复前 Judge 也曾误给高分，不能用其分数证明该项改善幅度。
- **保留限制**：L1-001、L4-206 解释为空；L1-008、L1-013 各只有一次有效评分，仍缺独立噪声证据；L4-215 三次评分均无效，未评分，不借用旧轮分数填充。独立人工校准与真人评审仍未完成。本轮完成的是预定的多轮基线、四组对照、诊断工具实跑及发现项修复回归，不是产品所有体验质量或公开发布验收完成。
- **用量与产物**：18 次调用均有用量，输入 39882、输出 6678、合计 46560 token；耗时 96.704 秒。产物 `artifacts/evaljudge/20260909-135536-2559582244/`，日志 `artifacts/eval/judge-v1-closeout.log`；评分程序 `bin/evaljudge-v1-closeout.exe`，SHA256=`af7f21799fa434e8090d1aee2f6f944feca36b4d90168d8f1adfbf9917d66eab`。完整来源校验与零模型重放均通过，评分调用因保留无效结果而退出 1、重放一致退出 0，二者含义不同。
- **完成边界**：10 段新多轮场景、原 40 题保留、固定模型基线、两种消融、两个角色的独立模型对照、异构解释诊断和四项产品修复均已有实际证据。生产仍使用 Qwen Builder、DeepSeek Screening，额度链为空；产品库和价格数据未在本轮修改，未推送远端。后续按真实自用反馈或独立可核验的软偏好证据演进新题；Judge 可靠性与人工校准单独推进，不靠继续刷同一组题提高数字。


## 2026-09-09 14:34–14:49：变更回归设施实测，保留一题真实退步

- **本轮目标与入口**：新增 cmd/evalchange 的零调用 plan、显式限额 run、离线 compare，固定基线完整题目、模型配置、完整目录/价格快照及 Harness 机制；允许新旧二进制不同。构建输入摘要关联实际程序，逐题即时保存证据；旧基线不会自动替换。本轮修改评估设施，产品提示词和 Screening 核验逻辑未改。
- **判卷修补**：新 grader_version=independent-v2 从冻结证据核查本轮候选成员及品类、兼容规则重算与实际选件报价；缺证据明确失败。反例覆盖“目录有但未给模型”、错品类、Succeeded=true 却保存 fail、规格不兼容却保存 pass、自洽但价格不符等。旧基线 20260909-134447-3770035618 的 150 条记录按新口径复核，成绩改变 0 条；新旧原成绩与同口径成绩分别保留。
- **真实条件**：v1.5 清单 SHA256=5a3a4fdf5b6badc42a5369febb21c4c2eab009f1ddadc38b931a92f99ca19ad3，50 题 × 3 次，09-08 独立评估库；Qwen Builder、DeepSeek Screening、Qwen Embedding，链为空、最多三次、语义检索开启。模型与 Embedding 合计上限 300 次逻辑调用，实际 244 次，未触顶。
- **结果**：149/150、Pass^3=49/50；69 次交付、24 次合理非交付、27 次单轮初筛、29 次多轮对话正确。veto/RUN/data-error 均为 0。对照相对原基线识别 1 题退步、0 题改善，29 题共 60 次输出变化；门禁退出 1，没有将这轮升级为基线。输出变化不全是质量变化。
- **失败证据**：L5-301 / 第二次重复 / 第三轮，用户明确说“用2K分辨率。”，模型草稿 budget_cny=6000、use_case.type=gaming、resolution="2k"。最终可见输出再次询问分辨率，missing_fields=["resolution"]，触发 S1。来源核验 groundedResolution 将来源识别为规范值 2K，最后与模型值作区分大小写的相等比较；这里的小写值因此没有通过。该问题由真实原文和当前代码定位，题目期望没有改写，也不能把一次波动归因成此次评估设施修改导致产品退步。修复规范化与来源核验列入后续，首跑失败保留。
- **资源差异**：模型调用 224→229（+5），Embedding 15→15。新增调用来自 L2-102 首次重复的尝试数 1→3（+2），以及 L2-105 三次重复各 1→2（+3）；两题最后仍通过。全部 229 次模型响应返回 usage；已知输入 496668、输出 25740、合计 522408 token，比基线多 8239。累计用例耗时 728.067 秒，比基线多 100.458 秒，其中 L5-305 一次约 45 秒；不将顺序实跑、缓存与上游延迟波动当因果效果，不换算费用。
- **产物**：工作目录 artifacts/evalchange/20260909-143405-run-1286854101/；真实运行在其 runs/20260909-143433-2017771388/，程序 SHA256=e8c4dd89ce8ab2910f2edeaa57acf8bf7c3aac5ce79c3ce28ef8de7cdf745de6。自动对照在其 20260909-144645-comparison-2009184032/；随后当前源码补充逐次调用/尝试字段及元数据检查，再零调用比较，最终可读报告在 artifacts/evalchange/final-verification/20260909-144815-comparison-629454902/。未覆盖真实二进制、题目、模型原文或首跑成绩。
- **验证**：基线自身离线对照为零差异；当前源码重放本轮断言与归因一致（含失败），保存的 57 次单轮/多轮原文核验均可复现。用量未知、模型/快照/机制混变、缺重复和伪造记录的反例测试通过；调用上限测试证明拒绝后的请求不计作已发送，未执行题不缩小分母。最终 go test ./...、go vet ./...、golangci-lint v2.12.2 通过（0 issues），日志在 artifacts/evalchange/。附加 -race 检查在本机 runtime/cgo 编译阶段退出 2，尚未执行测试，保留 race.log/race-diagnostic.log，不宣称该项通过。
- **自动化边界**：新增 eval-offline 工作流文件，当前 v1.1–v1.5 冻结校验本机通过；v1 的两条旧追问题需从历史副本恢复，未修改旧哈希。远端 Actions 未在本轮验证，未配置定时真实跑测或自动上传轨迹。生产长期监控、向量索引身份、独立人工校准仍是后续工作；本轮完成的是修改后可执行、可复核、可看逐题差异与调用代价的本机回归入口。


## 2026-09-10 · 动态需求状态栏定向验收

- **范围**：当前会话需求草稿、字段来源、面板/聊天同一归并、确认与历史版本保护；详见验收记录（2026-09-09/10，文档已清理）。
- **离线结果**：Go全测试、build/vet、Store独立PG集成、真实HTTP/PG三视口浏览器通过；旧150条基线零调用重放一致，新协议14轮保存真实输入以明确标注的人工操作oracle复验。
- **真实调用**：固定当前Screening deepseek-v4-flash-0731，累计4次、0 Builder、0 Embedding、6,135 tokens，无重试/模型链；不运行全量评估。
- **发现与修复**：第三次明确撤销操作缺quote，被服务端拒绝；增加仅匹配本轮明确字段撤销的来源绑定，录制输出离线回归通过。剩余1次从第2轮状态续跑，撤销品牌和4K备选隔离均通过。原失败证据保留，累计记录不可重复加总。
- **边界**：浏览器配置来自保存真实生成产物回放并重新核算，验证业务与版本链路；没有将其当成实时Builder重新配单成绩。跨会话记忆及全面真实模型回归仍未建设/执行。
- **产物**：artifacts/requirements-state/，固定模型结果与截图链接见定向验收文档。

## 2026-09-22 · Jev 影子意图评测首跑（shadow-live，change: jev-shadow-intent-evaluation）

- **口径**：冻结套件 `internal/planningeval/testdata/jev-intent-v1/`（sha256 `0a81b9b6…`，37 案例 / 63 个带 `expect.next_action` 标注的 message 轮，current-178 冻结目录 + 6 个合成意图用例，AI 辅助逐条标注复核见套件 provenance）；产品链路全部为录制 oracle，Jev 为唯一真实外部调用；`jev-1.13.0`（请求/响应模型一致 63/63）；`-jev-max-calls 70 -jev-timeout 2s`；问题定义 SHA256 与全部 provenance 见 `plan.json`。
- **调用结果**：63/63 成功、0 失败、0 次歧义拒答；延迟 p50=279ms / p95=331ms；tokens 45,226 输入 / 3,024 输出（未提供确认费率，未折现）。
- **意图指标**：整体准确率 **33.3%**（21/63）；与最终 Screening `next_action` 的一致率 **33%**（21/63，两者口径独立）。混淆矩阵：collect 11/11 全对；plan 9/10；**confirm 42 轮中 38 轮被判为 collect（系统性坍塌）**。holdout 最优点：confidence t=0.94 时 precision 0.833 / coverage 0.194；selected-probability t=0.95 时 0.833 / 0.161。校准/保留划分 32/31，会话不相交。
- **结论**：**不满足** jev-shadow change 的 Follow-on gate（95% 精度 / 20% 覆盖 / 无切片坍塌）：confirm→collect 坍塌是主要失败模式，疑与问题定义中 confirm 判据对"本轮是否给出授权"的表述有关；不开启生产 Shadow change。Jev 可用性与延迟本身良好，但价值未在当前问题定义下成立。
- **边界**：标注为 AI 辅助复核（15 条已有冻结标注全部一致，63 轮仅支持方向性结论）；套件继承源冻结件 42 个本机 replay red 检查（与离线基线逐项一致，Jev 观察未改变任何产品检查结果）。
- **产物**：`artifacts/jev-shadow-20260922/`（plan.json / events.jsonl / report.json；无 Key、无 Authorization 落盘）
