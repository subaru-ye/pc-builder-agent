# current-178-20260917：快照 11 当前目录基线

2026-09-17 规划评估基线从快照 9（123 件，`current-123-20260915-r2`）更新到快照 11：178 行目录 = 126 件 active_core（全部有价）+ 52 件退库（无价），parts release `4dc0da59-f63b-5adc-a3b0-cd30394b7bd9`，冻结点 tag `release-candidate-20260917-r2`（HEAD 5f12d54），备份 `var/backups/rc-20260917-r2/`。旧 123 基线原样保留，显式传入其 suite 仍可运行。

## 基线构成

| 套件 | 模式 | 内容 |
| --- | --- | --- |
| `mechanisms` | 人工 oracle（10 case） | c1–c4 沿用 r2（升级/降预算、退库换新、退库内存、静音 unknown）；c5 改造为 ITX SFX 电源规则；c6 改造为办公核显；c7 重建为散热容量 unknown；另含 3 个行为 case（build_cases） |
| `screening-recorded` | 录制回放 | 20 题，原回答与断言不变，仅换 catalog；replay 20/20 |
| `thermal-recorded` | 录制回放 | 1 题 Builder 失败录制，仅换 catalog；**replay 已知失败（L1-006 保留 0/1）**，与 r2 录制在当前代码下的既有结论一致（见[收尾记录](业务收尾有限验收-20260915.md)）：录制断言与 2026-09-16/17 的数据补齐（主板 memory_speed_max_mts 已全量）及产品修复（已有件 issue 聚合、内部码剥离）已脱节；按"不重写历史录制"政策原样保留，当前行为以规则单测为准 |
| `cpu-recorded` | 录制回放 | LIVE123-CPU 首题录制，仅换 catalog；replay 1/1 |

`index.json`/`coverage.json`/`budget.json` 由 `scripts/eval/planning/index_current_178.py` 生成；无 GPU 下限 3060.3 元（最便宜核显 CPU = 4600G 657.8 元）。

## 新增能力点

- **ITX SFX 电源**（c5）：nr200p（supported_psu_form_factors=[sfx,sfx_l]、限长 130mm）+ ATX 电源 → FORM_FACTOR_SUPPORT 错误级 fail（issue 含"电源形态"）；换 `psu-cm-v850sfx-gold-white`（sfx/100mm）后 ready、validation=pass。规则实现 `internal/rules/clearance.go` formFactorSupportRule。
- **办公核显**（c6）：4000 元办公、`cpu-r5-4600g`（has_igpu=true）+ gpu=null → DISPLAY_OUTPUT 不再拦截，validation=pass；降预算到 3000 元后如实给出超支问题。真实模型冒烟会话 c2323e57（docs/eval/real-trial-20260917.md）已验证该场景可通过。

## c7 重建说明

r2 的 c7 前提是"主板内存频率上限未知"；2026-09-16 数据更新已补齐全部主板 memory_speed_max_mts（快照 11 无一缺失），该前提失效。c7 重建为等价的 unknown 保留机制场景：`cooler-noctua-nh-u12s`（cooling_capacity_w unknown）→ COOLER_THERMAL_CAPACITY unknown 如实保留（issue 含"散热"）→ 换 `cooler-deepcool-ag400`（220W）后 ready、validation=pass。这与散热 capacity 暂缓缺口（见下）一致，验证 unknown 不被默认值替代。

## 暂缓缺口（如实保留，不放宽规则）

见 docs/data/数据获取与发布规则.md §5.4 与 docs/eval/real-trial-20260917.md：

1. RX 6600/7600：三轮买手复核无干净 A 级行，暂缓。
2. 5600G/5700G：官方页未公布 Supporting Chipsets，暂缓、不猜测补位。
3. 散热 cooling_capacity_w：10 款复核厂商均未公布，保持 unknown。

上述三条已写入套件 provenance limitations 与 coverage limits；AMD 低预算场景不断言选中缺失 SKU，散热场景只断言 unknown 如实保留。

## 冻结与复现

- 冻结：`python scripts/eval/planning/freeze_current_178.py`（从 `var/backups/rc-20260917-r2/pcbuilder-rc2.dump` 恢复到一次性 `pgvector/pgvector:pg16` 容器 `peval-freeze-178`，只读导出，断言 latest_id=11、126 件全有价、release=4dc0da59）。
- 构建：`python scripts/eval/planning/build_current_178.py && python scripts/eval/planning/index_current_178.py`（OUT 已存在即拒绝，防覆盖）。
- 离线回归：`python scripts/eval/planning/run.py`（默认 suite 已指向本基线 mechanisms；`--go-tests ./internal/planningeval` 与 replay 互斥，需分开跑）。

### 2026-09-17 回归结果（离线，零模型调用、零联网）

- mechanisms：10/10 通过（`artifacts/planning-replay-178-mech-r2`）。
- screening-recorded：20/20；cpu-recorded：1/1。
- thermal-recorded：0/1，既有已知失败（见上表）。
- `--go-tests ./internal/planningeval`：ok；`go test ./internal/rules ./internal/planningeval`：ok。

## 真实模型冒烟（live-smoke-178-20260917）

用户授权的单批冒烟：2 场景（`SM178-OFFICE-IGPU`、`SM178-ITX-SFX`），共享 16 次调用硬上限，每场景一次、不重跑追分，网页与搜索离线。套件由 `scripts/eval/planning/build_live_smoke_178.py` 从新基线 mechanisms 派生（剥离 oracle，仅保留业务交付断言与用户点名项）。

结果（`artifacts/live-smoke-178-r1`，model pin `baseline-20260915.json`）：

| 场景 | 结果 | 观察 |
| --- | --- | --- |
| SM178-OFFICE-IGPU | 通过（delivered、无 issue、validation=pass） | 真实模型自主选中 `cpu-r5-4600g`、gpu=null、总预算内——办公核显能力点真实验证成立 |
| SM178-ITX-SFX | 未交付（0/1） | screening 把用户点名新购的"酷冷至尊V SFX Gold 850W 白色版"误分类为已有配件（issue：已有配件尚未对应到候选中的准确型号），未映射目录 SKU，方案停在待解决 |

消耗：actual_model_requests 14/16，tokens 248,670（约 12 分钟量级），external_requests 0。

**登记的模型能力问题（不改提示词、不追分）**：用户以"指定/要求 X"措辞点名的新购配件被 screening 归入已有/owned 语义，未进入购买选型；与 real-trial-20260917 场景 5（已有件）的语义边界问题同类。ITX SFX 规则本身的机制正确性已由 mechanisms c5 离线覆盖；真实模型下该规则路径尚未被端到端触发。

## 真实模型全量 mechanisms（r1 批次，2026-09-17）

用户授权的全量批次：10 场景共享 120 次调用硬上限，每场景一次、不重跑追分，网页与语义检索离线。套件由 `scripts/eval/planning/prepare_current_live_178.py` 从 mechanisms 派生（剥离 oracle）。

结果（`artifacts/live-178-mech-r1`，suite sha256 `cdd12232…`，执行套件完整副本保存在该目录；model pin 同冒烟）：

- **0/10 通过**；actual_model_requests **96/120**（screening 20 + builder 76），tokens 1,982,798，约 14 分钟，external_requests 0；工具调用 110 次（evaluate 24、search_local_batch 17、search_local 6、read_evidence 5、search_web 4、register_candidate 2）。剩余 24 次额度因场景失败早停未耗尽。
- 步骤分类：behavior_failure 27、delivered 3、pending_with_evidence 2、read_or_retry 5、technical_fault 4。

**失败分解（327 项断言，245 项通过；82 项失败）**：

| 类别 | 数量 | 说明 |
| --- | --- | --- |
| 套件设计错误（评估方责任） | 17 | r1 派生时给所有 message/confirm 步强加 `require_tools=["evaluate"]`；首条消息只跑 screening（无工具调用），该断言不可能通过。authored 套件首步期望本就是 `versions:0, builder_calls:0`。生成器已修正为仅保留 authored 工具要求 |
| confirm 时会话仍处于 collecting（technical_fault 4 + 连锁失败约 12） | ~16 | 模型未在 authored 步数内完成需求收集，confirm 动作被后端拒绝（"会话阶段不允许当前操作"）；主要发生在 B2 行为场景与 c7 |
| 真实模型行为失败 | ~49 | 见下 |

登记的真实模型行为问题（一次尝试，不代表成功率；不改提示词、不追分）：

1. **超预算交付**（c1、c2）：预算 7500/7000，模型分别交付 7682/7673 元的 proposal 并附"需用户确认取舍"的未解决 issue，而非自行调整到预算内（如降内存/换 SKU）。
2. **已有配件无法匹配目录**（c3、与冒烟 ITX-SFX 同源）：G.Skill 32GB 已有内存在目录中无精确 SKU 时，模型以 16GB 占位交付且超预算，owned_parts 语义处理失败。
3. **需求收集节奏**（B2-001/002/003、c7）：多轮 free-text 字段（workload/offline/avoid_brand/owned_cpu/owned_board）未被 screening 提取为 active；会话长时间停在 collecting，final_outcome 为空。screening 模型（deepseek-v4-flash-0731）对自由文本字段的提取能力是主要短板。
4. **冲突处理**（c4）：noise_pref 被记为 conflict 而非 uncertain。
5. **正向信号**：c6（办公核显）除被强加的工具断言外全部通过——4600G 核显路径在真实模型下端到端成立，与冒烟结论一致；c5 交付了合规 ITX 构建但规划进度断言未过。

**批次状态**：r1 已消耗，按"单批不重跑"政策不再自动补批。r1 的 17 项结构性假失败源于派生脚本缺陷，修正后套件版本为 `current-178-live-mechanisms-20260917-r2`（`internal/planningeval/testdata/current-178-live-20260917/`，已通过零调用校验），如需以修正套件重新执行需用户另行授权与调用预算。

### r1 失败项的提示词修复（2026-09-17，已授权）

依据 r1 与冒烟的失败现场登记并修复（提示词改动均通过离线回归与 `go test ./...`；真实模型效果待下次授权批次验证）：

1. **owned/指定购买语义**（`screening_requirement_state.go` 增量协议 + `screening_new_build_prompt.go` 草稿协议）：明确"指定/要用/必须配某型号"是购买要求，必须存独立 free.* 约束条目（草稿协议写 notes），绝不写入 existing_parts/owned_parts。现场依据：冒烟 SM178-ITX-SFX 模型把"电源指定酷冷至尊V SFX Gold 850W 白色版"写成 owned_parts（live 输出留档于 artifacts/live-smoke-178-r1）。
2. **负向品牌约束**（增量协议 remove 语义处）：补充示例"不要英伟达显卡"应存 `free.no_nvidia_gpu` kind=constraint，不写 brand_pref.gpu=amd（B2-001 现场违反）。另登记：B2 系列部分 free.* 命名差异（如 live 用 `free.local_whisper` 覆盖 workload/offline 语义）属 authored 期望对自由编号过度锁定，非模型能力缺失，不改套件、在此留档。
3. **value/strength 混淆**（增量协议 strength 说明处）：明确 value 与 strength 是不同字段，枚举字段 value 只能取枚举值。现场依据：c4 第 3 步模型把 "prefer" 当 noise_pref 的 value（应为 silent 枚举），非法值触发程序侧 conflict。
4. **超预算交付**（`internal/planning/runner.go` builder 指令）：总价超出预算硬上限时先自行调整未锁定配件压回预算内再交付，不把可自行决定的降配挂起等用户确认；只有牺牲硬性要求或已有件才留待解决。现场依据：c1/c2 分别交付 7682/7673 元超上限方案并附"需用户确认取舍"。

已有配件无法精确匹配目录 SKU 时的处理（c3）属 builder 检索/评估能力问题，本轮未改，维持登记。

### 修复效果验证（2026-09-17，用户授权第二批复跑）

冒烟复跑（`artifacts/live-smoke-178-r2`，16/16 次）：0/2，但**修复 1 在 screening 层验证成立**——"电源指定酷冷至尊V SFX Gold 850W 白色版"被输出为 `free.psu_model` 约束条目（r1 为 owned_parts），全链路无 owned 误分类。两个失败均在后续步：SM178-ITX-SFX confirm 步撞上 503 上游服务不可用（供应商故障）；SM178-OFFICE-IGPU 的 builder 耗尽 24 轮额度后以散热 capacity unknown 未解决项收尾（r1 该场景通过，属运行间波动）。

全量 r2（`artifacts/live-178-mech-r2`，套件 current-178-live-mechanisms-20260917-r2）：**1/10**（r1 0/10），112/120 次（screening 20 + builder 92），tokens 2,657,372，约 25 分钟，external 0。断言 320 项通过 250（r1 327 通过 245；r2 断言面已剔除结构性假失败）。与 r1 对比：

| 修复项 | 验证结果 |
| --- | --- |
| require_tools 结构性假失败 | 消除（r1 的 17 项不复现） |
| owned/指定购买语义 | 成立：全批无一处把点名新购写成 owned_parts；C123-002 整案例通过（r1 交付 7673 超上限） |
| 超预算交付 | 部分成立：C123-002 通过；但 c1 仍超 6500 上限交付、c6 以 3110.30>3000 交付并声称"目录无更低可换"（未核实为真） |
| value/strength 混淆 | 部分成立：c4 不再产生 conflict，但模型把"静音改成尽量"记成 value=normal（应为 silent+prefer），新错误形态 |

r2 新登记的模型问题（一次尝试，不追分）：

1. **builder 轮次额度耗尽**（c1、冒烟 OFFICE）：24 轮上限内未完成修正，以"本轮处理额度已用完"的未解决 proposal 收尾。
2. **目录事实误报**（c5）：声称"目录无 AM4 ITX 主板/无 AM5 核显 CPU/无 SFX 电源"，而 b650i-edge 与 v850sfx 均在目录中——检索覆盖或断言准确性问题；同时把 AM4 CPU 与 AM5 主板送进 evaluate。
3. **confirm 时会话仍 collecting**（B2×3，与 r1 同）：需求收集节奏问题未修复。
4. **B2 free.* 命名差异**（与 r1 同）：authored 期望锁定自由编号，已在上文留档为评估侧过度约束，不计为模型回归。

批次状态：r2 已消耗（剩余 8 次不足以重跑）；后续修复优先级建议：builder 轮次内完成修正与目录检索覆盖（c5）、noise_pref 枚举值语义、预算内自纠的稳定性。

### c5 目录误报根因与检索修复（2026-09-18，零模型调用）

对 r2 报告 C123-005 全部 search 调用做离线复核，修正此前一处误判：step2 批量子查询并非返回空数组（先前解析把 `planning_action` 响应口径看漏），每个子查询都返回了 8 个候选——但全部是该品类价格最低的 8 件，与查询词无关。根因确认：

1. **price_asc 排序下查询词失效**：`search_local` 排序为"价格优先、评分仅同价并列"（刻意设计，防平台过滤，见 legacy-retrieval-recheck 测试）。模型用 `order_by=price_asc` + 规格词（"SFX ITX"、"SFX SFX-L 小1U"）检索，返回的永远是品类最便宜的 8 件；v850sfx 价格 999 元被 `truncated=8` 截在页外。模型随后断言"目录无 SFX/SFX-L 电源"，而该商品在目录中。主板查询"ITX AM4"同理只见 mATX 板。step3 改用默认 relevance 按完整型号查询时 v850sfx 即排第 1，证明 relevance 路径正常。
2. **修复**（`internal/planning/runner.go`，不改价格排序语义）：价格排序响应新增 `query_matched_count`（本品类 query 命中总数）与 `keyword_hits`（命中候选，最多 8 个，按价格序）；relevance 路径不变。工具说明同步增加：命中者未进当前页不代表目录没有；`query_matched_count=0` 时换通用规格词重查，仍为 0 才可断言该品类缺少该规格。

新增回归测试 `TestKeywordHitsExposeMatchesTruncatedByPriceOrder`（c5 形态：9 件 ATX + 1 件 SFX，price_asc 页保持不变且 keyword_hits 暴露 SFX 命中；零命中不附 key）。`go test ./internal/... ./cmd/...` 全过，离线 replay 零模型调用不受影响。真实模型验证需下批授权。

### builder 预算自纠辅助与轮次收敛（2026-09-18，零模型调用）

针对 r2 新登记问题 1（24 轮额度耗尽）与"超预算交付"部分成立，两项改动（`internal/planning/runner.go`）：

1. **evaluate 超预算反馈附 `budget_alternatives`**：报价超预算硬上限时，反馈自动附各品类最便宜的有报价候选（每类最多 5 件、按价格排序、排除已选项、不做兼容核验，附说明文字）。模型无需额外检索即可判断能否自行替换压回预算；r2 中 c6 曾在超预算 110.30 元时直接交付并声称无更低可换，该数据此前需要多次 search_local 往返才能核实。触发条件：`budget_cny` 为 active must 且报价（PurchaseTotalCNY，缺省用 TotalCNY）超过上限。
2. **首次 evaluate 时机**：工具说明由"尽早检查"改为"首次 evaluate 应在工具额度过半前完成，之后优先用反馈修正"。现场依据：r1 C123-001 出现 `[search_local evaluate search_local×4 evaluate]` 循环，额度在反复评估中耗尽。

新增回归测试 `TestBudgetAlternativesAttachCheapestOnOverspend`（超预算附最便宜候选、已选项排除、缺价品类省略、预算内/无预算不附）。`go test ./internal/... ./cmd/...` 全过。真实模型验证需下批授权。

### noise_pref 示例与交付质量评分口径（2026-09-18）

1. **noise_pref 转换示例**（`internal/agents/pipeline/screening_requirement_state.go`）：strength 条目追加示例"静音改成尽量安静"→ `set noise_pref value=silent strength=prefer`。现场依据：r2 c4 模型记成 value=normal（应 silent+prefer），错误换了形态但仍是枚举/强度混淆。
2. **交付质量评分口径**（`scripts/eval/planning/delivery_quality.py`，零模型调用）：从既有 report.json 重算硬性门（事实正确、预算内、兼容错误 0、无缺价），失败 check 按名分类，机制/评估侧 check（require_tools、state:*、version_count 等）不计入。对 r1/r2 报告重算基线：r1 硬过 3/10、r2 硬过 4/10（机制口径为 0/10、1/10）。可用标准 27/30 需三批各 10 case 达标；关键能力点（办公核显、ITX+SFX、已有件替换）按 case 逐批核对。

### B2 confirm 被拒根因与修复（2026-09-18，零模型调用）

r1/r2 三例 B2 confirm 步均报"会话阶段不允许当前操作:当前为 collecting"。离线复核根因分两类：

1. **B2-001（模型行为缺口）**：用户原话"做本地Whisper转写，音频不上传云端，不要英伟达显卡，预算还没定"是直接装机请求，screening 模型因预算未定输出 `next_action=collect`，会话停留在 collecting，后续 confirm 被拒（`StartConfirmRun` 要求 phase=requirement_ready；`completeRequirementState` 中只有 next_action=collect 会留在 collecting，planningProjection 的 missing 恒为空，故确认门槛纯粹由模型 next_action 决定）。修复：screening 提示词 confirm 条款明示"预算未定不是 collect 的理由"，附"做一台本地转写用的机器，预算还没定"→confirm 示例。
2. **B2-002/003（套件作者错误）**：用户原话"先看看升级方向"/"先比较处理器……暂不选配"明确推迟配置，screening 停在 collect 是正确行为，authored 的 step2 confirm 在真实产品流程中不可能成功。修复：`prepare_current_live_178.py` 对这两 case 剔除 step2 confirm，后续 message/refresh 步仍覆盖 plan 与续跑。

另核实 c5 主板断言真伪：目录中 ITX 主板仅 mb-msi-b650i-edge（AM5），"目录无 AM4 ITX 主板"为正确断言；c5 的目录误报仅限电源侧（v850sfx 为 SFX 规格在目录中，已由 keyword_hits 修复覆盖）。

`go test ./internal/... ./cmd/...` 全过。全部离线可修缺口至此登记完毕；下一批真实模型验证（r3，需新套件版本与授权）是验证上述修复的唯一途径。

### r3 套件登记与 c5 闭环真值核实（2026-09-18）

1. **r3 套件已注册**（`internal/planningeval/testdata/current-178-live-20260918/`，版本 current-178-live-mechanisms-20260918-r3，120 次上限）：含 B2-002/003 confirm 剔除，plan-live 零调用校验通过（`go run ./cmd/evalplanning -mode plan-live ... -max-calls 120`）。执行需用户授权。
2. **c5 闭环真值**：离线核实目录——AM5 核显 CPU 共 11 款（最低 r5-7600 1492.7 元）、SFX/SFX-L 电源 2 款（v850sfx 1199、loki-850p 1649）、ITX 主板仅 b650i-edge（AM5）。即 **ITX+办公核显方案在目录内可以闭环**（b650i-edge + r5-7600 + v850sfx + nr200p），c5 模型"无法闭环"的结论是检索漏检所致的误报（"无 AM5 核显 CPU""无 SFX 电源"两条均为假断言），keyword_hits 修复正对此形态；修复后 c5 预期可从 clarify 翻转为完整交付。

### preserved_other_parts 条件性不可满足：根因与修复（2026-09-18，零模型调用）

r2 C123-001 step3（"直接把CPU升级为Ryzen 7 5700X，其他配件尽量不动"）硬门失败 `preserved_other_parts`。根因链：

1. **事实**：模型把 CPU 5500→5700X（+510 元）后总价 7891 超出 7500 硬上限，为压回预算将内存 32GB DDR4-3600（1759 元）砍成 16GB DDR4-3200（569 元，-1190 元）——牺牲剪辑用途容量而非最小替换。目录核实：最便宜的 32GB DDR4 就是已选款，**不存在更便宜的 32GB 同容量替换**。
2. **预期不可满足**：v1 已花 7381/7500（余量 119 元），CPU 差价 510 元；`preserve_other_parts` 对非 CPU 逐件 DeepEqual，而预算 `budget_ceiling` 是硬门。任何非 CPU 改动都违反前者，不改必超后者——**两条硬门在该 v1 组合下矛盾**。唯一通过路径是模型在 confirm 时恰好留下 ≥510 元余量，属于运气而非行为。
3. **修复（三处）**：
   - **评分侧**（`internal/planningeval/grade.go` + `types.go`）：新增 `preserve_essential_parts` 预期，把"尽量不动"按实质分解为可判定硬不变量——GPU 槽位不变 + 内存容量不降级（容量从冻结 SKU ID 形态 `mem-*-<GB>-<MTS>` 启发式解析，目录 specs 无容量字段，已验证覆盖全部 14 款内存 ID）。同品类更便宜替换（电源/机箱/散热器）仍允许。`TestPreserveEssentialPartsAllowsCheapSwapsButNotCapacityCuts` 覆盖四形态（换便宜电源过、容量升过、容量砍败、GPU 变更败）。
   - **套件侧**（`prepare_current_live_178.py`）：r3 生成器对 C123-001 把 `preserve_other_parts` 换成 `preserve_essential_parts`，provenance 说明依据；离线 replay 套件不动（旧套件仍用逐件相等口径，recording 不重评）。
   - **提示词侧**（`internal/planning/runner.go`）：evaluate 文档与 `budget_alternatives` 反馈注记追加最小替换策略——"替换优先同品类且容量或性能档位不降，不得为压预算单方面削减与用途相关的容量或档位（如内存容量、显卡档次），无法两全时交付 proposal 留待用户取舍，不要擅自砍容量后直接交付"。
   - `delivery_quality.py` 硬门分类同步加入 `preserved_essential_parts`（事实正确门）。

验证：`go test ./internal/... ./cmd/...` 全过；冻结 mechanisms 套件离线 replay 10/10（一次性 pgvector 容器，用后即删）；r3 重新生成后 plan-live 零调用校验通过。C123-001 step3 至此从"条件性不可通过"变为可判定：合法的最小替换交付可过，擅自砍容量仍判败。r3 真实模型验证需用户授权。

### r3 执行顺序重排：队尾饿死防线（2026-09-18，零模型调用）

r1/r2 逐例调用统计揭示结构性饿死：C123-001 单例消耗 35 次（两批相同），排在队尾的 C123-007（r1）与 B2 全部三例（r1、r2）拿到 0 次调用即被跳过——**B2 修复（confirm 时机、owned 语义）在两批中从未被现场验证**，其 r1/r2 失败记录不能作为模型行为证据。另核对 r3 套件中 4 处 `model_input_contains` SKU 级断言（cpu-r5-5600、gpu-sapphire-6600-pulse 等）在 r2 现场全部 pass，内容来自程序拼装的计划输入而非模型选择，不构成结构性假失败。

修复：`prepare_current_live_178.py` 把执行顺序改为 B2-001/002/003 → C123-007 → C123-001…006。B2 的 screening+confirm 流每例只需数次调用，排在队首用约 10-20 次即可完成三例验证，剩余额度留给重型 C123 场景；若 C123-001 修复后的 evaluate 时机与 budget_alternatives 生效，其调用量应显著低于 35。r3 套件已重新生成（版本不变，provenance 记录重排依据），plan-live 零调用校验通过。额度风险仍在：若单例行为异常仍可能饿死队尾，授权时可考虑上调 120 上限（如 150）作缓冲。

### C123-003/C123-004 硬失败根因补全与提示词修复（2026-09-18，零模型调用）

r2 硬失败逐项核对发现两项此前未登记根因，均可离线修复：

1. **C123-003 step1（已有件替换关键点）**：预期 outcome=clarify（离线录制行为：先 search_local 型号词检索，确认缺失后澄清"可补充来源或选择新购内存"），实际模型 search_calls=0——**未检索就断言"目录无此 SKU"**，并静默把 memory 槽位留空交付 proposal（final_outcome、meaningful_planning_progress 失败）。这正是"无结果断言需检索证据"原则未覆盖 SKU 精确匹配场景的漏洞。修复（builder 提示词 owned_candidates 条目）："已有件在目录无匹配时，先用 search_local 以型号词检索，以 query_matched_count=0 为证才能断言缺失；确认缺失后 clarify 告知用户可补充来源或改购新件，不得不检索就断言缺失，也不得静默把该槽位留空交付 proposal。" step2 的超预算 6561.50/6000 已由 budget_alternatives 覆盖（模型当时声称"已选最经济款"但未核实）。
2. **C123-004 step1**：预期 validation=pass，实际 overall=review——模型选了缺 cooling_capacity_w 字段的 hyper212-black 散热器，COOLER_THERMAL_CAPACITY 只能 unknown（validation 失败是本例唯一硬门失败）。目录内 AG400 等散热器字段完整（r2 C123-001 同字段校验通过），模型本应换候选重试。修复（builder 提示词注册条目）："本地候选缺兼容字段导致校验 unknown 时，优先换字段完整的同类候选重试，目录内确实都缺才如实说明并交付。"

`go test ./internal/planning/ ./internal/planningeval/` 全过；r3 套件 provenance 同步更新并重新生成，plan-live 零调用校验通过。至此 r2 全部硬失败（C123-001/003/004/005/006、B2×3）均有登记根因与对应修复；剩余验证只能由 r3 真实模型批次完成（需授权）。

### builder 提示词通读：预算压力政策矛盾调和（2026-09-18，零模型调用）

多轮追加后通读 instruction，发现 line 24 与新加的超预算最小替换政策直接矛盾：通用超预算句"先自行调整未锁定配件的**容量、档位**或层级把总价压回预算内再交付"——这正是 r2 C123-001 把内存 32GB 砍成 16GB 的提示词诱因（该例超预算源于执行升级请求，不是用户主动降预算）。调和为两条清晰分工：

1. **用户主动降预算**（已授权场景）：新预算是用户明确表达的新要求，模型可自主检索更便宜件并做必要的平台联动、说明取舍（保留原自主性，避免退回"把降配回问用户"的 r2 前失败模式）。
2. **执行请求导致的超预算**：优先换同品类更便宜候选，不得削减 base_draft 已交付方案的用途相关容量/档位；必须牺牲时交付 proposal 留待用户取舍。

修改仅涉 line 24 一句（"说明容量或性能取舍"→"说明必要的取舍"；通用超预算句删除"容量、档位"授权并加"已交付方案"保护）。`go test ./internal/planning/` 全过。此矛盾若不修，r3 中旧句会持续引导模型砍容量，使 preserve_essential_parts 新检查与 budget_alternatives 提示词互相抵消。

## 真实模型 r3 批次（2026-09-18，授权上限 200）

用户授权的第三批全量：上限提高至 200 次。套件 `current-178-live-mechanisms-20260918-r3`（快照 11），相对 r2：执行顺序重排（B2×3+C123-007 先行）、C123-001 step3 改 `preserve_essential_parts`（配套新评分项）、提示词含 budget_alternatives/evaluate 时机/owned 缺失取证/缺字段换选重试等修复。执行于 `artifacts/live-178-mech-r3`（plan created 2026-09-18T05:35:54Z）。

**结果（delivery_quality.py 硬过口径）：3/10**（C123-004、C123-006、C123-007；r1 3/10、r2 2/10）。调用 **104/200**（screening 20 + builder 84）。执行顺序重排生效：C123-001 仅 16 次（r1/r2 均 ~35），10 个 case 全部执行、队尾零饿死；B2×3+C123-007 先行仅耗 ~24 次。防重复计费护栏（输出目录必须不存在）在并行实例冲突中正确拦截了一次重复启动。

**生成器缺陷（本轮发现并修复）**：`prepare_current_live_178.py` 的 per-id 步骤块（B2 confirm 剔除、C123-005/007 步骤剥离）重绑局部 `steps` 后未回写 `case["steps"]`，这些修订从未实际落进套件——r1/r2 及本批执行的套件均含被阻挡的 B2 step2 confirm 与未剥离的 C123-005/007 步骤。修复后已重新生成 testdata（B2-002/003 剔除生效、C123-007 剥至 2 步、C123-005 剥至 3 步，generator_sha256 已更新）。对 r3 结论的影响：B2-002/003 的失败由已知 confirm 阻挡问题复现（step2 再次被拒"会话阶段不允许当前操作"），**不作为模型证据**；C123-005/007 以未剥离 oracle 执行，C123-007 的 version_count 失败属机制断言不计入硬过（硬过有效），C123-005 的 final_outcome 失败属真实业务行为（见下）。

三个关键能力点：办公核显 **成立**（C123-007 硬过）；ITX+SFX **未成立**（C123-005）；已有件替换 **未成立**（C123-003）。

r3 真实行为问题（一次尝试，不追分；均已在 r3 前修复过的提示词下发生）：

1. **C123-002 预算自纠未完成闭环**：恢复 32GB 后 7464>7000，模型正确守住内存容量未砍，但把"16GB/预算内 vs 32GB/超预算 464 元"直接抛给用户（clarify），未尝试下调未锁定 CPU/主板/电源等把总价压回 7000。"不得砍已交付容量/档位"被遵守，"先自行调整未锁定配件"未执行——政策的前半句生效、后半句未生效。
2. **C123-003 已有件与缺字段候选**：(a) 用户已有 G.Skill 32GB 目录无精确匹配，模型检索取证后确认缺失（取证要求生效），但交付时以 mem-crucial-ballistix-16-3200 占位并说明"实际应用户自有内存"——确认缺失后应 clarify 而非以其他 SKU 占位交付 proposal；(b) hyper212-black 缺 cooling_capacity_w，模型保留该候选交付 unknown proposal，未按"优先换字段完整的同类候选重试"（目录 AG400 有该字段，且 step 已有 18 次调用余量）；(c) 6432>6000 交付 proposal 待取舍而非自行调整。
3. **C123-001 升级证据与降预算收敛**：step3 未用 search_local_batch 直接 evaluate（require_tools 失败）；changed_cpu 评估附证据不足（"performance benefit needs separate evidence review"）；step4 预算降至 6500 后未产出新版本（version_count 停在 2），以"散热能力字段缺失"的 proposal 收尾。evaluate 时机修复本身成立（调用 35→16）。
4. **C123-005 平台联动缺失**：模型断言"目录缺少 AM4 ITX 主板"经核实为真（16 块主板中仅 b650i-edge 为 ITX 且为 AM5），但以 clarify 待用户在 mATX 与 AM5 间选择，未主动提出满足 ITX must 的 AM5 平台方案（b650i-edge 1450 元在 9000 预算内）。authored 期望 step2 即 proposal——ITX 板型 must 不可满足时，平台切换属可自主检索并说明的联动，不是必须追问的用户取舍。
5. **B2-001（首次现场执行）**：用户明说"预算还没定"，builder 仍在 step2/3 追问"整机预算上限是多少"（clarify 而非 collect），违反"用户已回答的问题不重复问"；未执行期望的 search_local。free.* 命名差异（模型用 free.workload_audio_local/free.no_nvidia_gpu，期望锁定 free.workload/offline/avoid_brand）维持 r1/r2 留档：评估侧过度锁定自由编号，不计回归。screening 提取本身正常（3 条 free.* 全部 active 且 strength 正确）。

正向信号：C123-004 noise_pref 场景硬过（value/strength 修复在真实模型下成立）；C123-006 办公核显连续三批硬过；owned 指定购买语义全批零误分类（r1 修复持续成立）；C123-007 在未剥离套件下仍硬过。

批次状态：r3 已消耗（104/200），按单批政策不自动补批。testdata 已按修复后的生成器重新生成并入库；重新执行需用户另行授权与上限。

## 真实模型 r4 批次（2026-09-18，授权上限 200）

v4 批次：修正后的套件（B2 confirm 剔除与 C123-005/007 剥离首次实际生效）+ r3 后针对 5 项缺口的提示词修复（不追问已表态未定的信息、硬性要求不可满足时主动提平台联动 proposal、预算自纠先下调未锁定件价位且不抛取舍、已有件缺失禁以其他 SKU 占位、缺字段候选存在完整同类时不交付 unknown、升级收益结论必须引用检索证据）。执行于 `artifacts/live-178-mech-r4`。

**结果（硬过口径）：2/10**（C123-006、C123-007；r1 3、r2 2、r3 3、r4 2）。调用 **82/200**（screening 18 + builder 64），工具 88 次，全部 10 case 执行、零饿死。

逐项（一次尝试）：

1. **B2-002/003 首次干净现场**：confirm 剔除后不再出现"会话阶段不允许当前操作"；新根因显形——用户 confirm 后 screening 仍停在 collecting（每例仅 2 次调用、builder 从未触发），final_outcome 为空。authored 流程期望 confirm 触发 builder 并以 collect 收口+执行 search_local。"confirm 时机"修复首次有了干净证据：问题从"confirm 被后端拒绝"转移为"screening 不在 confirm 后升级到规划"。B2-001 仍以 clarify 追问预算（7 次调用），不追问修复未生效。
2. **C123-001 明显收敛**：硬失败仅剩 changed_cpu 证据不足（10 次调用，r3 为 3 项硬失败）；仍未用 search_local_batch，但交付了新版本。性能收益引用检索证据的修复未生效（仍写"需另行核验"）。
3. **C123-002 透明但未自纠**：守住 32GB 未砍（正确），交付 7434>7000 的 proposal 并列出 4 个用户选项，声称"保持 32GB+独显前提下目录无更低价组合"——该断言未经验证（仅 8 次调用，price_asc 覆盖面存疑）。预算自纠修复未改变行为。
4. **C123-003 透明化但行为未变**：不再静默占位，明确写"用户已有内存无准确型号匹配、需服务端支持已有件占位"后仍以目录 SKU 站槽交付；hyper212 缺 cooling_capacity_w 仍保留并附工程推理（"四热管塔式配 65W TDP 无风险"）而非换字段完整候选（AG400）；6561>6000 仍抛给用户。18 次调用为全批最高。
5. **C123-005 平台联动修复未生效**：模型断言"无 AM4 ITX"仍为真，但仍以 clarify 要用户在 mATX 与 AM5 间取舍（8 次调用），未按新提示词直接交付 AM5 proposal。version_count 0。
6. **C123-004 批次内回归**：r3 硬过后本批 step2 出现 validation 失败（兼容错误）且 noise_pref 专项 issue 缺失——单次尝试的运行间波动，与冒烟 OFFICE 场景 r1/r2 交替通过的模式一致。
7. 正向：C123-007 办公核显连续两批硬过、C123-006 三批硬过；owned 指定购买语义四批零误分类；防重复计费与调用量控制持续正常。

**批次对比结论**：提示词修复对"透明度"有效（占位、取舍说明更诚实），对"交付决断"无效——预算自纠、平台联动、缺字段换选三类动作模型仍反复退回 clarify。四批硬过 3/2/3/2，单次尝试方差大但短板稳定：短板不是检索或格式，而是"把可自行决定的调整当成用户取舍"。下一杠杆建议转向程序侧防线（服务端在存在可下调候选时拒绝以超预算/平台取舍为议题的 clarify、自动以字段完整候选替换校验 unknown 项、已有件槽位允许无价占位由服务端核账），提示词继续辅助但不作为主要手段。

批次状态：r4 已消耗（82/200），不自动补批；重新执行需另行授权。

离线回归全部零模型调用、零联网；provenance 记录 source_commit、套件与生成器 sha256。真实模型已执行四批授权：冒烟 r1（1/2）、全量 r1（0/10，含 17 项评估方结构性假失败）、修复后复跑冒烟 r2（0/2，screening 层验证 owned 修复成立）、全量 r2（1/10）、全量 r3（硬过 3/10，104/200）与全量 r4（硬过 2/10，82/200，修正套件首次生效）；再次执行需另行授权与 `--max-calls` 上限后登记。
