# 当前状态与路线图

> 更新：2026-09-15。本次工作按用户指定的[业务收尾与有限验收](../eval/planning-v2/业务收尾有限验收-20260915.md)交付，历史分数不再作为本目标门槛。独立真人评审和长期维护项目仍单列，不伪称已完成。

## 当前能力

- Web 工作台已具备需求确认、配置生成、增量改单、版本对比、分享和导出；ADK dev UI 用于调试。
- 新会话具备[动态需求面板](../tech/动态需求状态栏.md)：当前需求、未知项、增量编辑、撤销、临时例外、来源与确认快照由服务端统一维护。三视口和数据库链路已验收，固定模型小规模冒烟累计4次；跨会话长期画像尚未实现，旧会话不自动回填推断偏好。
- 默认[自主规划](../tech/自主规划流程.md)：模型自主检索本地与外部资料，工具反馈事实；待解决方案独立持久化，不覆盖正式版本。原 Harness v2 仅作历史诊断。
- 本地缺规格可用已读取正文补充到会话候选快照，保留本地报价、逐字段来源和旧版本；续聊恢复并重新校验，当前目录已知事实优先。
- 本地可选账号、匿名数据认领、Redis 会话与缓存已实现；公网账号能力仍有限制。
- 数据发布已具备字段证据、风险隔离、不可变 release、last-known-good 和本机任务；人工价格 CSV 与过期提示可用。
- 历史评估集 v1.5 共 50 题：原 40 题保留，新增 10 段需求确认前的多轮对话。旧执行器固定模型基线150/150、Pass^3=50/50不等于当前planning成绩。新流程两批定向真实冒烟及差距见[目标验收进度](../eval/planning-v2/目标验收进度-20260915.md)，适用历史场景的迁移和同口径重复评估尚未完成。
- 修复环、语义检索及两个角色的模型对照已完成，同一二进制的四组结果均可重放；最终修复代码回归 150/150、Pass^3=50/50。保留原生产模型和机制，具体发现与用量见[运行记录](../eval/运行记录.md)。

## 当前待办

| 工作 | 状态与下一步 | 验证条件 |
|---|---|---|
| 多 Agent 运行时修复 F1-F9 | 已完成:取消生命周期(F1)、产物归档与控制面传输(F2)、运行可观测性(F3)、增量协议格式重试(F4)、落库侧零 LLM 复验(F5)、取消级联轮询(F6 实测:a2a-go 剥离请求 ctx)、token 硬预算(F7)、文档收敛(F8);离线与 PG 集成测试全绿,golangci-lint 清零,真实模型冒烟通过(builder=deepseek-v4-flash-0731:10 次模型请求、284k tokens、proposal/未解决诚实收口,产物 live-smoke.json);完成证据见[审计与修复方案](../tech/多Agent运行时审计与修复方案.md) §6 回填 | 真实链路上的取消级联与预算触发行为、同口径评估批次对比仍待验证;代码回归不能替代 |
| 生产评估运行设施 | 本机已增加[变更回归](../tech/变更回归.md)：显式固定条件实跑、调用上限、同口径新旧评分和逐题差异；零模型 CI 工作流已提供 | 可信远端评估库/密钥、定时真实跑测、线上分布和费用监控仍待补齐；工作流文件不等于远端运行已验证 |
| 解释质量与软偏好 | 固定八题异构 Judge 已完成诊断及协议验证，仍有无效引用与语义误判；空理由、静音证据不足单列 | 独立人工校准未完成，评分不接门禁；颜色以具体 SKU 证据为准，不能用检索文本或高硬分证明偏好满足 |
| 评估可信度 | v1.4 已迁移预算题契约，旧 L1-004 和历史失败保留；通过报告区分交付与合理非交付 | 分数变化须拆分验收契约、程序修复与模型波动；S3/S4 仍为中文近似规则，误判先查判卷系统 |
| 初筛追问质量 | 当前planning真实回归能保留最新预算和2K分辨率，但补充后仍重复询问游戏名、静音等可选项；提示调整尚未通过复测 | 依据保存的真实请求与回答继续修复；必要补充后应进入确认或规划，不通过关键词或固定字段门槛绕过模型 |
| 目录预算可达性 | 预算下界只作目录范围内的参考证据，默认产品不再据此提前拒绝，允许模型寻找替代路径；不自动改预算 | 扩展已有件/改单证明前分别设计保守边界与反例；当前结论只限完整目录及快照，不代表全市场无解 |
| 数据接入与价格复核 | 当前175件、123条最新报价、快照9；ITX补证已发布，剩余100个逐项暂缓 | 优先核实影响整机搭配的准确型号、规格与报价；FIT V缺官网正文和购买链接继续暂缓，不以全量入库为成功条件 |
| 真人评审 | 最近记录仍为 0/3，缺少独立参与者 | 按[真人评审](../eval/真人评审.md)完成三类样本；开发者不得代评 |
| 关键缺陷与发布验收 | 本轮截断草稿、次数文案、白色偏好说明和重复口径追问已修复并完整回归；真人验收仍未完成 | 继续处理人工评审的实际发现；P0/P1 缺陷关闭，延期 P2 有规避和证据；代码回归不能代替真人门禁 |
| 自动价格来源 | SerpApi/Baidu 实测未达门槛，来源和每日任务禁用；来源工作暂停 | 找到可用来源后重做 canary、精确匹配、零误匹配和真实周期验证，不能降低标准 |
| 评估反馈闭环 | 工作台负反馈、运行证据冻结、本机待复核候选导出已实现；正式题库仍由人工审核 | 通过开发者自己的真实使用积累问题、复核归因，再形成少量有依据的新题；合成多轮和对照实验不冒充用户反馈 |

## 数据维护目标

以下是后续目标，不是当前数据覆盖声明。数据规则以[数据获取与发布规则](../data/数据获取与发布规则.md)为准。

- 扩展八类目录至每类至少 50 个，分批 10–20 个引入；`active_core` 的错误级字段证据完整率 100%，不能为数量指标放松门槛。
- 核心池价格覆盖率 100%，平均年龄不超过 7 天、最大不超过 14 天；自动价格来源激活前保留已有 last-known-good 与过期提示。
- 自动价格来源恢复时保留 12 SKU canary、零误匹配、双商家等门禁；96/64 目录调整和每日自动采集均不得提前启用。完成两个真实 14 天轮转周期后再评估稳定性。
- 性能分先核验许可与再分发范围，再做版本化导入；CPU/GPU 覆盖率目标 80%，缺失显示 unknown。
- 按文本、provider、model、dimensions 指纹生成增量 embedding；模型变更需处理已有向量的一致性。
- 完成回滚恢复与人工维护流程验证；稳态维护目标平均每周不超过 30 分钟，数据发布主链零模型调用。本机定时任务已于 2026-09-21 移除，周期采集需要持久 runner 时再另行立项。

## 后续范围

多平台、跨会话偏好画像和社区反馈按实际需求排期。公网部署、邮件验证、找回密码等先补运行前置条件；当前不承诺实时价格、库存、24 小时采集或生产 SLA。京东联盟路线不在实施计划内。

## 维护方式

每项只记录状态、下一步和完成证据；完成后从待办移除。质量基线放入 `docs/eval/`，运行步骤放入 `docs/ops/`，设计决策放入 `docs/tech/`。历史发布标签不再作为路线图分组，但未完成的真人门禁不能被文档整理视为已通过。
