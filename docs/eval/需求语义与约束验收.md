# 需求语义与约束调整验收

2026-09-10，基于 `7dd1270` 的独立 worktree。此次验证只使用离线模型替身、保存的真实输出和独立临时数据库，没有调用真实模型，没有改动用户会话或现有服务。

## 故障证据与修复范围

用户输入“预算 6000，主要剪 4K 视频，尽量安静”，后续只把预算从 must 改为 prefer。已核对的失败运行 `0d8aab2d-10a3-4b3a-a852-722e7de38d31`、`e97c2c5b-7daa-4057-9aad-dffd346119ff` 的生成输入包含 `notes=剪4K视频`、`use_case.titles=[剪4K视频]`、软静音和软预算。失败直接来自任意非空 must notes 的证据门禁；不是预算或静音导致的拒绝。

此前初筛失败的真实 `deepseek-v4-flash-0731` 输出继续原样保存在 `internal/agents/pipeline/testdata/requirement_video_editing.json`。本次另将已核对的生成输入关键字段保存在 `internal/buildharness/testdata/video_editing_failure_requirement.json`，注明它是诊断字段转录，不冒充完整数据库导出。未修改历史运行或既有 fixture 的真实价格。

改动包括：由现有 Screening 结合当前状态理解语义，移除当前会话路径的关键词复判；区分事实、说明、配置条件与强度；逐项保留可靠字段和未结构化原文；保护撤销、备选、来源、确认快照和版本；Builder 对真正硬条件继续过滤或明确待核验；具体业务原因跨 A2A 传递并保存到聊天展示。

## 离线证据

| 检查 | 结果与覆盖 |
|---|---|
| `go test ./...` | 全部通过；显式 Live 门禁未开启 |
| `go vet ./...`、`go build ./...` | 通过 |
| schemas / Screening 定向回归 | 开放口语、部分不确定、推断与默认不采用、类型和来源错误、撤销不复活、备选不生效、朋友装机局限于会话、零更新不改变已确认投影 |
| Harness 定向回归 | 录制的 must 用途备注进入生成；真正自由文本硬条件保留；硬品牌/板型筛选、筛空业务原因、硬显卡不能被省略或在预算修复中删除 |
| `TestRequirementStatePersistentWorkflow` | 真 PostgreSQL、真实 Screening 适配器、需求编辑幂等和 revision、已知信息不重问、临时覆盖恢复、确认与历史不可变 |
| `TestVideoRequirementConfirmationReplay` | 真实初筛输出→预算强度编辑→确认→真实候选/Harness/规则/报价→保存 v1→刷新→硬静音失败及具体原因→用户明确修正→保存子版本 v2；v1 需求保持不变 |
| A2A 业务结果单测 | data part 转换及产品接收；受控原因显示，模型原始校验文本不进入错误摘要 |

数据库测试经 `PG_TEST_DSN` 连接本地 PostgreSQL 管理入口，创建唯一命名的 `requirement_test_*` 临时数据库并运行迁移，业务表、候选及版本均在临时库内。它们不会读写用户业务库的会话。测试结束清理临时库。

全链回放使用真实产品 service、HTTP、PostgreSQL、候选准备、Harness、规则引擎、报价及 presenter；仅模型输出是替身。Screening 的视频输出来自保存的真实响应；其他多轮输出是明确标注的人工 oracle。视频配置使用独立的合成低价显卡候选，在测试库中标注 `synthetic-offline-fixture`，预算内选件用于验证完整生成路径，不代表真实模型已能稳定选出同一配置，也不代表当前商品报价。

## 浏览器与复核

使用 Next.js `3102` 和离线 API `18084`，浏览器经真实 HTTP API 与临时 PostgreSQL 交互，不使用 route mocks。不使用或停止用户项目的 `3100`，不替换装机项目的 `3000`、`8082`、`8081` 服务。

最终 Playwright 三档 **9/9 通过**（约 1.2 分钟）。测试场景包含既有多轮编辑和版本保护，以及本次视频原句、预算强度编辑、生成、刷新、硬条件具体失败、继续聊天、原文来源/撤销和备注分类。手机、平板、桌面均无横向溢出，axe 未检出 serious/critical 问题。

Web TypeScript、ESLint 通过。Vitest 全量 **16 文件、54/54 通过**；随后仅调整旧事实字段的强度标签展示和重复标签，新增一项断言后，需求面板定向 **9/9 通过**。这次微调不改变状态或布局，未重复全量浏览器。Go 全量检查后最后的边界修复另经受影响包定向测试、全仓 vet/build 及独立数据库回放复核。

截图存于 `artifacts/requirement-semantics/browser-final/`，另保存 `desktop-generated.png`、`desktop-failure.png`、`mobile-failure.png`、`desktop-context.png` 便于查看。已人工检查桌面生成结果及手机具体失败说明。截图中的服务/事件存储提示来自离线替身与内存事件存储，不代表实际模型服务的健康结果。

验证后通过测试二进制专用 `POST /__offline/shutdown` 退出并清理临时库；最初不带退出接口的旧测试二进制也按本轮精确进程和唯一临时库名清理。`3102`、`18084` 已无监听，Next 自动生成的 tsconfig 改动已还原。用户主服务、原会话和历史数据未变。

## 调用量与保留边界

本任务实际调用：Screening **0**、Builder **0**、Embedding **0**、真实 tokens **0**。没有运行全量真实模型评估，没有改模型、模型链或 `.env`。测试中的模型调用计数只统计进程内替身，不能算作真实调用，也不能作为新提示词的模型准确率证据。

不新增每轮总结模型，不建设跨会话个人画像。无法统一核验的真实自由文本硬条件仍需明确证据，不用兼容性通过冒充满足。旧会话的已确认需求和配置版本不回填、不重写；带状态的新会话使用增量语义协议，旧 host/旧评估入口保留原协议。此次完成离线回归与浏览器验证，尚未切换用户正在运行的服务，也未进行新提示词的真实模型冒烟。

## 主项目接入记录

2026-09-10 15:27（Asia/Shanghai），在用户授权整理后，将独立工作区提交为 `902b7d8`，快进合并到主项目 `main`。复核并重跑 schemas、pipeline、buildharness、product、producthttp 测试，真实 PostgreSQL 的视频故障及多轮状态回放通过；前端类型检查、需求面板 9 项定向测试通过。没有重跑真实模型评估。

已重新编译并切换 8081 Builder 与 8082 API，3000 Next 开发服务加载主目录新版代码，健康检查全部正常。接入检查另发现新建空会话把字段保存为 `{}`，导致面板没有未知字段的补充入口；创建时改用 `schemas.NewRequirementState()` 完整初始化未知值，并用独立数据库回归验证不会把朋友需求带入新会话。没有回填或修改已有会话。

在正常 3000 页面实际新建会话，通过面板保存预算和用途事实分类，再刷新核对服务端值与来源；没有确认生成，也没有调用模型。测试会话清理后，切换前后的全部 `web_sessions`、`builds` 行数及内容摘要完全一致。接入证据、截图与服务日志位于 `artifacts/requirement-integration/`。本轮新增 Screening、Builder、Embedding 真实调用均为 0；新的提示词真实模型表现仍待单独受限冒烟验证。
