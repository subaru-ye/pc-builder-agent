# 缺价商品退出当前目录

按用户明确要求，将最新价格快照 ID 9 中无报价的 52 件商品从当前目录移除，不继续补采。当前有效目录为 **123 件／123 条快照报价**；数据库另外保留 52 件 `retired` 历史记录，供历史报价外键、规格证据和发布追溯使用，不再进入本地结构化检索、语义检索或新规划目录。

这不是把旧报价更新为新价，也不是补齐了全市场覆盖。退库仅表示本项目不再将其作为当前商品，不宣称该型号停产。已保存的配置版本仍显示原商品和原报价；后续改单重新使用当前目录，已移除的原件可能需要有价替代或保存为待解决方案。

## 发布

- 基线商品 release：`4a3da72c-e001-57dd-ad26-9838c90ade72`，175 件。
- 本次商品 release：`e04a9a3c-d9bd-59a7-86d3-50ec11e21e9c`，123 件 active_core、52 件 retired。
- 价格快照仍为 ID 9（2026-09-15），123 条；没有创建新价格快照或改变观察日期。
- 准确 SKU 清单见 `decisions.json`。通过已有 `create_review`、`publish_reviewed_release` 和 Go 导入器发布，`var/data/current.json` 已切换，避免仅改数据库后被初始化恢复。
- 独立数据库演练和正式发布证据：`artifacts/catalog-retire-unpriced-20260915/`；包含前后事实哈希、审核包、rehearsal.json 和 publication.json。原商品规格、来源、向量、字段证据、全部报价快照、需求、正式版本和待解决方案均核对不变。
- `publish.py --publish` 再执行返回 Already published，不重复发布。本脚本只处理此次固定基线，不是自动删除缺价商品的定时任务。

本批已执行命令：

```bash
python scripts/data/reviewed-intake/2026-09-15-retire-unpriced/publish.py
python scripts/data/reviewed-intake/2026-09-15-retire-unpriced/publish.py --publish
```

首次命令需要未改动的原基线及新的输出目录，会用同一 PostgreSQL 容器内的独立临时数据库演练，结束删除该测试库。正式发布只消费已演练的本地审核包。恢复需通过既有商品发布流程显式恢复状态，不能只回填旧价格。

## 工作台与验证

- 配置右侧去除总览及每个配件重复的价格时效告警；保留版本快照日期、逐件观察日期、参考价说明、缺价数量及未知库存说明。价格年龄计算和 API、公开分享不改写。
- 真实浏览器连接常驻 Next.js 和产品 API，打开已有 v3（7118.90 元）、切换 v1（8168.90 元）、刷新后仍为历史只读，原 2026-07-28 日期保留，右侧无“已过期／价格可能已变化／距今若干天”告警。未发送生成请求，未 mock 接口。
- 前端 18 文件、62 项现有测试通过；TypeScript 和改动组件 ESLint 通过。本次为数据发布与展示调整，没有重新运行历史模型评估；旧冻结评测资料保持原样。
- 发布演练、正式发布及幂等检查通过。浏览器已确认旧配置能继续读取；未新测退库后的真实模型选配成功率。
- 本轮模型请求、模型 Token、买手、外部搜索和网页读取均为 0；外部账单金额未查询。
- 恢复 Docker 及既有 PostgreSQL、Redis、Auth 容器，启动常驻 API／Builder／Next.js。`/readyz` 全依赖 ok；Next.js 已加载本轮展示代码，Builder 每轮读取已发布目录，无须重编译后端。
