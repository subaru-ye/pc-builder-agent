# 数据接入与真实评估：验收进度

目标尚未完成。本文件记录当前证据和剩余验收项，不替代原始报告，也不将离线回放计为真实模型成绩。

## 已完成：代码基线整理

| 提交 | 内容 |
| --- | --- |
| `acd8148` | 四批已审型号、人工报价发布、同日不可变价格批次及历史读取兼容 |
| `6a80319` | 技术资料HTTP优先读取、可选浏览器读取、正文窗口与调用记录；网页价格不用于新报价 |
| `9d80b41` | 产品链路离线评估、旧新目录对比、多轮回归及Screening提示冲突修正 |

以上均为本地提交，未推送远端。Go源码包测试通过；数据pytest 166项、接入脚本9项、评估脚本7项、前端60项测试及类型检查通过。证据为 `artifacts/goal-baseline-{go-tests,web-tests,replay}-20260915.log`。未将未提供数据库环境而跳过的集成测试算作本次新增验收。

全源码检查发现并修复历史判卷兼容：新Quote含snapshot_id，旧保存记录没有该字段。旧记录仍按自身完整冻结目录重算；显式记录的错误批次编号仍判失败。未改写历史原始用例、原始结果或判分答案。旧CLI的replay会重新生成派生report.md，本次已执行一次；后续重放先复制到新的验证目录，避免再修改历史派生报告。

## 已完成：历史基线证据核对

`baseline-20260915.json`冻结历史meta/cases/results原始文件哈希及当前模型配置。来源为 `artifacts/eval/20260909-134447-3770035618/`，150次执行、50题、每题3次，保存结果全部通过；零模型重放与首跑一致。

- Builder：qwen3.8-max-0902；Screening：deepseek-v4-flash-0731；Embedding：qwen3.7-text-embedding。当前配置与历史脱敏配置一致，模型链为空，SDK自动重试为0。
- 历史224次逻辑模型调用、15次Embedding；489092输入token、25077输出token、总514169。历史未记录Embedding token与内部HTTP尝试，不能冒充完整账单费用。
- 50个旧场景均保留；23题需迁移规划断言、14题需迁移状态断言、13题需重写已失效预期。旧提前拒绝、固定必填追问、预算下界和程序固定改单限制不作为新版通过条件。
- 历史总分不是新planning成绩；先用相同的新评分标准重判可兼容旧证据，不能把旧100%直接当成新版总分。没有新执行证据的项目单列不可比。

## 已完成：剩余候选逐项处置；继续按缺口补证

初始处置基线174件/122条报价；原115个新增候选中14个已发布。提交 `740e557` 冻结剩余101个的逐项发布处置与证据缺口：28个报价绑定被拒绝，73个需要准确变体复核；31个还缺原始购买链接。101个在该基线均暂缓发布，型号本身并非全部被否定。清单为 `scripts/data/reviewed-intake/2026-09-15-disposition/decisions.jsonl`，每项保留原行、具体缺口、原因及下一步，不代表已逐页完成官网核实。后续ITX补证发布使当前目录为175件/123报价、剩余100个，见文末；初始处置记录不改写。

后续发布仍按真实整机失败的报价与规格缺口排序；不强制把101件全部入库，不改写原始报价、来源或历史配置。

## 真实模型与浏览器验收记录

提交 `8b45383` 已增加显式 `plan-live/live` 入口、固定模型配置核对、两角色共享请求上限、调用前计划及逐请求/逐步同步落盘。供应商模型链为空、SDK自动重试为0；使用独立数据库与完整冻结目录，网页资料回放，外部服务不被隐式调用。该入口的离线回归12/12通过，记录在 `artifacts/planning-live-preflight-offline-20260915-r1/`。

第一批真实冒烟已完成：**12/12次模型请求（Screening 4、Builder 8）、Embedding 0次、外部搜索/读取0次**。2个场景、7步，1/2通过；186806 tokens，89987毫秒。固定174件目录及122条报价。原始计划、回答、token和判分保存在 `artifacts/planning-live-smoke-20260915-r1/`，没有重跑或覆盖失败记录。调用上限已耗尽；下一批必须另登记范围及上限。

生成场景中，模型先评估12705元候选，再主动调整到6513元，报价完整且兼容性通过，最终空问题列表的proposal仍被服务端错误要求提供“预算口径、预算弹性”的硬件资料而阻断。这两项已由真实报价、采购口径与明确授权的弹性一起核算；修复只移除它们不适用的硬件引用要求，不取消预算核验或其他必须条件。

修复验证：保存的原始最终回答在8000元预算下可交付；4000元且弹性0仍未交付；只有明确授权的弹性才参与计算。完整4次Screening、8次Builder原始回答和原判分断言另存离线回放，2/2通过，正式v1及刷新恢复通过，实际模型和外部请求均0。证据为 `artifacts/planning-live-recording-fixed-20260915-r1/`，版本化夹具为 `internal/planningeval/testdata/live-smoke-recorded-20260915/`。

以上不能报告为真实复测2/2。首批同时暴露尚未验收的问题：Screening仍重复询问可选游戏/静音/外观信息；Builder的“2K中高画质60fps+”与内存通道描述没有相应实测/型号证据，兼容性通过不能证明这些性能承诺。方案质量与不重复追问需要独立评分，首批状态断言通过不覆盖这些问题。

新版完整评估不能沿用离线oracle的精确SKU、固定调用次数或自由条目编号判分。应检查实际需求、工具证据、兼容性、报价、父版本、合理未交付、执行行为及不重复追问；模型可选择其他满足条件的零件。随后才运行适用的历史50场景及3次重复，并分开报告新问题回归。

第二批计划（运行前登记）：`internal/planningeval/testdata/live-followup-20260915/`，Screening/Builder合计最多24次真实请求，外部搜索/读取0次、Embedding 0次，模型及完整174件目录与首批相同。FU-001新增明确的confirm行为断言；FU-002复核正式交付，再要求直接升级CPU，检查不同CPU、其他配件保持、版本2、刷新和幂等重试。该批保留独立分母，不替换首批。CPU不同只证明执行，是否更好还需结合型号与用途审阅；其他配件保持是本次兼容升级的可行性目标，必要联动不能静默算作通过。

本批前的提示修正：Screening不再将可选偏好未知当成持续collect理由；Builder需区分性能选型判断与实测保证，不凭容量频率宣称双通道。没有新增模型调用阶段，也没有增加程序关键词追问或准入路径。

第二批结果（`artifacts/planning-live-followup-20260915-r1/`）：16/24次真实请求，Screening 5、Builder 11，405366 tokens，171198毫秒，原评分0/2。本批已经结束，剩余8次不用于未登记的重跑。FU-001补充分辨率后仍collect且重复可选追问；新提示确实进入了实际请求，因此不能将其归因于旧服务未更新。FU-002先选超预算8714元候选，后续升级到9415元，均未交付。升级原话确实触发plan、search_local和evaluate，但由于前置v1未成功，不算“已确认版本升级”的完整验收。模型还把用户未要求的具体帧率实测写进issues，提示修正未通过真实验证。

第二批判卷审查另发现三处区分：原“保留其他配件”比较误把新draft的build_ref变化当配件变化，已修复并用真实原始draft加内存/数量反例验证；原owned_parts=unknown预期不接受用户明确“没有已有配件”对应的active空数组；priority必须为prefer也不足以体现“明确要升级CPU”与“其他件尽量不动”的区别。后两项在原报告中不回改，后续题库须采用事实/授权语义；即使修正这些误判，本批重复追问和超预算造成的失败仍然成立。没有放宽预算、规格或正式版本断言。

两批累计28次真实模型请求、592172 tokens，外部查询与Embedding均0；实际金额未知，不能把token数量直接当账单。与历史150次执行的总token不可直接比较，因为场景、执行链路和重复数不同。

开销定位及离线修复：CPU检索返回大量字段来源，每项重复URL、标题和时间，导致后续每次输入持续膨胀。仅将正文截至240字的尝试没有降低这批输入（其原始摘录本来很短），未据此报告收益。最终search_local改为按候选编号返回字段来源索引，完整链接、时间和原文仍由read_evidence读取；同时修复长段落命中词落在短窗口外的问题。保存原始回答回放2/2通过，证据与目录一致的8次Builder输入总字符由403132降到260519，减少142613（约35.4%）。记录在 `artifacts/planning-source-index-replay-20260915-r1/`；这是字符量比较，不是实际token、延迟或模型效果复测。

浏览器本批使用独立3105/18087与测试数据库、保存的真实回答和明确的离线oracle，0真实模型：新建并发送需求→面板改预算且保留软静音→确认自动v1→面板设must静音生成待解决→刷新恢复→切回v1→聊天放宽静音→再次确认自动v2（父版本v1）已观察通过，来源显示本轮未联网、1个链接下10条依据。期间浏览器输入法把一次消息重复粘贴，触发精确回放夹具不支持的输入；校正原文后继续成功，不算真实语义模型失败。离线网关/Redis探针显示不可用/降级，未伪装为真实依赖健康。

最后的历史版本切换后刷新，被测试服务器默认10分钟超时打断；本批不计完整浏览器验收通过。`artifacts/planning-browser-20260915-r1/tests.log`与manifest保留超时失败。独立容器已清理，临时Next进程已停止，Next自动修改的tsconfig已恢复。运行器新增显式 `--test-timeout 30m` 可供后续离线交互验证使用，普通Go测试默认仍10m；模型每轮/每批限额未改变。正常项目服务未重启，最新修复尚未部署到常驻服务。

首两批结束时尚未完成：重复追问和自主预算修正的可靠真实复测、解释事实性审阅、适用历史50场景的迁移及重复运行、完整浏览器末段验收。后续进展见下文；所有显式验收完成前保持目标进行中，不宣称达到历史模型效果。

## 后续修复与历史 Screening 迁移

带有非空问题列表的 proposal 也获得一次交付反馈复核，前提是仍有工具阶段和工具额度；可自主追加检索和修复，也可保留真实问题正常结束。不会强制用满 8 轮，不更改 must、价格和兼容性事实。离线测试覆盖超预算 proposal → 收到实际核验 → 自主检索 → 修正交付，以及缺噪声证据重复 proposal 正常结束和小额度不追加复核。原完整且无问题方案仍一次交付。新版协议夹具 `proposal-review-20260915` 显式增加两条人工复核回复，旧夹具不覆盖；离线产品链路 12/12，原状态、版本、证据断言保持，0真实请求。

Screening 将动作决策放在需求提取之前，并要求 confirm 回复不追加可选问题、plan 直接交接、collect 用于讨论或明确影响决策的歧义。删去相互重复的决策段落，保留单次模型调用与原有来源/reducer校验。提示修改的真实效果尚待以下独立批次验证。

历史 v1.5 的 19 个 Screening 场景已全部迁移，32 条用户消息逐字不变；每题增加刷新恢复。`internal/planningeval/testdata/legacy-screening-v1.5-20260915` 保存原题哈希、迁移后评分及无 oracle 的 live 版本。离线19/19、790项检查通过（`artifacts/planning-legacy-screening-offline-20260915-r3/`）。旧版默认 any 改为未表达时 unknown，固定必填追问改为状态及必要交接断言，撤销显式保留 removed；这不是沿用旧规则重判后的100%。可选追问和语义解释需另做人工审阅，固定字段评分不等于完整语义评审。其余31个历史选配场景仍未完成迁移。

下一真实批次在执行前登记：仅运行上述19题的一次重复，最多32次模型请求（Screening与Builder共享硬上限），预期Screening32、Builder0，外部搜索/读取0、Embedding0。固定当前模型及174件/122报价冻结目录，沿用baseline-20260915模型pin；新目录 `artifacts/planning-legacy-screening-live-20260915-r1/` 独立存计划、请求、回答和评分，不重跑之前批次，不将剩余额度转作其他题。这是历史需求理解子集的真实复测，不是完整50题或三次重复验收。

该批已结束：32/32请求、93387 tokens、190708毫秒、外部0，原评分仅5/19。供应商请求本身无错误，6条消息因输出协议错误成为产品失败；另有缺失op造成字段未采用、evidence误用嵌套对象、错误observe操作和来源/字段问题。完整JSON操作示例在提示精简中被删去是本轮引入的风险点，已恢复明确op、evidence字符串和顶层observations范例；不靠补猜op或宽松接受错误类型修复。L5-302两轮均直接进入confirm且软静音保留，但L5-301预算修改因错误observe结构未成功，不能报告重复追问问题整体已通过。

判分另有独立适配缺口：owned_parts.quantity省略在业务契约中表示1，而原新增评分按JSON精确比较，将缺省1判错（L4-212、L4-214等）；本批原始成绩不回改，后续需按同一规范化口径补充重判，并保留错误型号和实际数量变化的反例。L5-304的预算口径推断、来源丢失等仍是真实问题，不能全部归结为评分。

小规模协议复测执行前登记：`screening-protocol-recheck-20260915`选取L3-103、L3-104、L5-301、L5-306、L5-308，5题9条消息；保留上述原题原评分（包括已知quantity限制），固定模型，最多9次请求，Builder、Embedding、外部均0。独立目录 `artifacts/planning-screening-protocol-live-20260915-r1/`，不重新使用已结束批次。用于验证恢复完整格式范例后的协议可靠性，不代表全题复测或已达到历史基线。

浏览器后续批次 `artifacts/planning-browser-20260915-r2/` 使用独立3105/18087和测试数据库，保存的真实候选/资料与离线模型oracle。完整观察：新需求→面板预算6000改12000（软静音保留）→确认自动v1→must静音待解决且v1不变→刷新恢复和来源归并（1链接10依据、搜索/读取0）→聊天恢复prefer→确认自动v2（父v1）→切到v1历史只读→刷新仍为`?version=1`→切回v2→新对话全未知且建议输入可见。1280×720下document尺寸等于viewport，没有整页额外滚动。历史选型文案复用原始记录，不能据此宣称旧性能断言已获实测证明。

此浏览器批次服务器正常关闭，但同包的TestPlanningProposalPersistentWorkflow仍断言旧3次调用而失败，故原manifest退出1保留。修改为新增复核后的4次调用，其余持久化、版本不变和工具断言不改；独立数据库重跑`artifacts/planning-proposal-review-http-20260915-r1/`通过。浏览器操作本身与该断言失败分开记录。临时Next进程/数据库已清理，自动改动的tsconfig已恢复；共享服务未重启。

协议复测结果：9/9真实请求、28137 tokens、34263毫秒、外部0，原评分4/5。所有9条消息均完成，预算修改/补分辨率（L5-301）、中文金额（L5-306）、常规游戏需求（L3-103/104）正确进入状态并confirm，未重复可选追问。L5-308的已有CPU准确从AMD Ryzen 5 7600改成Intel Core i5-12400F，来源和quantity=1均正确，但existing_parts没有同步为cpu，原评分仍失败；不是本批的数量默认值误判。保留该结果，不改数据库或期望让其通过。两批改善只覆盖这些明确场景，未证明所有语义与格式均可靠。

本目标目前累计4个已结束真实批次：12+16+32+9=69次模型请求，共713696 tokens，Embedding和外部查询均0；金额未知。新增预算proposal复核仍只有离线证据，真实Builder的预算内交付/升级效果尚待复测。默认离线入口已切到proposal-review新版夹具，避免新增复核让默认回放读到旧短轨迹；历史v2.0保持原样供审计。

下一验收重点：已有件类别/型号的状态一致性和评分数量默认值适配；迁移剩余31个历史选配场景；在固定模型、明确新批次额度下验证自主预算修正和正式CPU升级，并按统一口径复核可比较旧证据。完整浏览器基本链路已补齐，但全历史真实评估与性能建议事实性审阅仍未完成，目标继续进行。

## 历史选配场景迁移与下一批真实验证

31个历史Builder场景已冻结为 `internal/planningeval/testdata/legacy-builder-v1.5-20260915/`：12个首次生成、10个改单、9个异常/数据不足场景。原始文件逐题校验哈希，显式结构化需求经产品面板编辑及确认入口进入当前状态，不补旧默认值，不用新的自然语言改写冒充原题。该子集不计Screening理解成绩；原19个Screening场景单独保留。

改单先向独立评估数据库保存原base_selection，按冻结目录重算基线报价和兼容性，缺项如实保留；不把此预置版本计为本轮交付。预算变更前值只按原题明确delta逆推；新增尺寸/静音从基线需求中移除，不推测更早的历史偏好。之后走实际产品确认、Builder、自动交付、父版本关联及刷新。每步核对旧版本不变，实际Builder输入含正确base_draft。测试首次发现内部Go结构体直接Marshal不符合线上JSON，已改为保留原parts线上结构。首次失败记录 `artifacts/planning-historical-precondition-tests-20260915-r1/` 保留，修复后的独立数据库复测r2通过（没有真实模型调用）。

评分新增对实际选择的品牌/候选集合、锁定CPU/GPU、ITX主板与机箱、采购口径预算的检查，不采信模型rationale。GPU芯片品牌按冻结型号的Radeon/GeForce集合判定，不把Sapphire等板卡制造商误当芯片品牌。软静音、外观、帧率目标及用途表现仍需独立语义/事实审阅；自动评分通过不等同于已证明性能。L4-215暂用严格预算内交付作为自动试验，有证据的非交付需另行审阅，不能与旧budget_adaptive总分直接混比。

同时修复已有件数量语义：quantity省略/0按现有契约等于1，多轮来源guard不会因此要求重新提供未变的旧型号；新增型号、真实数量变化仍检查来源。评估字段比较仅规范化这个默认值，错误型号、额外字段、重复项和真实数量变化仍失败。原四批报告不改写；existing_parts未同步的显示一致性问题没有被这个修复掩盖，也未声称已解决。

下一批执行前登记：`legacy-builder-smoke-20260915`，L1-001（8000元2K游戏）、L1-007（8000元剪辑生产力）、L2-104（预算降至7500且锁定原GPU）各一次，合计最多24次Builder请求。Screening 0、Embedding 0、外部查询0；每个Builder仍最多8次往返。固定baseline-20260915的模型和174件/122报价目录；独立结果目录 `artifacts/planning-legacy-builder-smoke-20260915-r1/`。该批只验证迁移入口与预算/锁定件执行，不替代全部31题或三次重复成绩。达到上限停止，不把剩余额度转用于其他任务。

该批已结束，2/3通过；17/24次真实Builder请求，191397 tokens，117904毫秒，工具13次，外部和Embedding均0。L1-001、L1-007均交付正式v1并刷新恢复；L2-104没有新版本。失败是模型未检索锁定的RX 7700 XT，误称目录无报价，选用RTX 5060再要求用户确认。冻结目录实际上有该型号及3299元报价。旧v1保持不变，未误交付；该行为失败不归因于数据缺失。累计五批86次请求、905093 tokens；本批剩余7次关闭不用。

根因处理：初始模型输入只给每类两件的局部样本与base_draft编号，原件规格/价格未直接随基线上下文提供。新版增加base_candidates和unresolved_base_ids，按本次真实目录匹配原配置；缺价保留null，不把缺价误标为缺型号，不将原配置当用户已购。search_local的相关性同时匹配候选ID，使准确SKU可直接检索。这不是缩小候选范围或增加准入规则；模型仍可检索其他路径。离线测试覆盖原件不在初始样本、缺价、缺型号和准确SKU检索；不据此宣称已修复真实模型行为。

针对性真实复测执行前登记：只运行原L2-104一次，输入及评分保持原样，最多8次Builder请求，Screening/Embedding/外部0，固定相同模型和目录。使用新批次 `artifacts/planning-legacy-base-context-live-20260915-r1/`，不覆盖原3题结果，不复用原剩余额度。

该批8/8请求结束，0/1，79705 tokens，54052毫秒。实际Builder输入包含正确原件资料，GPU保留断言通过，不再误称RX 7700 XT无报价。仍未交付：模型修补SSD/机箱的真实缺价后保留3799元内存，总价11248.94元超过7500元，未检索更便宜的内存/平台方案，反而要求用户批准未锁定配件变更。这是自主降预算的行为失败；旧v1不变，预算校验没有被放宽。累计六批94次请求、984798 tokens，外部与Embedding均0。

已有可行解证据：前一批L1-001的真实7114元方案使用同一RX 7700 XT、同一2K黑神话用途。将这份原始选择和已记录规格/报价在L2-104实际状态下重算，7500预算与GPU锁定均满足，结果可交付；预算改为6500则仍不交付。夹具 `internal/planning/testdata/locked_budget_witness_20260915.json` 保留两份源报告哈希。这只证明该目录存在预算内解，不将失败模型重判为通过，也不把固定方案放进产品修复器。提示明确只有用户指定的锁定件必须保持，未锁定原件可自主联动检索并说明取舍。

下一针对性批次执行前登记：原L2-104同输入同评分一次，最多8次Builder请求，其他模型/外部0，同一固定模型与目录，结果存 `artifacts/planning-legacy-budget-autonomy-live-20260915-r1/`。用于验证上述自主改单说明，不沿用任何已关闭批次余量。

共同口径重判已落盘 `artifacts/planning-common-core-comparison-20260915-r1/comparison.json`，复现脚本 `scripts/eval/planning/compare_legacy_core.py`。只比较兼容性、完整报价、明确预算、品牌、锁定件等可读事实；不把新产品持久化、软静音、外观或性能解释冒充旧证据中已有的指标。这3题旧9次记录在明确预算口径下为4/9（5次超出原始预算，旧默认弹性曾允许通过）；新3题批次为2/3，单题复测仍0/1，各批独立列示。旧报告不修改。

开销已明显增加：同题L1-001历史均值约4775 tokens/3.2秒，新一次53245 tokens/31.3秒；L1-007历史6577 tokens/6.9秒，新一次56740 tokens/28.6秒。目录/报价、调用流程和重复数不同，只作描述性成本对比，不能归因于某一项改动。L1-001文案中的“12GB显存避免8GB瓶颈”“性价比最优”缺少相应同工况比较证据，性能解释尚不能作为通过项；硬件和预算自动评分通过不替代文案事实性审阅。

自主改单提示复测已结束：`planning-legacy-budget-autonomy-live-20260915-r1`，8/8请求，81482 tokens，47194毫秒，0/1。GPU保持，但仍保留无报价机箱及超预算候选，要求用户批准内存降级；正确的自主调整说明没有形成可靠执行。记录该失败，不继续用相同单题重复调用掩盖结果。七批累计102次真实模型请求、1066280 tokens，Embedding/外部0，实际金额未知。本阶段真实模型还没有达到目标，不因两个首次生成场景成功就宣布全部通过。

共同口径脚本已补全旧空报价的读取适配，空字符串、非数值、NaN等均不能算完整报价。`artifacts/planning-common-core-comparison-20260915-r2/comparison.json`保留全部24个可交付/预算自适应旧场景（72次）的重判，并分别列示3个新版批次。此交付子指标旧结果36/72：33次有报价但超出原题明确预算，另3次没有可交付候选；旧budget_adaptive允许合理不交付，所以后3次不能据此判定旧业务错误。其他7个旧预先拒绝场景不纳入这一交付指标，仍保留在31题迁移和后续行为评估中。不是把历史总分降为50%，也不等于新版只需达到50%便完成目标。

本轮本地提交：e17ca0d（已有件数量语义）、b2c3519（31题迁移与旧配置入口）、d998edc（原件当前事实与SKU检索）、75a7a5c（自主改单说明、可行解证据及共同指标重判）。全Go源码测试/静态检查、15项Python评估脚本检查、12题产品离线回放和独立数据库历史基线/父版本检查通过。没有更新共享产品服务或用户会话数据，没有新增网页/API采集。

尚未完成：其余28个历史Builder场景的真实执行、19题Screening全量修复后复测、按共同口径的重复实验、CPU实际升级与完整解释事实性验收。已有件类别/型号显示不一致仍需处理；自主降预算已确认存在模型执行失败，不能用数据不足或兼容性校验搪塞。下一阶段按全场景失败分布安排修复，不无限追加同题试跑；目标保持进行中。

## 扩展真实评估：执行前计划

按顺序运行两个新批次，运行期间保持业务代码不变，固定baseline-20260915中的两角色模型、重试/超时设置及174件/122报价目录，外部查询与Embedding上限均为0：

1. `artifacts/planning-legacy-screening-current-20260915-r1/`：原19个Screening场景完整复测一次，32条原始用户消息，模型请求硬上限32，预期全部为Screening。使用已经冻结的原live套件及评分；仅评分器已按契约处理已有件数量默认值。单独保留旧32次批次和本批，不回改旧成绩。
2. `artifacts/planning-legacy-builder-remaining-20260915-r1/`：剩余28个历史Builder场景各一次，排除已经单独验证的L1-001、L1-007、L2-104。使用`legacy-builder-remaining-20260915`冻结子集，Builder请求硬上限224（每题最多8），Screening预期0。输入、旧基线配置及评分与31题迁移一致，不提供模型oracle，也不新增人为答案。

总上限256次是两个独立批次上限之和，不表示必须用满或允许重试已关闭批次。正常评估失败继续记录后续场景，技术故障先停止诊断。此次补齐场景覆盖，不作为同一代码下50题三次重复的最终验收；首次3题与其后代码修正的批次仍分开统计。真实CPU升级、解释事实性及旧新同口径重复验证仍保留在目标中。

## 完整场景覆盖结果与已有件状态修复

上述两个批次均已结束，原报告与评分不改写：

| 批次 | 原定评分 | 实际模型请求 | Token | 耗时 | 真实外部请求 |
|---|---:|---:|---:|---:|---:|
| Screening current | 13/19 | 32/32，均 Screening | 101231 | 118861 ms | 0 |
| Builder remaining | 10/28 | 202/224，均 Builder | 2574182 | 1331017 ms | 0 |

Builder 工具168次，其中发起搜索工具操作1次，但冻结网页适配没有执行网络请求；不能把工具调用量混成真实联网量。剩余22次模型额度关闭。本目标九个已结束批次累计336次真实模型请求、3741693 tokens，Embedding和真实外部请求均0，账单金额未知。当前两个批次共享代码 `3a1b374`、174件/122报价快照和固定模型；前七批仍单独列示，不能合成同代码三次重复成绩。

当前 Screening 所有32条消息均完成，没有产品级输出格式失败。真实缺陷包括静音偏好误写must、明确新增预算口径遗漏、已有型号与品类不同步。L5-304的model:null和L5-309的resolution:null/uncertain被保护逻辑转成conflict，不能仅归结为评分把未知显示得过细；缺少信息与真实矛盾仍需区分。

Builder 本批10个首次生成场景5个交付、9个改单场景1个交付；9个异常/缺数据场景4个按预先声明的行为标准通过。失败轨迹已经保存于本批 `audit-progress.json` 和 `diagnostic-notes.md`：重复搜索不增加候选、逐品类串行查询耗满工具往返、超预算后过早要求用户决定、误称已存在的型号或NVMe不存在。另有真实数据缺口：唯一ITX主板没有最新报价，不能用旧价格填平。L4-210把未提供准确型号的已有CPU说成4500/5500，属于事实错误；L4-203在100元预算下直接澄清是否有已有件，原评分因没调用search/evaluate失败，这一特定工具顺序要求是否合理需独立语义复核，不能将全部失败都算成模型不会规划。

共同口径报告 `planning-common-core-comparison-20260915-r3/comparison.json` 保留历史72次36次满足共同交付指标；本批其中21个对应交付场景为6/21，另3题首次冒烟2/3及两次单题0/1独立列示。新结果尚未达到目标。该指标只覆盖可核对硬件、报价和明确预算等事实，不评价性能文案。已交付案例仍出现静音、游戏特性支持和“最适合”等缺少对应资料/工况的陈述，不能以硬件得分代替解释真实性验收。

已有件联动修复 `deeba43` 在独立工作区完成，待本批终止后才快进合入主工作区，未污染评估运行源哈希。统一 reducer 负责准确型号补入品类、品类取消同步移除型号；仅忘记型号保留品类，备选不改变当前状态，临时恢复通过服务端derived_from关联，避免恢复旧型号覆盖后来的直接更正。OpenAPI与前端生成类型同步，来源和历史同修订持久化，不新增模型调用或数据库迁移，也不批量更改历史会话。

验证：新增状态回归、Go全源码测试及vet通过；真实临时数据库 `ownership-state-fix-db-20260915-r2` 通过（r1新增测试误按JSONB字节顺序比较的失败保留）。浏览器 `ownership-state-browser-20260915-r1` 实际操作型号录入→类别同步→临时取消→刷新→恢复→永久取消→预算修改→刷新，均与服务端状态一致；仅验证本次面板链路，不新增真实模型或生成验收声明。隔离服务、数据库和临时tab已关闭，共享产品服务未重启。两个原失败场景L4-214/L5-308的3条真实Screening原始输出另保存为 `owned_live_recording_20260915.json`，经当前pipeline与reducer离线重放通过；这证明原输出现在能正确进入状态，不是新的真实模型通过率。

下一重点：改善有界往返内的检索覆盖与自主修复，补齐影响整机的报价/规格，处理剩余Screening语义问题，再登记针对性真实批次。当前没有追加新的真实调用额度；全目标保持进行中，不能以本批覆盖已跑完或已有件修复通过宣布完成。

## 批量本地检索与已有件上下文复测

针对上述真实轨迹新增 search_local_batch：最多8个独立本地查询在同一次模型往返执行，每个子查询仍单独计入24次工具上限，不足额度的查询返回 pending_queries。单次/批次均反馈首次返回数、重复返回数和品类覆盖数，不自动过滤旧候选或替模型指定平台。已有件使用与采购核验相同的准确型号匹配，当前 active 型号的规格、多个同名候选和缺价 null 进入初始 owned_candidates；简称和不匹配型号不猜测，用户状态不修改。没有新增模型阶段、Embedding调用或外部查询。

评分器识别与调用编号对应的实际批次子结果；仅提出批次、pending、失败和空缺结果不能证明检索成功。成功空数组可以证明本次查询没有候选；旧单次查询的历史null空结果保留兼容。禁止本地检索的题目也不能通过批次绕过。历史期望、价格、预算和版本要求未放宽。

离线验证：Go全源码测试与vet通过；初次针对性测试因评分器缺genai import编译失败，修正后通过，原日志保留。真实临时数据库 `planning-retrieval-db-20260915-r2` 验证历史基线不变、批量查询→校验→v1→直接CPU升级v2→刷新/幂等，两个用例通过；r1因新增聊天用例仍沿用Screening次数0的旧测试断言失败，现按两个离线聊天调用检查，真实请求仍0。完整现行离线集 `planning-retrieval-offline-20260915-r1` 12/12，Screening23、Builder40、工具26均为离线协议调用。批量8品类→evaluate→结果的单测使用保存配件资料与人工轨迹，3次模型协议往返、9次工具；不当作真实模型提效成绩。

下一真实批次执行前登记：`legacy-retrieval-recheck-20260915`，仅L1-006（串行检索耗尽往返）、L2-104（降预算保留GPU）、L4-206（准确已有CPU/内存按采购预算）各一次，原题输入及评分不变。合计最多24次Builder真实请求，每题最多8次；Screening、Embedding、真实外部搜索/读取均0，工具仍每运行24次。固定baseline-20260915模型及同一174件/122报价目录，SDK自动重试0。独立产物 `artifacts/planning-legacy-retrieval-live-20260915-r1/`，余量不转用于其他题；本批不等同于历史全题或三次重复验收。主要观察批量工具是否被实际使用、真实evaluate反馈能否进入后续修正、已有件准确事实是否被采用，以及最终交付/预算/锁定件是否满足。

该批在 `c3f4163` 完整结束，原评分 **0/3**，23/24次Builder请求、33次实际工具执行、427336 tokens、240144毫秒，真实外部与Embedding均0；剩余1次关闭。本目标十个已结束真实批次累计359次请求、4169029 tokens，账单金额未知。批量确被三题采用，L1-006在第3轮、L2-104在第2轮即evaluate，修复了原逐品类搜索耗满往返的问题，但未据此计为选配成功。

- L1-006：已找到完整有价配置，水冷缺 cooling_capacity_w 后继续查水冷/外部资料，没有比较有能力参数的风冷路径。冻结目录实际存在有价的AG400、AK620、Dark Rock Pro 5；这同时涉及所选件数据缺口和模型未探索替代，不能将整题失败归为目录无解。回复的“已遍历所有”“不影响实际安全性”和具体游戏性能断言仍缺相应证据，保留事实性缺陷。
- L2-104：保留锁定GPU、修复SSD和机箱缺价，但始终局限AM5/DDR5，最终10133元高于7500元，又要求用户批准未锁定内存等变更。原预算、锁定及版本评分不放宽，旧正式版本保持不变；批量工具没有解决这项自主规划失败。
- L4-206：准确已有CPU/内存实际进入模型且正确选用；12项兼容性通过，新购报价5925元在6000元内，只有已有内存缺价。服务端 finalize 在 deliveryIssues 已按采购口径通过后，又无条件检查整机 MissingCount，错误降为proposal且没有问题说明，导致重复4次无意义回复。这是明确业务缺陷，不是模型需要更多信息。

针对最后一项删除重复整机缺价裁决，统一由已有的按口径价格核验决定；移除一次计算后立即丢弃的 BudgetQuote 调用，保留 evaluate 的准确型号/数量核销。保存原始8次回答、真实输入和目录哈希于 `owned_purchase_delivery_recording_20260915.json`。新增回归先复现原缺陷（`planning-owned-purchase-red-20260915.log`），修复后原完整proposal在第4轮即eligible；full_build、5000元预算、未拥有或型号不符的内存、新购显卡缺价均仍为有具体原因的proposal。Go全源码测试与vet通过，没有新增真实请求。

完整原始回答离线回放 `planning-owned-purchase-replay-20260915-r1` 为 **1/3**：L4-206通过产品需求确认、实际工具/价格/规则、正式v1持久化及刷新，L1-006/L2-104原失败保持。总Builder协议调用由原23降至19，只说明此保存轨迹少了4次重复反馈，不是重新测得的真实耗时/token收益。原真实0/3报告不改写，也不把离线1/3混进真实成绩；历史版本和用户会话没有修改，共享服务未重启。本轮未新增浏览器验证，仍不能宣称目标整体达标。下一工作优先解决未锁定平台的自主预算探索、真实数据缺口及剩余Screening语义问题，再按新上限复测。

## 价格检索能力补齐

继续核对发现旧 store.Candidates 支持价格排序，但新规划器的 search_local 只按相关性返回，模型说“价格低”仍仅被当作文字。新增显式 order_by=relevance/price_asc/price_desc，单次与批次共用；不从预算自动派生条件，不过滤其他平台或缺价候选。price_range_cny 反馈整个查询品类的已知范围及缺价数量，避免把当前页误当作全库价格范围。零/缺价语义、币值精度、分页和用户状态保持；错误排序参数明确报错。

本次离线回归保留L2-104真实查询原词“DDR5 32GB 6000 CL30 价格低”，主动改为价格升序后实际返回冻结目录中569元DDR4候选，仍保留全部21个内存路径；这证明工具能提供跨平台价格信息，不证明模型已经自主选对替代。另覆盖精确小数排序、同品类范围与分页、缺价始终排尾、空范围为null、独立批次排序及原工具额度。Go全源码测试/vet通过，`planning-price-order-offline-20260915-r1` 完整离线12/12，模型协议调用数仍Screening23/Builder40、工具26。没有新增真实模型请求或重判之前成绩；真实自主降预算效果仍待新的有界批次验证。

## ITX 配套数据补证

价格检索修复已提交 `2d45e54`。随后针对真实评估暴露的ITX报价缺口，复核MSI MPG B650I EDGE WIFI：官网7项型号/规格证据与原买手搜索、详情的准确标题、卖家、链接及1450元报价对应。聚合请求/响应ID不同的情况显式存档，不静默等同；报价仍为2026-09-13观察值、库存未知，不冒充本轮新价。

先在独立数据库演练，再经已有手动审核发布：正式目录175件、123报价，快照ID 9；旧174件属性、旧价格、已确认需求和配置历史不变，报价重试幂等。原115个新增候选累计15个发布，初始101个待审中完成此1项，剩余100个。审核与复现说明见 `scripts/data/reviewed-intake/2026-09-15-itx/README.md`；原101项清单、原采集及174件真实评估冻结输入保持不变。

发布后新快照SHA256为 `08ef04e6af1d8ff7f98995e07b3738c1216c6608d21241171c20f77b3589422e`，位于 `artifacts/data-intake-after-itx-20260915-r1/`。本次2项脚本测试含5个身份/规格漂移子例通过；演练和正式发布均通过现有加载器、审核与历史保护检查。新增网页研究1次搜索、1次官方页打开及采集脚本1次HTTP读取、重试0；没有新增真实模型、Embedding、买手、SerpAPI或Firecrawl调用。359次模型请求/4169029 tokens的历史累计不变。

本次只证明新ITX型号及原始报价可以被正式读取，不声称ITX整机、性能说明或新的模型通过率已验证。共享产品服务未重启，也未新增浏览器生成验证；历史共同口径差距、剩余Screening语义和自主预算探索仍未解决，目标继续进行。

## Screening 未知信息不再误报冲突

原始真实轨迹L5-304返回owned_parts的model:null，L5-309返回resolution:null/evidence=uncertain；旧适配把两者转为must冲突，虽回复说可继续确认，服务端状态却不一致。6条原始回答及来源哈希保存为 `internal/agents/pipeline/testdata/unknown_live_recording_20260915.json`，新增回归先复现两处错误，红灯日志为 `artifacts/screening-unknown-red-20260915.log`。

当前字段既没有有效值，也没有已有冲突时，缺值set仅保留原文观察，未知保持未知、撤销保持撤销。原有有效要求的模糊更正仍进入待澄清状态，显式conflict及非空非法值沿用原检查；空数组仍表示清空，不当作缺值。Screening提示同步明确未知与冲突的差别；没有关键词改写、额外模型阶段、schema迁移或历史会话批量修正。

Go全源码测试与vet通过。原19题32条真实回答经现行产品、临时数据库和刷新操作离线重放为 **17/19**：`artifacts/planning-screening-unknown-replay-20260915-r1/`，Screening32、Builder0、工具0均为离线协议调用。相比原真实13/19，差别来自此前两题已有件联动修复及本次两题未知状态修复；这不是新真实模型成绩。原强度误判L3-101和预算口径遗漏L4-213仍失败，评分及回答均未改写。原真实13/19及全目标359次模型请求/4169029 tokens累计不变。

本轮未新增网页、真实模型或浏览器操作，共享产品服务未重启，已有会话不因刷新被改写。下一步仍需修复强度/预算口径的模型提取，并按新登记额度做真实复测；本轮状态修复不等同于整体需求理解达标。

## Screening 语义完整性复测：执行前登记

L3-101的原回答把“想要安静”记录为must；L4-213在回复和budget_cny摘录中已识别“新增购买预算”，却漏掉独立budget_basis操作。当前提示增加同次输出的语义检查：stated不等于must，普通选配愿望与不可妥协分开；金额和费用范围分别形成操作，已有件未知不阻止记录明确口径。没有程序关键词填值、额外字段、独立审查模型或修改原评分。

离线机制回归后执行一批原19题、32条消息的真实Screening复测，目录 `artifacts/planning-screening-semantics-live-20260915-r1/`。固定原live套件、174件/122报价快照及baseline-20260915模型；本次不使用新ITX目录以免混入数据变量。真实请求硬上限32，预期Screening32、Builder0，Embedding与外部搜索/读取均0，SDK自动重试0。保存原19题的状态、刷新及来源断言，单次完整复测不当作三次重复达标；批次结束即关闭余量，不将失败题自动重复执行。

该批在 `4033d15` 完整结束，16/19通过，32/32真实请求、113429 tokens、129612毫秒，外部与Embedding均0。原静音强度误判L3-101及明确口径遗漏L4-213通过；新增失败为L4-212/L5-304从“其他件没有/要新买”推断new_purchase，以及L5-305将普通办公归为productivity。均为原文与实际操作不符，原评分保持。与上次同32消息的101231 tokens/118861毫秒相比，本次Token约增加12%、耗时约增加9%；只作该两批描述性比较，不宣称稳定延迟。十一批累计391次真实请求、4282458 tokens，金额未知。

后续修正提示对金额、购买计划、金额覆盖范围的定义，补充对照例子；general与productivity按本项目语义明确区分，不能按英文泛称或CPU型号猜用途。不新增语义关键词后处理。新的针对性批次只取原L3-101、L4-212、L4-213、L5-304、L5-305，共5题8条消息，原输入与断言完全保持。目录 `artifacts/planning-screening-scope-live-20260915-r1/`，上限8次Screening，Builder/Embedding/真实外部均0，模型与174件快照沿用同pin；不同代码批次独立列示，不合并成19题成绩。离线测试通过后执行，达上限即停止。

针对性批次在 `62a6654` 结束，**4/5通过**，8/8请求、29581 tokens、35527毫秒，外部/Embedding均0。L4-212/L5-304未表达的预算口径保持未知，后续准确型号及明确口径可更新；L3-101的prefer和L4-213明确new_purchase通过。L5-305普通办公已正确归general，但首条仍漏budget_basis；回复说“新增采购预算”不代表状态已记录，该题仍失败。未继续追加同题请求。十二批累计 **399次真实模型请求、4312039 tokens**，实际金额未知；各批结果独立保留。

人工复核前一完整32条原始回答还发现自动断言没有覆盖的语义问题：L5-301将未明确口径的新主机预算写为new_purchase；L5-303把“看中售价3000元显卡”写成owned_parts，model甚至为“售价3000元的显卡”。后者在下一轮用户说明没买后移除，但第一轮错误仍成立。原评分只检查预算/用途等局部字段，16/19不能解释为16题所有语义正确。原报告不改写，新增语义审计应独立增加“未表达预算口径未知”“询价不等于已拥有”的负向断言，不能拿扩大断言后的成绩与旧分数直接拼接。

本轮业务提交 `4033d15`、`62a6654`；提示修订均经过pipeline/schema测试，首轮还运行完整离线机制集12/12。实际只新增40次Screening请求、143010 tokens，未运行Builder、网页或浏览器；隔离评估数据库随各批关闭，共享服务未重启。当前仅修复了部分提取误差，遗漏和推断仍存在，不宣称需求理解或整体目标达标。下一步优先补齐上述未覆盖断言并处理来源语义与结构化遗漏，避免靠反复追加同题提示复测来判断成功。

## 补齐语义负向断言

新增独立 `screening-semantic-audit-20260915` 套件生成器，复用L5-301/L5-303原消息、全部原断言及已保存回答，只追加未表达口径与初始非拥有状态断言。旧评分与报告完全保留。保存回答经实际产品/独立数据库/刷新回放为0/2，人工正确操作为2/2，分别保留产物；这验证了原断言漏检的错误，并不证明产品已修复。18项评估脚本测试通过，两次回放各5次离线Screening协议调用，真实模型和网络消耗0。详见[Screening语义负向审计](Screening语义负向审计-20260915.md)。

原文逐字匹配无法验证“已经拥有”或“准确型号”的语义；费用口径遗漏也发生在原始模型operations中，不是服务端丢存。当前没有引入型号白名单、回复关键词回填或额外模型调用来掩盖问题。来源语义、结构化完整性、Builder自主规划与整体验收仍需推进，十二批累计399次真实请求/4312039 tokens保持不变。

## Builder 价格排序与采购口径复测：执行前登记

价格排序 `2d45e54` 和采购口径完成判定 `0007dc0` 已通过离线检查，但尚未真实复测。复用原 `legacy-retrieval-recheck-20260915` 的L1-006、L2-104、L4-206及全部原断言、父配置和174件/122报价快照，观察显式价格排序是否实际使用、模型是否自主比较可行替代、准确已有件缺价是否仍错误阻止采购口径交付。场景范围与之前0/3批次一致，新的数据发布不混入此比较。

离线测试后运行 `artifacts/planning-price-basis-live-20260915-r1/`：最多24次Builder真实请求，每题仍8次往返/24次工具；Screening、Embedding和外部搜索/读取为0，模型及重试配置沿用baseline-20260915。使用新目录保留全部轨迹，不覆盖旧0/3或离线1/3；本批结束即关闭剩余额度，不自动重复失败题。兼容性和交付自动评分之外，继续审阅性能、静音及检索范围断言，不将工具通过视为性能承诺已验证。

## Builder 复测结果与重复预算评估修复

该批在代码 `1418f90` 结束，原评分 **0/3**；20/24次Builder真实请求、31次工具、371619 tokens、224182毫秒。搜索工具操作1次，但使用冻结网页适配，实际外部请求和Embedding均0；剩余4次额度关闭。十三批累计 **419次真实模型请求、4683658 tokens**，实际账单金额未知。没有因失败追加真实请求，也没有更新原174件/122报价快照或评分。

显式价格排序确实被模型使用，但并未解决全部选配行为：L1-006先批量按价格降序检索，后续选到21680.08元完整报价，却因散热能力字段未知及往返额度用尽停留为方案。L2-104使用升序检索，仍自行保留未被用户锁定的R5 7600平台，形成9429元方案并要求用户另行确认换平台；已有7114元可行见证仍保留，不能将失败解释为目录无解。两题都未交付新版本，性能文案仍有缺少对应实测的确定表述，尚未通过解释真实性验收。

L4-206暴露的是另一项重复判定：已有CPU/内存已准确对应，12项兼容性通过，采购金额5166元低于6000元且新购缺价0；整机已知金额6658.70元、已有内存缺价1。模型评估了预算金额但漏写budget_basis的独立met评语，服务端因“预算口径仍待确认是否满足”反复拒绝。这与此前无条件检查整机缺价的缺陷不同：本次移除预算金额/口径/浮动比例的重复模型评语门槛，由既有报价核验统一判断，不更改报价、必须条件、预算上限、兼容性或模型原始输出。

8条真实回答及输入、目录/原报告哈希保存于 `internal/planning/testdata/budget_basis_assessment_recording_20260915.json`。第4条首次完整输出即能交付；降低预算到5000元、新购显卡缺价、改为整机口径时仍保存待解决方案。此前已有件型号不符/不属于已有件的负例继续通过；非预算必须条件仍需有据评估。原单测对“预算少写模型评语必定失败”的预期改为服务器核价，修复反馈测试改用必须显卡品牌，继续验证缺少评估会反馈而非静默放行。没有改动评估集原断言。

Go全源码测试（`./cmd/... ./internal/... ./db`）和vet通过。红灯复现与中间测试错误日志保留；最终日志为 `artifacts/planning-basis-assessment-all-20260915.log` 和 `planning-basis-assessment-vet-20260915.log`。完整3题原回答经产品/临时数据库离线回放 **1/3**：`artifacts/planning-basis-assessment-replay-20260915-r1/`，L4-206生成正式版本并通过刷新断言，另两题原失败保留。共16次离线Builder协议调用、30次工具操作，真实请求0；相比原20次，减少的是这份保存回答回放中的4次重复往返，不宣称新的真实提效成绩。

散热规则另有待审问题：原架构把散热能力列为warning，新规划交付却将所有非pass检查都视为阻断。且跨厂商TDP数字不能直接证明具体CPU/整机散热表现，Noctua的[官方说明](https://www.noctua.at/cn/expertise/guides/noctua-standardised-performance-rating)区分CPU特定表现与机械兼容性。本批没有改规则、填造散热值或放宽评分；需要独立评审证据语义及交付说明。此调查使用Codex网页工具1次搜索调用（2个查询）、2次打开调用（3个目标，其中1次PDF读取超时），与本批Builder实际联网0分开记录，未调用SerpAPI/Firecrawl。

本次没有新增浏览器验证，也未重启共享产品服务；当前修复需在后续统一版本验收中验证界面。当前175件/123报价发布状态未变，剩余候选、Screening语义和模型自主调整仍需推进，整体目标未达标。

## 已校验候选后的追问复核：执行前登记

L2-104原始第4条回答明确将“其他件尽量不动”升级成换平台需要再次授权，输出clarify、draft:null并立即结束；当前运行实际已有9428.70元候选校验，界面文案四舍五入约9429元。仅增加系统提示不足以纠正：原提示已经说明只有用户锁定件不能自行更换，终止路径却不复核clarify。

当前复用proposal的一次决策复核：只有本轮实际调用工具且已有候选校验的clarify，并且还留有工具阶段/额度，才把真实报价与校验反馈给模型，提示根据权威来源检查是否发明了授权条件。模型可重新检索或保留问题，服务端不把clarify强行变ready，不改变用户状态、预算、锁定件或评分。首次追问、未校验的搜索、剩余额度不足直接结束；与proposal共享复核次数，模型/工具总上限仍8/24。

保存的4条原始输出见 `internal/planning/testdata/budget_clarification_recording_20260915.json`，带目录/报告哈希。原回答后重复clarify在第5条正常结束，只有4次往返时原样结束；人工补写的继续检索→evaluate→最终输出在第7条形成7114元候选并保留锁定显卡，用户状态与父配置不变。后3条属于机制示例，不是真实模型成功，未改写前4条回答或历史成绩。

完整离线与Go检查通过后，登记一个原场景L2-104真实复测：`artifacts/planning-clarification-review-live-20260915-r1/`，冻结输入 `artifacts/planning-clarification-review-suite-20260915/suite.json` 仅提取原题，断言、父配置、174件/122报价与模型pin均不变。最多8次Builder请求，每运行24次工具；Screening、Embedding、实际外部查询均0。批次结束关闭余量，不反复同题追跑；重点分别记录是否发生复核、模型是否实际检索跨平台候选、是否生成预算内新版本及解释是否有据，不能仅以新增一次复核证明有效。

该批在 `e47aae3` 完成，原交付评分 **1/1**，7/8次Builder请求、10次工具、137053 tokens、68624毫秒，实际外部与Embedding均0；剩余1次额度关闭。首次启动因子集provenance漏suite_sha256在预检阶段终止，未创建运行目录或发出模型请求；补齐相同套件的哈希后才执行，原启动错误日志保留。不是失败题重复采样。十四批累计 **426次真实模型请求、4820711 tokens**，实际金额未知。

轨迹明确经过本次修复路径：第5条仍要求用户确认换平台并返回clarify；反馈后第6条自主evaluate AM4方案，第7条交付。最终6754元，保留RX 7700 XT、12项兼容性通过、正式v2和刷新断言通过，父版本不变。模型没有照抄单测的7114元见证，而是选择自己先前检索到的R5 5600/B550M/DDR4等候选。相较上一单题4次请求后直接结束，本次多3次包含复核及选配修正；单次通过不能证明稳定成功率，也不代表完整历史集已追平。

解释质量仍有缺口：最终回复把AM4说成“无后续CPU升级空间”，这一绝对结论没有本轮资料支持，冻结目录本身仍包含同平台其他CPU（如5800X3D）。平台新旧、特定CPU升级选择和BIOS支持不能混为一谈；本轮没有以交付评分替代该事实性审计。原回复完整保留，不静默清洗为正确解释。真实7条输出归档为 `internal/planning/testdata/budget_clarification_success_recording_20260915.json`，离线回归再次经过第6条前的复核并核对6754元/保留件/用户快照不变；没有新增模型请求。

Go全源码测试、vet通过，完整机制集 `planning-clarification-review-offline-20260915-r1` 为12/12（Screening23、Builder40、工具26均离线），保存新真实回答后的planning测试也通过。本次没有改兼容性规则或评分，没有网页或浏览器操作，共享服务仍未重启。下一阶段仍需处理解释真实性、其他历史失败和Screening语义，再在统一冻结版本验证整体指标；当前目标继续进行。

## 当前业务版本浏览器补验

使用上一批7条真实回答及原冻结目录，新增测试二进制专用浏览器入口；其余检索、核价、兼容性、A2A、HTTP、事务及刷新均走生产代码。桌面/移动端实际完成确认→自动v2→旧v1只读→刷新→返回v2→来源展开→新会话与建议输入；服务端断言v1未变、v2只生成一次、离线模型协议总计7次。测试PASS，产物 `artifacts/planning-recorded-browser-20260915-r1/`，详见[验收记录与复现](browser-recorded-20260915.md)。

复制按钮显示成功但内置浏览器读取/粘贴旧剪贴板，本项保留未通过，不猜测修复；隔离环境未接生产Builder/Redis健康探针，因此服务可用性与SSE不在本轮通过范围。此前 `planning-browser-20260915-r2` 包整体exit_code=1的旧记录不视为全套测试通过，本轮单独保存明确范围。新增实际模型/外部请求0，426次/4820711 tokens累计不变。临时服务与容器已关闭，共享服务未重启。

重新核对候选清单：原101项均已有逐项pending_evidence及理由，28项拒绝的是原报价绑定，并非拒绝型号存在。后续ITX批次对其中mb-msi-b650i-edge完成补证并正式发布；原清单不可改写为全部已入库，目前仍100项待证据。175件/123报价的发布记录与快照保留，浏览器使用174件冻结目录以保持同一模型复测口径，不能把测试库的数量当作生产发布状态。

## 来源关系与预算口径：执行前登记

FIT V内存补证本轮普通HTTP返回406，原始清单及失败记录归档于 `scripts/data/reviewed-intake/2026-09-15-fitv-review/`，没有重试或买手调用。较低的旧聚合报价不能直接进入正式数据，当前175件/123报价保持。补查预算链已核对到84次detail/307次HTTP，不重新从原82次开始。

Screening修正先区分对象关系：报价、购物意向、备选与确实已有分别表达，准确型号与拥有关系独立；明确新增采购费用时同句记录金额和口径，不能因已有件型号是否完整漏项。复用当前一次Screening输出，不增加关键词修补、后置总结或请求次数，历史错误状态不静默改写。

离线检查后执行 `artifacts/planning-screening-ownership-live-20260915-r1/`。输入 `artifacts/planning-screening-ownership-suite-20260915/live/suite.json`（SHA256 `76802b7cb554ce5090ef99a79aed166baabc8c4b9949a263e484e17154c343f4`）取L5-301/L5-303既有独立负向审计和原L5-305，保留全部消息及断言，共3题7条消息。不是把新增断言回填历史19题成绩。配套oracle仅验证机制，不能当真实模型成绩。

真实批次上限7次Screening；Builder、Embedding、实际外部查询均0。模型沿用baseline-20260915的deepseek-v4-flash-0731及原174件/122报价快照，自动重试0。结束即关闭余量，不因失败追加采样；保存并审阅每轮原始operations，分别报告新增审计与原题结果，不以reply措辞代替状态通过。

该批在 `77054f9` 结束，预登记评分 **3/3**：独立负向审计L5-301/L5-303为2/2，原L5-305为1/1。7/7次真实Screening请求、26916 tokens、34310毫秒，Builder、工具、Embedding和外部请求均0。十五批累计 **433次真实模型请求、4847627 tokens**，实际账单金额未知。本批额度关闭，未追加同题采样；这不是原19题整体复测或稳定成功率。

L5-301三轮未推断预算口径或已有件；L5-303不再把显卡报价记录为已有型号，首轮预算保持未知；L5-305同时记录金额和new_purchase，下一轮改为full_build且保留CPU、金额和普通办公用途。真实产品数据库、来源和刷新断言通过。与旧样本相比目标错误本次未复现，但单次样本不能证明所有同义表达已解决。

人工审阅另发现L5-303第一轮回复声称“显卡报价已保留为备选参考”，operations仅记录用途/分辨率，observations仅记录预算未知，实际没有保存显卡报价参考。第二轮也只将未购买说明作为观察记录。原负向断言没有覆盖参考信息保留，因此3/3不能解释为本轮原文信息完整或回复完全真实。原回复原样保留，未根据回复自动回填状态；此缺口仍未修复，需要单独验证信息保留和“已记录”文案一致性。

7条真实输出及来源报告/目录哈希归档为 `internal/agents/pipeline/testdata/ownership_scope_live_recording_20260915.json`，由 `TestRecordedOwnershipAndBudgetScope` 回放已有件关系和口径更正，已知遗漏仍写入fixture说明。完整可恢复产品回放套件位于 `artifacts/planning-screening-ownership-recorded-suite-20260915/`，可用既有run.py离线执行；不是新真实成绩。生成前pipeline/schema测试、机制集12/12、人工oracle3/3以及新增录制回归通过。本轮不涉及UI，未重复浏览器验证或重启共享服务；正式数据及历史配置保持不变。当前仍需补齐未解决语义、Builder历史失败、解释真实性与统一版本全套验证，整体目标继续进行。
