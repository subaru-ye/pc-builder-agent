# Design Context — 装机配置单 Agent

## Product

装机配置单 Agent 是一个对话式 DIY PC 配置工作台。它把自然语言需求转成结构化 RequirementSpec,由多 Agent 生成配置,再用确定性规则引擎给出兼容性、功耗、报价和版本差异。

产品的可信度来自可见的规则结果、带日期的价格快照、可回看的版本和诚实的 unknown,而不是聊天拟人感。

## Audience

- 不熟悉兼容规则,希望快速得到可理解配置的普通用户。
- 需要帮他人配机、解释理由并比较多个方案的进阶用户。
- 用户默认懂预算和游戏名称,不要求理解 socket、TDP 或供电接口术语。

## Primary workflows

1. 输入需求并回答必要追问。
2. 查看、编辑和确认结构化需求。
3. 等待生成与校验,理解真实进度。
4. 阅读配置、价格、理由、规则和免责。
5. 用自然语言改单,回看版本并比较差异。
6. 导出 Markdown、分享图或只读链接。

## Design goals

- Trustworthy:规则与快照比模型措辞更醒目。
- Dense but calm:同屏信息多,但层级稳定、颜色克制。
- Actionable:每个状态都告诉用户下一步。
- Honest:不伪造实时价格、进度、兼容结论或上下文恢复。
- Local-first:没有域名/服务器时仍是一套完整产品,并准确说明分享范围。
- Accessible:中文长文本、键盘、移动端和色觉差异均可用。

## Brand feel

精确、冷静、技术可信,但不冷漠。更像一张经过工程校验的工作台,而不是电竞灯效页面或电商促销页。语气使用简洁中文,避免夸张承诺、AI 神化和硬件圈黑话堆砌。

## Information density

桌面端为中高密度。聊天负责意图和解释,右侧检查器负责结构化真值。配置和校验使用紧凑行,不是八张商品卡加十二张规则卡。手机端通过面板切换保持内容完整,不把关键校验删掉。

## Color strategy

- 深色为默认唯一主题;阶段 1 不实现浅色模式。
- 紫蓝只表达主操作、选择、链接和焦点。
- pass/review/fail/unknown 使用克制的语义色,并配文字/图标。
- 价格不是强调色;总价通过字号、位置和字重建立层级。
- 快照、缺价、免责使用可读的 muted 文本,不能低于 WCAG AA。

## Typography

Geist Sans/Mono 为拉丁与数字主字体,中文回退到 PingFang SC、Microsoft YaHei 和系统 sans。中文正文不做过紧字距。SKU 使用 mono,型号和理由使用 sans。全产品不超过 sans + mono 两套字体角色。

## Layout principles

- 桌面是左聊天、右检查器的稳定双栏工作区。
- 顶栏只放方向、健康和交付动作。
- 每个区域只有一个主要任务;内部优先列表、分隔线和定义项。
- 免责和快照在配置/分享面始终可见。
- 不使用营销 Hero、dashboard card mosaic 或装饰性图像。

## Content principles

- 使用「正在生成并校验配置」,不使用「AI 正在深度思考」。
- 使用「价格快照 2026-07-28」,不使用「最新低价」。
- 使用「数据不足,需复核」区分 unknown,不把它写成「基本兼容」。
- 错误说明数据是否已保存、能否重试、下一步是什么。
- 快捷改单只预填自然语言,让用户保留最终决定权。

## Product constraints

- 当前数据只有 AM5,每类约 20 个 SKU。
- 没有可信商品图片资产。
- 生成可能持续数分钟,必须持续提供真实状态/心跳。
- Redis 可降级,但完整验收依赖 Redis。
- 第一阶段匿名、无账号、无正式公网 SLA。
- Next.js 不拥有业务规则或数据库写逻辑。

## Success check

只扫描页面的标题、状态、价格和操作时,用户应能回答:

- 我现在处于收集需求、待确认、生成中还是已有配置?
- 当前是哪一版,比预算多/少多少?
- 配置是否通过,哪些项目需要复核?
- 价格是哪一天的?
- 我要如何修改、比较、导出或分享?
