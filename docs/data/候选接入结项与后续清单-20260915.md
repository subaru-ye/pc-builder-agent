# 候选接入结项与后续清单（2026-09-15）

本轮完成115个新增候选的批次审查、四批可确认型号发布、旧新目录离线对比和多轮机制回归。接入范围按证据成熟度分批推进，不将全部候选强制发布。原始采集文件、旧需求及旧配置版本保持不变。

## 当前结果

- 正式目录174件，最新报价快照ID 8（2026-09-15）含122条参考价。报价来自聚合来源的人工复核，不代表实时可购或库存确认。
- 115个候选中14个已发布：CPU 8、GPU 1、SSD 3、内存1、机箱1。
- 101个尚未发布：28个有明确身份/变体冲突；其余73个仍需精确变体和官方规格复核，不能解读为已通过审核。
- 待审分布：CPU 10、GPU 25、主板21、内存6、SSD 14、电源5、机箱11、散热器9。其中31个原始主报价没有购买链接；该数字与身份、规格缺项有交叉，不能相加。

逐项状态与输入哈希见 `artifacts/data-intake-disposition-20260915/{candidates,summary}.json`；复现：`python artifacts/data-intake-disposition-20260915/build_report.py`。清单基于冻结输入，不会修改数据库，也不把尚未复核的官方字段写成已知事实。具体来源、发布批次和尺寸规则限制见[接入审查](新型号接入审查-20260914.md)。

## 评估结论及边界

- 原有12个机制场景通过；新增3个多轮行为场景通过，覆盖非预设需求、预算未知、已有件简称、撤销、检索失败反馈和刷新恢复。
- 固定旧新目录的2个成对场景分别通过。新型号可进入真实本地检索；旧AM4方案在最新快照仍缺4条报价，服务保留待解决结果，没有拿旧报价填平缺项。
- 166项数据测试、9项接入脚本测试及已发布型号的Builder本地检索测试通过。详情见 `docs/eval/planning-v2/`，保留失败的首轮证据，不覆盖为成功。
- 模型响应为保存/脚本回放。以上证明接口、状态和数据流，不证明真实模型选件质量、实际响应延迟或扩库后的方案成功率。本轮没有新增独立模型、SerpAPI或Firecrawl请求。

## 本地运行与浏览器补验

2026-09-15重新构建并启动API、Builder和前端，日志及二进制位于 `var/run/data-review-20260915/`。启动前未发现这些服务的运行进程，数据库中运行中任务为0；保留已有PostgreSQL、Redis、账号服务及其他项目容器。`GET /readyz` 返回ready，四项依赖均为ok。Screening提示修正已随新API生效。

此前浏览器工具连接失败的问题在本次检查中消失；以下是真实浏览器操作结果，不替代上述离线生成链路回放：

- 首页服务就绪，已有会话可访问；读取原有3版配置。
- 从v3切到v1，显示历史只读、原始2026-07-28快照与8168.90元；刷新后仍停留v1，没有被当前目录或新报价覆盖。
- 新建空白会话可正常导航，建议输入和左右侧栏存在，需求面板明确预算/用途/分辨率等为未知；刷新可恢复。
- 720px视口下document与body高度均为720px，未见整页多余滚动；本次读取的浏览器error日志为空。
- 只创建了一条空白检查会话（`f3223d1d-28eb-43e2-b89b-d755406d5107`），未发送消息、未修改原有需求，也未生成配置。未验证真实模型生成、移动端或全部交互边界；不会将本次检查表述为这些环节已通过。

## 后续优先级

1. 补价格覆盖：优先为显卡、内存、SSD、机箱补精确型号报价，尤其固定旧AM4方案的4个缺价件。报价仍走买手路线，不用官网标价或旧快照伪装新报价。
2. 解决28个身份冲突：检查选中型号、容量、系列后缀及成色；无法唯一匹配时保留候选或按真实变体另建档，不沿用错误型号。
3. 按整机搭配需要补规格：主板平台、内存代际、显卡供电/尺寸、机箱和散热限制；官方信息与卖家报价分别留证。LANCOOL 207额外主板宽度/电源长度限制尚不在现有确定性校验字段中，详见接入审查。
4. 下一批发布后冻结新快照，再用相同代码、模型和预算做小规模真实A/B。分开计报价覆盖、兼容性未知、模型停滞、方案质量、耗时与调用开销，不把数据扩充收益混进机制回归分数。

长期个人偏好平台、全市场采集和全部101个候选自动晋级均不属于本轮交付。
