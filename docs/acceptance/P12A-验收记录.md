# P12A 验收记录

> 日期：2026-08-27。状态：P12A 已完成；P12B 未开始，因此 P12 不标记完成。

## 已实现

- 数据库迁移 9：不可变 `price_observations`、price snapshot 发布链和逐 SKU 观察元数据。
- 旧 `2026-07-28` 快照按 bootstrap 回填，不伪造商品 URL 或 observation。
- `pcdata price import/review/publish/health` 和 weekly/retry inbox。
- 精确变体、库存、价格类型、中位数/优先级、25% SKU 异常和系统性批次异常门禁。
- 独立 price release v1、哈希复核、Go 单事务导入和 current-price 指针。
- 产品 API 的兼容性 freshness 字段、工作台/分享页/Markdown 动态过期提示。
- ZOL 保持禁用候选；定时链没有模型入口。

## 尚未完成

- 没有获准并启用的真实自动价格来源。
- 没有连续两个真实 weekly 周期。
- 没有达到自动来源覆盖全部正式选价、平均年龄 ≤7 天、最大年龄 ≤14 天。

以上三项属于 P12B，未通过前不得把 P12 或阶段 2 写成完成。

## 2026-08-27 跨语言烟测

在临时 PostgreSQL 数据库和临时 runtime 中执行完整链路：空库迁移 → 160 个 parts → 历史 160 个 bootstrap 价格 → 严格 P12 CSV → review → price release → Go 原子导入 → health。

结果：

- 数据库迁移到版本 9。
- 新快照完整保留 160 个选价，其中 1 个新 observation、159 个逐 SKU 沿用。
- 数据库共有 2 个 snapshot，当前 snapshot 有 160 行，observation 有 1 行。
- health 正确报告 `stale`、最大年龄 30 天、平均年龄 29.86 天；没有因新发布日伪造旧价年龄。
- 临时数据库在烟测结束后删除；正式数据卷未删除，未调用模型。

## 验收命令

```text
go test -count=1 ./...
go vet ./...
go build ./...
PG_TEST_DSN=... go test -count=1 ./cmd/importpriceobservations ./cmd/importprices ./internal/store
uv run --project scripts/data pytest scripts/data/tests
pnpm --dir web lint
pnpm --dir web typecheck
pnpm --dir web api:check
pnpm --dir web test --run
pnpm --dir web build
pnpm --dir web test:e2e
git diff --check
```

## 最终结果

- PostgreSQL/Redis healthy；正式数据库迁移版本 9，parts 160、当前 prices 160、正式 observation 0。没有为验收伪造正式价格证据。
- 设置 `PG_TEST_DSN`、`REDIS_TEST_ADDR` 后执行 `go test -count=1 ./...`，PostgreSQL/Redis 集成测试通过且没有环境变量缺失导致的 SKIP。
- `go vet ./...`、`go build ./...` 通过。
- 官方 `golang:1.26.5` Linux 容器执行 `go test -race -count=1 ./...`，全部通过。
- Python 149 项通过；Web lint、typecheck、OpenAPI 漂移、16 项 unit、build、18 项 mock E2E 通过。
- JSON schema、PowerShell 语法、Markdown 本地链接、UTF-8 无 BOM、LF、末尾换行、敏感信息扫描和 `git diff --check` 通过。
- 工作树干净；没有调用模型、删除数据卷、移动标签或创建 P12 标签。
