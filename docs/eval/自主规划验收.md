# 自主规划验收记录

## 2026-09-11：自动交付、需求修订与来源展示

- 保存的真实案例：7000 元视频剪辑会话，Builder 返回 proposal、issues 为空、12 项兼容性通过、报价 4579.90 元、100 条 catalog 来源。记录保存在 `internal/planning/testdata/complete_proposal_recording.json`；没有再次请求真实模型。离线 Runner 用原选件重新执行真实校验与报价，结果自动转为 eligible，再由产品事务保存 delivered 和正式版本。
- Go 回归：`PG_TEST_DSN=... go test ./...`、`go vet ./...` 通过。数据库测试在独立临时库执行全部迁移，验证并发/重复完成仅生成一版、后段消息写入失败整体回滚、需求修订变化只存 stale 方案、历史配置与需求不被覆盖。
- 前端回归：17 个测试文件、58 项测试全量通过；随后新增“同链接未选中摘录保留在次级区域”用例，来源模块 4 项测试通过（当前合计 59 项）。TypeScript 与 ESLint 检查通过。
- Playwright：桌面 1440×960、平板 768×1024、手机 375×812 完整流程通过。覆盖新对话、需求编辑与确认、proposal 自动转正式 v1、必须静音保留为具体待解决方案、来源归并、切换旧版本、聊天放宽为软偏好后生成 v2、自动退出旧版本链接、分享/导出、刷新恢复与继续编辑。使用真实 API、数据库、规则和报价；Screening 与 Builder 为离线录制/oracle，没有网络模型或搜索客户端。浏览器测试选件、报价日期、价格与来源取自保存记录；后台使用内存事件服务，页面如实提示离线测试环境的事件存储降级。
- 素材修改：真实用户原话“2K 视频 → 素材大约1080p”，通过修正后的 oracle 操作验证稳定字段覆盖、同轮重复说明清理、来源历史保留、预算和其他用途不丢失、撤销后不复活。这里只证明提示契约与业务合并链路，**未重新验证真实 Screening 模型能否稳定输出这些操作**。
- 本轮真实生成模型、Embedding、SerpAPI 搜索和网页读取均为 0；普通启动只检查健康状态。Builder 原有往返与工具限额不变；补齐选中配件来源只增加本地数据库读取。
- 主库仅应用迁移 00017 增加方案与版本关联，不修改已有需求、方案内容或历史版本。当前服务入口使用 `artifacts/planning/api-delivery.exe`、`artifacts/planning/buildsvc-delivery-final.exe`，前端仍为 3000 端口。旧方案不会因刷新自动转成正式版本；后续按当前需求重新选配。补充回归确认：待解决方案保留外部备选，正式配置只保存实际选中配件。

离线浏览器复验：先使用 `PG_TEST_DSN` 启动 `TestPlanningBrowserServer`（`PLANNING_BROWSER_ADDR=127.0.0.1:18084`），再用 `GO_API_BASE_URL=http://127.0.0.1:18084` 和独立 `NEXT_DIST_DIR` 启动 3102 前端，执行 `REQUIREMENTS_WEB_URL=http://127.0.0.1:3102 pnpm --dir web exec playwright test --config=playwright.requirements.config.ts planning.spec.ts`。测试结束调用离线 API 的 `/__offline/shutdown` 清理临时数据库；不要对用户主库做数据回放或重置。

## 2026-09-10：首次自主规划链路

默认执行器已切换为 planning，迁移 00016 已应用到本地数据库。运行方式与边界见[自主规划流程](../tech/自主规划流程.md)。本轮未运行全量模型评估，旧 v1.5 分数不代表此执行器。

## 已完成验证

| 验证 | 结果与证据 |
|---|---|
| Go 单元/协议回归与静态检查 | `go test ./...`、`go vet ./...` 通过；API、buildsvc 编译通过 |
| 实际数据库 | `TestPlanningProposalPersistentWorkflow`：正式 v1 → 必须静音 → 待解决方案 → 撤销静音 → 正式 v2；刷新恢复、原 v1 及候选快照未变 |
| 配额竞争 | `TestPlanningSharedQuotaIsAtomic`：8 个并发预留争用 2 次额度，仅 2 个成功；旧 provider usage 不重新开放额度 |
| 开放需求 | 未预设用途、多个自由条目、低预算、已有件简称、目录外品牌与便携要求进入真实模型输入和检索工具；预算及所有字段未知仍可正常追问 |
| 需求变更 | 原有 reducer/Screening 回归与新自由条目测试：修改预算保留偏好、撤销、备选、must/prefer、临时例外与朋友装机隔离 |
| 校验修复 | 保存的真实商品/配置加一块明确不兼容主板；真实规则返回失败后，离线模型 oracle 自主选择原兼容主板并再次校验通过 |
| 外部候选 | TLS 测试服务的 HTML 包含保存的真实商品参数；读取 → 逐字段引用 → 注册会话候选 → 快照校验通过；不写全局目录。虚构/无引用数值保留未知，私网与非 HTTPS 读取被拒绝 |
| 前端 | TypeScript、ESLint 通过；16 个测试文件、55 项测试通过 |
| 浏览器 | desktop/tablet/mobile 各 1 个完整场景通过：新会话、自然语言初筛、面板修改、正式配置、公开分享页与 Markdown 导出、must 修改后的方案、切回旧配置、刷新及自由要求新增 |
| 本地运行 | 3000 页面显示服务就绪，新建对话和输入可用；真实 API 面板写入自由要求后刷新恢复。8081 日志确认 harness=planning，8082/Redis 正常 |

浏览器测试使用临时 PostgreSQL 数据库。模型决策明确标为离线 oracle，HTTP、状态合并、工具、规则、价格、持久化、分享和导出均为实际实现，未使用前端 route mock。临时服务器和数据库已清理。

## 真实模型调用与发现

固定 Screening `deepseek-v4-flash-0731`、Builder `qwen3.8-max-0902`；模型链为空、自动重试为 0。场景为“预算6000元，主要剪4K视频，尽量安静”，随后“静音改成必须满足，其他要求不变”。

- 首段：Screening 2 次、Builder 7 次；工具 5 次；Builder 36.723 秒，53,878 Token。
- 续段：复用已保存状态，剩余 Builder 3 次；工具 2 次；22.704 秒，66,848 Token。
- 累计 **12 次模型请求**，**125,115 total Token**（含 Screening 4,389），**0 次外部搜索请求、0 次网页读取**。达到原定模型上限后停止，没有换模型或追加全量评估。

首段暴露全文精确匹配导致自然语言查询多次为空；改成相关性排序后，续段保留了 16 个具体电源/散热器候选，模型提出核显+代理剪辑、独显加速两条讨论路径，并追问复用件和静音工况。must 保留，没有因缺少静音目录证据在调用模型之前退出。最后结果为 clarify，没有冒充可交付配置。

来源上下文使 Token 明显增加；随后补充了上一方案摘录压缩和 `read_evidence` 按需展开，原始证据仍完整保存。该压缩改动未再调用真实模型验证节省幅度。

原始产物：`artifacts/planning/live-smoke.json`、`artifacts/planning/live-continuation.json`；浏览器截图位于 `web/test-results/`，均按仓库规则不入库。

## 尚未验证或仍存在的限制

- 真实 SerpAPI 查询、真实网页成功读取和目录外商品的完整线上交付本轮未验证；相关协议、共享配额、失效处理及快照接入通过的是离线测试。
- 真实模型本轮止于候选及必要追问，完整正式交付通过的是数据库/浏览器离线回放。不能据此宣称任意要求都能自动满足或保证静音。
- 首段真实 Screening 把素材 4K 误写进目标分辨率。已补充区分素材参数与显示目标的示例；受累计调用上限限制，更新后的提示尚待后续定向实测。原始失败输出保留，不将其记为已通过案例。
- 没有建立跨会话个人画像、长期偏好平台或全市场采集系统。模型主动选择的搜索条件不改变用户要求；服务端仅持久化本次会话和已生成版本。
