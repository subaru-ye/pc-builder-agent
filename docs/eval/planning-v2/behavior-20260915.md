# 自由需求与工具失败的离线回放

新增3个场景、12个步骤、147项检查，在完整冻结172件目录上通过。原12个机制场景也全部通过。本轮未改变商品/价格数据或共享会话，未调用模型提供方或联网API。

| 场景 | 验证内容 |
| --- | --- |
| B2-001 | 预算未知、本地Whisper用途、音频不上传云端和负向品牌条件分别保存；确认后进入Builder检索，撤销品牌限制不丢失其他要求，刷新不恢复旧值 |
| B2-002 | 已有5600/B550M简称保留原文，不猜准确型号；先检索CPU与主板，追加6000预算后仍保留已有信息，只澄清需要核对BIOS的主板型号 |
| B2-003 | 无Embedder与网页读取失败均进入模型工具反馈，已有本地9950X3D候选及规格保留；用户要求继续后可再检索，刷新不触发模型 |

Screening协议6次、Builder协议15次、工具9次，其中网页读取工具1次；真实提供方与外部请求均0。网页故障由未登记URL的离线transport产生，并非本次真的访问外网失败。模型决策是人工oracle，不能据此声称真实模型已能自主理解所有上述需求。

## 实际修正

Screening现行提示仍包含“已有件型号缺失给程序追问”和“负向品牌约束放notes”的旧指令，已统一为模型判断必要追问、独立条件使用free.*条目。冲突字段不阻止其他可靠信息更新；新notes只保留补充背景，历史notes中的有效内容不静默删除。没有增加总结模型或关键词修补路径。

首次回放2/3通过，已有件数组触发评分器误判：服务端与Builder字段的值、状态、来源、强度一致，但JSONB与接口编码产生空格差异。评分器复用已有JSON语义比较核对整个字段；反向测试确保丢失配件、改变强度、丢失来源仍失败。未降低业务预期、改写已有件或更新失败数据以绕过检查。

## 复现与证据

```bash
go test ./internal/agents/pipeline ./internal/planningeval ./cmd/evalplanning
python scripts/eval/planning/run.py --suite internal/planningeval/testdata/behavior-20260915/suite.json --out artifacts/behavior-new-run
python scripts/eval/planning/run.py --out artifacts/base-new-run
```

套件与来源哈希：`internal/planningeval/testdata/behavior-20260915/`；显式生成器：`scripts/eval/planning/build_behavior_extension.py`，只读取已冻结的新目录套件，不访问运行数据库。

失败记录保留在 `artifacts/planning-behavior-20260915-r1/`；最终通过记录在 `artifacts/planning-behavior-20260915-r2/`；基础回归在 `artifacts/planning-v2-after-behavior-20260915-r1/`。中断发生时两次最终运行均已完成，exit_code均0，专用容器已清理，没有重新执行。

本轮没有浏览器验收、真实模型或生产耗时结论。后续真实模型评估仍需独立设置额度与未用于调整提示的保留题。
