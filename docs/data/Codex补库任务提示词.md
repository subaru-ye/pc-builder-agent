# Codex 补库任务提示词

> 本文是此前的混合路线方案。当前用户指定的“仅用买手发现新型号并扩库”任务，请使用[买手扩库任务提示词](买手扩库任务提示词.md)，不要把 Crawl4AI 加入该任务。

以下正文可复制到本项目的新 Codex 本地任务。建议使用基于最新提交的独立工作区；数据采集及人工辅助审核由该任务完成，不调用产品模型流水线。

---

请为 pc-builder-agent 完成一批真实、可追溯、可离线重放的商品数据补充，包含新型号/规格补全和旧价格刷新。项目主目录为 C:\code\pc-builder-agent；优先在当前独立工作区修改，读取主目录已有原始资料时不要覆盖它们。

先阅读项目开发约定、docs/tech/网页读取与补库策略.md、docs/data/数据获取与发布规则.md、docs/data/数据来源决策.md、docs/data/AI辅助采集与入库指南.md，以及 scripts/data/price-batches/2026-09-08/README.md 和对应 collection-tools 脚本。复用已有 schema、canonical 字段、来源证据、review/quarantine/release、导入器及离线测试。

你就是本次资料理解、比对和候选复核的 Agent。使用当前 Codex 任务的能力，不编写或执行另行调用项目 Screening、Builder、Embedding、SerpAPI 或第三方模型 API 的脚本，不启用 Crawl4AI 的 LLM 提取。允许使用 Codex 当前可用的网页搜索寻找网址、读取公开厂商资料和本地 Crawl4AI；允许通过买手技能请求其聚合服务。记录各类实际调用数量，不能把“没有单独的模型 API 调用”写成“没有网络请求或额度消耗”。

买手技能在 C:\Users\83818\.qoder\skills\taobao\：先读取该目录 SKILL.md，确认实际 search/detail 接口，再按游客态调用。若技能或工具不可用，报告具体缺项并继续可独立完成的官方规格采集，不猜造结果，不安装不明替代工具。聚合报价始终标记 aggregator_secondary，用于项目学习/评测快照；detail 对价和短链不能当作已核实库存、最终成交价或平台授权。

Crawl4AI 已部署为本机 Docker HTTP 服务，默认 http://127.0.0.1:11235。检查 /health；必要时按运行文档启动。仅从主目录 .env 读取 CRAWL4AI_URL / CRAWL4AI_API_TOKEN，不能输出 Token 或复制整个 .env。通过 /crawl 读取指定公开 HTTPS 网页，使用固定的非 LLM 配置，保存正文和来源；不要改变全局容器配置、启用私网访问或控制用户浏览器登录态。官方页面入口可用 Codex 搜索或厂商导航发现，Crawl4AI 本身不等于全网搜索引擎。

先审查现有目录及保存的脱敏失败记录，选择最多 20 个目标 SKU，兼顾有实际价值的新型号/升级路径与旧价刷新，不为凑数量降低质量。重点考虑 CPU 升级候选、主板 CPU/BIOS 支持证据；价格补采优先显卡和 SSD。2026-09-08 快照为 102 更新+58 沿用，其中显卡 12/20、SSD 10/20 仍是旧观察，先核实现状再选择目标。简要说明名单和理由后继续执行，普通采集和整理无需逐项询问。

执行要求：
1. 在新的日期+唯一批次号目录工作，不改写 v3.1 或历史决策表。初始上限为买手 search 60 次、detail 30 次、Crawl4AI 页面请求 40 次；失败及重试计入，达到上限保存进度并报告，不自动追加。复用已保存结果，登录/验证码/限流或拒绝访问时保留失败并换合适来源，不绕过。
2. 新型号先明确品牌、完整型号、后缀、容量、条数、版本和包装等身份。公开厂商规格优先，记录逐字段 URL、获取时间、原文摘录和原始文件哈希；主板兼容还要保留准确板型、硬件修订及 BIOS 条件。没有证据的字段为未知，候选仍可保留供后续补齐，不能猜成通过或塞进已有近似 SKU。
3. 买手保留原始标题、价格、店铺、平台、goodsId、明确变体、详情结果和链接。以 source+goodsId+变体+原始行哈希关联；保留 needle 作为可读判别。无法唯一定位、型号混串、多型号最低价不明确时保留待复核/拒绝。旧价带与家族梯度仅作异常提示，不能单独证明或否定价格真实性。
4. 维护可重放决策表：accept、carry、reject/待复核及理由均有证据。你的判断标为 Agent 辅助复核，不伪称用户人工批准。carry 保留旧价、旧来源、旧观察时间；无基线的新 SKU 缺价时保留未知，不能套用旧 SKU 价格。公开商品事实与价格观察分开，不把私人会话偏好写进公共库。
5. 产出候选数据、价格快照/学习行、字段证据、来源清单、决策表、校验报告和导入说明，依现有忽略规则放置原始大文件。原始资料位于主目录 var/data/collections/ 时可通过 COLLECTION_DATA_DIR 只读复用；工作区没有资料时明确报告，不假装重放成功。
6. 用现有加载器、schema、唯一性/变体/来源日期检查验证；离线重放应得到同一产物。需要数据库验证时仅使用明确隔离的测试库，验证导入可重放以及新型号可被结构化检索到。不要调用 embedding API；语义索引未更新要单列。不要删旧记录、覆盖历史配置或往当前产品库直接导入。

完成后提交这批数据和必要工具改动，不 push。汇报新增型号数、补齐字段数、更新/沿用/拒绝数、每类网络调用数、未解决问题和验证范围。给出数据 Diff 与目标库明确的导入命令，让主任务可审核后整合；不要扩展前端、改 Builder 业务流程或建设新的数据平台。
