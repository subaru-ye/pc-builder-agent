# 动态需求状态栏验收

执行日期：2026-09-09 至 2026-09-10。验证当前工作树，未发布部署，也未改变项目模型配置。设计与用法见[动态需求状态栏](../tech/动态需求状态栏.md)。

## 需求与证据

| 验证项 | 当前证据 |
|---|---|
| 预算修改保留用途、静音、品牌等其他信息 | reducer 回归；真实 PostgreSQL workflow；固定模型第1、2轮 |
| 已有信息不重复追问 | 仅 active 状态投影生成必要追问；14轮保存真实输入重放；固定模型断言 |
| 明确撤销后不生效、不被旧消息复活 | removed 标记、Screening 只接收当前状态/本轮消息；真实模型最终轮；浏览器撤销与临时恢复 |
| 必须条件和软偏好分开 | schema/Screening grounding；Builder 品牌/尺寸硬筛选与软排序；无证据必须条件明确停止 |
| 讨论备选不改变当前要求 | reducer 独立 alternatives；固定模型4K仅备选、2K保持；三视口浏览器 |
| 本次帮朋友装机不污染其他会话 | 真实数据库新会话全未知；状态仅存 web_sessions，不写个人画像 |
| 面板编辑与聊天一致 | 共用 reducer；真实 HTTP＋PG 浏览器任意预算编辑、聊天再改、来源定位 |
| 刷新/进程恢复 | 同一PG新建 Service 实例读取一致；三视口实际刷新 |
| 确认需求与版本不被草稿覆盖 | 数据库事务确认投影；保存v1后编辑/撤销及原确认重试，旧 requirements/builds 不变 |
| 并发与幂等 | expected_revision 冲突；完整操作指纹区别备选/采用；已完成确认不受后来不完整草稿影响 |
| 旧会话兼容 | 旧状态保持 NULL；增量编辑拒绝从空状态覆写旧需求，旧表单流程保留 |

## 已执行检查

- `go test ./...`、`go vet ./...`、`go build ./...` 通过；真实模型测试默认跳过。
- `PG_TEST_DSN=… go test ./internal/store -count=1` 全部通过，30.799秒。每个测试使用独立临时数据库，全迁移后运行，保留主库数据卷。
- `PG_TEST_DSN=… go test ./internal/producthttp -run TestRequirementStatePersistentWorkflow -count=1 -v` 通过，包含实际存储、归并、重新核算、快照与版本保护。
- Web 组件、TypeScript 与 ESLint 检查通过。
- `pnpm exec playwright test --config=playwright.requirements.config.ts`：1440、768、375三视口 3/3 通过；axe 无 serious/critical，预算行不超过72px，主要交互无横向溢出。三份截图经视觉检查。
- 保存基线150条离线 replay 与原归因一致；57个保存 Screening 样本原文审计通过。新协议另外重放14轮真实输入，增量操作为明确标注的人工 oracle，不能当成真实模型成绩。

浏览器验证使用真实 Next.js、产品 HTTP、Service、PostgreSQL、规则核算和 Presenter，只有 Screening 提取与 Builder 选件使用测试回放。面板编辑任意金额由实际 API/reducer 处理，不使用页面路由伪造状态。回放配置来自保存的真实生成产物，仅用于版本链路验证，不宣称是模型按任意新预算重配的结果。

截图：[桌面](../../artifacts/requirements-state/browser-final/desktop.png)、[平板](../../artifacts/requirements-state/browser-final/tablet.png)、[手机](../../artifacts/requirements-state/browser-final/mobile.png)。

## 固定模型小规模冒烟

固定项目当前 Screening `deepseek-v4-flash-0731`，不修改 .env，无模型链，SDK重试为0，整个本次验证硬上限4次。没有调用Builder或Embedding，也没有重新运行全量模型评估。

1. 初次预算8000、2K游戏、尽量安静、尽量N卡、帮朋友：通过。
2. 预算修改为6000，其他条件和来源保留：通过。
3. 取消显卡品牌偏好：模型漏填quote，服务端拒绝保存。由此增加“本轮明确撤销对应字段时补绑定原文”的狭窄确定性修复，并用保存的实际失败输出离线验证。
4. 从第2轮状态恢复，仅调用剩余1次，同时测试撤销品牌和4K备选：通过，当前2K/预算/其他偏好保持，显卡偏好removed，4K仅进入备选。

累计：**4次Screening、0次Builder、0次Embedding；输入5,800、输出335、合计6,135 tokens**。没有第5次调用。首次失败证据保留，续跑记录包含累计调用信息，不能把两个文件重复加总。

证据：[首次运行](../../artifacts/requirements-state/live-smoke/20260910T012025.488643900Z/result.json)、[最终续跑与累计用量](../../artifacts/requirements-state/live-smoke/20260910T012626.549857800Z/result.json)。本次小规模冒烟证明典型真实抽取链路，不代表开放自然语言或全量生成评估已经完成。

## 2026-09-10 对话布局与会话导航回归

根据实际使用反馈，将固定占据聊天宽度的右侧检查器改为按需展开的详情抽屉。聊天顶部保留简短需求摘要，桌面左侧持续显示会话列表，首页和会话页均可显式新建；窄屏通过顶栏打开会话列表。需求、来源、配置、校验和版本能力保持服务端原有链路。

- TypeScript、ESLint 和全部 44 项 Web 组件测试通过。
- 1440、768、375 三档共 6 项浏览器回归最终全部通过，58.5 秒：新建空会话、首条消息后导航保留、切换与浏览器前进后退、刷新、需求编辑与撤销、确认后返回聊天、配置与历史入口、关闭详情和来源跳转的焦点恢复。
- 三档无横向溢出，axe 无 serious/critical 问题。聊天主区不再预留固定右栏；手机详情使用完整宽度并保留「返回对话」。
- 浏览器回归仍使用独立 PostgreSQL 与保存的 Screening/Builder 回放。最终重跑前旧回放服务因超时退出产生连接错误，重启独立服务后通过；用户的 3000 服务保持运行。
- 另外通过实际 3000 页面检查已有会话、手机配置详情与新建空会话；本次布局调整新增模型调用为 0。
- 离线 Next 通过 `NEXT_DIST_DIR=.next-requirements` 使用独立缓存，避免占用用户正在使用的开发服务；测试完成后停止 3100/18083 离线服务。

最终截图：[桌面对话](../../artifacts/requirements-state/navigation-final/desktop-chat.png)、[平板对话](../../artifacts/requirements-state/navigation-final/tablet-chat.png)、[手机对话](../../artifacts/requirements-state/navigation-final/mobile-chat.png)。

### 同日补充：常驻配置栏与个人入口去重

根据后续界面反馈，桌面恢复会话导航／聊天／配置三栏。右侧仅常驻配置、校验和版本，宽度控制在 360–480px；详细需求从聊天摘要独立展开。个人设置统一在会话导航底部，工作台顶栏移除重复入口；手机通过会话菜单访问设置，配置继续使用抽屉。

- TypeScript、ESLint 与 44 项 Web 组件测试通过；三档共 6 项浏览器流程通过，无新增模型调用。
- 覆盖桌面配置生成与刷新后常驻、三栏不重叠、更换配件预填和聚焦、个人入口去重及手机菜单可达；需求来源、确认、历史与导航回归继续通过。
- axe 无 serious/critical 问题。首次桌面扫描捕获按钮从禁用到启用的透明度过渡；等待按钮恢复可操作且透明度为 1 后，仅重跑该失败流程并通过，未降低检查阈值或更改产品颜色。
- 实际 3000 页面检查 1440px 和 1024px：1024px 下左栏 240px、聊天 424px、配置 360px，无横向溢出；顶栏个人入口数量为 0。
- 离线 Next 显式使用 `--hostname 127.0.0.1 --port 3100`，与回放服务允许的 origin 一致；结束后停止独立测试服务并清理本次临时数据库，保留正常项目服务。

最终截图：[桌面常驻配置](../../artifacts/requirements-state/pinned-build-browser-final/desktop-build.png)、[独立需求编辑](../../artifacts/requirements-state/pinned-build-browser-final/desktop-requirements.png)。

### 同日补充：消息区分与单条复制

用户消息在聊天列内靠右，使用轻微底色，助手回复保持靠左。反馈入口缩小为桌面 28px 高，与单条复制并排；触屏保留 44px 点击区域。双方消息均可复制完整原文（保留 Markdown 与换行），显示成功或失败提示。

- TypeScript、ESLint 通过；Web 组件测试单进程运行 16 个文件、48 项全部通过，覆盖原文复制、剪贴板拒绝后重试和反馈适用范围。首次默认并发运行受 Windows 资源不足影响，worker 异常退出，改为 `--maxWorkers=1` 后通过。
- 实际浏览器连接受同次资源不足中断后，使用独立 PostgreSQL 与保存的 Screening 回放，在 3100 页面发送真实请求验证：用户/助手剪贴板内容逐字匹配服务端消息，反馈成功保存，刷新恢复。
- 浏览器检查 1440px 桌面与 375px 触屏：用户消息右边界对齐聊天区域、助手回复靠左，无横向溢出；操作高度分别为 28px、44px。浅色桌面与深色手机截图已检查。
- 本轮新增真实模型调用为 0。离线服务未配置实时事件存储，截图中的降级提示属于回放环境。

截图：[桌面消息](../../artifacts/requirements-state/message-actions/desktop.png)、[手机消息](../../artifacts/requirements-state/message-actions/mobile.png)。

### 同日补充：会话管理与重复配置摘要移除

移除对话底部重复的配置摘要/详情按钮，桌面直接使用常驻右栏，手机从对话上方查看配置。会话列表「…」支持重命名、归档、取消归档和确认后删除，提供已归档列表。

- 新增迁移 14、会话管理 API 和客户端生成契约。已在本地数据库应用迁移并重启 API，3000 页面可直接使用；没有修改模型配置，新增模型调用为 0。
- `PG_TEST_DSN=... go test -p 1 ./internal/store ./internal/producthttp ./internal/product -count=1` 通过。新增真实 PostgreSQL 测试覆盖所有权、标题校验、手动命名不被首条消息覆盖、归档筛选/恢复、消息与两个配置版本保留、运行中删除被拒绝，以及删除后版本、需求、运行、消息、分享清理和旧接口返回 404。
- TypeScript、ESLint 和 16 个文件共 51 项 Web 组件测试通过。新增测试覆盖重命名缓存同步、删除必须确认、删除失败保留窗口可重试、归档列表恢复。
- `REQUIREMENTS_WEB_URL=http://localhost:3000 pnpm --dir web exec playwright test --config=playwright.requirements.config.ts session-management.spec.ts` 使用普通服务端和独立浏览器身份创建空会话，无前端 route mock。桌面首次通过；平板/手机首次受抽屉关闭动画期间可访问树切换影响，测试过早决定打开抽屉，修正为等待抽屉或入口恢复后，仅重跑这两档并通过（29.9 秒）。三档均验证重命名后刷新、归档恢复、取消删除、确认删除、刷新后列表/详情不可访问，无横向溢出。
- 另外实际浏览器手动检查了桌面归档菜单和手机改名/删除全流程，使用本轮新建的测试对话，原有用户对话保持不变。

界面证据：[手机重命名](../../artifacts/session-management/mobile-rename.png)、[删除确认](../../artifacts/session-management/mobile-delete.png)。

### 同日补充：面向用户的配置摘要与纯图标操作

反馈/复制改为纯图标，保留 Tooltip、可访问名称和触屏点击范围。配置回复不再重复内部 SKU、build_ref、工程参数与落库标识；由 presenter 按消息对应的配置版本生成摘要，改单突出配件及价格变化，并从历史需求差异补充可确认的预算/品牌调整依据。超预算、缺价、适配问题和静音实测证据不足仍明确展示。

- 新增迁移 15，消息原始 content 保留，display_content 和 build_version 与完成消息同事务保存。旧消息仅通过保存的运行证据或版本号与 build_ref 双重匹配只读转换；数据库验证了新摘要持久化、历史版本不串用、旧原文不改写，以及无法关联/用户消息不被过滤。
- presenter、store、product、producthttp 离线测试通过，包括真实 PostgreSQL；额外覆盖中文型号、超预算金额、不同预算口径不误比较、缺价、硬静音条件、适配失败/未知以及仅列变动配件。TypeScript、ESLint、52 项 Web 组件测试通过。
- 使用保存的 Screening/Builder 回放，桌面和手机完整需求/生成/改单/刷新流程均通过（2 项，29.6 秒），同时验证图标按钮无文字、聊天使用服务端摘要、复制内容一致、历史配置保护。Windows 剪贴板将换行规范化为 CRLF；初次严格字节比较失败，改为仅规范化 CRLF 后比较完整内容，再跑通过。
- 实际浏览器检查了摘要段落、警告列表、图标悬停提示及手机排版，无横向溢出；修正了旧 Markdown 容器保留空白导致的多余空行。
- 原测试端口 3100 被其他项目占用，未停止该项目；增加 REQUIREMENT_BROWSER_WEB_URL 支持，将独立测试页面改到 3101。完成后清理本轮临时服务和数据库，正常 3000/API 服务保持运行，迁移 15 已应用。
- 新增模型调用 0；摘要由结构化数据确定性生成，没有额外总结模型。截图中的事件存储降级提示仅属于离线回放环境。

截图：[桌面摘要](../../artifacts/conversation-summary/desktop.png)、[手机摘要](../../artifacts/conversation-summary/mobile.png)。

### 同日补充：本地页面无响应恢复

用户反馈 3000 页面无法打开、新对话无法进入。现场检查：API 健康接口返回 200，但前端进程仍监听端口且 localhost/127.0.0.1 页面请求分别在 5/15 秒内无响应。重启该前端进程后恢复；日志还存在大量 Google Fonts 下载超时，因此将根 layout 改用固定版本 geist 1.7.2 自带的本地字体，避免编译依赖外网字体请求。字体超时是已确认的隐患，没有将其认定为进程停滞的唯一原因。

TypeScript、ESLint 通过；实际浏览器验证桌面连续新建两个会话、进入、切换、刷新，以及手机新建会话成功。字体资源均从本机加载，没有 Google 字体资源请求。最后首页返回 200（约 0.31 秒）、API 返回 200，测试空会话已清理，用户原有会话保留。新增模型调用 0。

### 同日补充：新会话欢迎区与可调侧栏

新会话欢迎内容在聊天阅读区居中，恢复与首页共用的三个建议输入；点击只预填并聚焦输入框，不写入需求。桌面左右侧栏分隔线支持拖动、方向键微调、双击/Enter 恢复默认，宽度保存在本机，刷新后恢复。窗口缩小时至少保留 360px 聊天宽度；手机继续使用抽屉，首页概览栏也支持调宽。

- `workspace-layout.spec.ts` 对正常 API 创建真实空会话，桌面、平板、手机 3 项通过（11.7 秒），覆盖欢迎居中、三个建议预填/聚焦、服务端仍无消息与配置、实际鼠标拖动、键盘调整、双击重置、刷新恢复、1024px 极限宽度、输入入口可见和无横向溢出。测试空会话已清理。
- 初跑发现首次加载时按钮可在 React 交互就绪前被点击，导致新建或移动端会话抽屉未打开；增加就绪前禁用后复测通过，没有以固定等待掩盖问题。
- TypeScript、ESLint、52 项现有组件测试通过；补充就绪保护后，会话导航相关 6 项再次通过。实际浏览器另检查桌面/手机排版、首页右栏与窗口收缩；新增模型调用 0。

截图：[桌面欢迎区](../../artifacts/resizable-workspace/desktop.png)、[手机建议输入](../../artifacts/resizable-workspace/mobile.png)。

### 同日补充：视频剪辑建议误报 Agent 不可用

现场两次发送“预算 6000，主要剪 4K 视频，尽量安静”均在初筛失败，本地服务健康。一次固定模型诊断复现：模型已正常返回，证据校验只识别“剪辑”等表达，漏认“剪 4K 视频”，随后通用错误分支误报服务不可用。真实响应已保存为离线回归素材 `internal/agents/pipeline/testdata/requirement_video_editing.json`。

修复口语视频剪辑证据识别；明确视频素材分辨率与显示/游戏分辨率的差异，模型混淆时将明确素材原话保留为补充说明，保留其他有效 notes。初筛输出校验错误使用独立错误类别，产品返回“需求暂未整理完成”，不再误报 Agent 服务断开。沿用 `generation_failed` 问题码，无接口和数据库迁移。

- pipeline、product、producthttp 离线测试通过，覆盖保存的真实模型输出、预算/用途/软静音保留、视频分辨率不变成屏幕偏好、已有 notes 保留、否定边界及错误分类。
- 已更新并重启本地 API；浏览器用原句发送，真实服务返回 `requirement_ready`，无错误；刷新仍保留预算 6000、productivity、软静音与素材备注，屏幕分辨率仍未知。没有确认生成配置，Builder/Embedding 调用为 0。
- 本次严格控制为 2 次当前模型 `deepseek-v4-flash-0731` 的 Screening 调用：诊断 1 次（1571 tokens）、修复后浏览器验证 1 次（1648 tokens），合计 3219 tokens；无 SDK 重试，无模型切换。原故障的两次用户调用不计入上述诊断用量。
- 诊断与脱敏用量证据位于 `artifacts/screening-diagnostic/`。用户原失败记录和已有配置保持不变，可通过“重试上一步”继续。

## 保留边界

长期偏好提取/用户授权、个人与他人画像隔离、跨会话冲突/失效、长期记忆查看和删除尚未建设。本版只持久化当前会话并保留扩展字段。噪声、外观及自由文本必须条件缺少统一目录证据时明确停止生成；没有把兼容性通过当成偏好满足。旧会话不自动从模型默认值回填新状态。

## 后续整体调整：需求语义与约束

上述内容保留为初版和局部修复的历史记录。`7dd1270` 之后已调整当前会话语义理解、部分更新和 Builder 的自由文本处理，补充真实故障输入到生成、刷新及历史版本的离线与浏览器验证；用途事实和补充说明不再仅因 must 标记被拒绝。结果、0 真实调用口径及仍待核验的真实硬条件边界见[需求语义与约束验收](需求语义与约束验收.md)。
