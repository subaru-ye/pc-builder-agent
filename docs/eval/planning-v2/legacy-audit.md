# 旧 v1.5 评估集逐题审查

50 题均保留历史场景和哈希；没有修改旧题或历史成绩。以下是迁移判断，不表示 50 题已自动转成新协议或已通过新模型。新入口先执行单独登记的真实问题回归集。

| 用例 | 原场景 | 判定调整 | 数据依赖 |
| --- | --- | --- | --- |
| L1-001 | 8K 预算 2K 玩黑神话(基线) | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-002 | 8K 1080p 全 AMD 平台 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-003 | 8K 2K Intel + NVIDIA 组合 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-005 | 15K 4K 3A 大作 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-006 | 25K 4K 高刷电竞,预算向 GPU 倾斜 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-007 | 8K 视频剪辑生产力 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-008 | 8K 2K 游戏,要安静(noise_pref 语义路径) | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-009 | 8K 1080p 白色海景房(notes 语义路径) | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-012 | 8K 2K ITX 小钢炮 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-013 | 15K 本地开发编译机,弹性 0.15,要安静 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 只有用户明确授权的预算弹性可保留。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-014 | 8K 家用机,A 卡即可 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L1-015 | 25K 4K 旗舰静音游戏机 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-101 | 基线A 换 AMD 显卡(锁定品类:无) | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-102 | 基线A 降预算 500 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-103 | 基线A 改约束为 ITX | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-104 | 基线A 降预算 500 且锁定 GPU | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 保留用户明确锁定；区分“尽量不动”，不延续程序固定改单数量限制。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-105 | 基线B 降预算 1000 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-106 | 基线B 换 AMD 显卡 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-107 | 基线B 降预算 800 且锁定 CPU+GPU | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 保留用户明确锁定；区分“尽量不动”，不延续程序固定改单数量限制。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-108 | 基线A 改约束为要安静 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 原文区分 must/prefer；不能根据旧结构默认强度。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-109 | 基线C 换 AMD 显卡 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L2-110 | 基线C 降预算 3000 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L3-101 | 陷阱:噪音偏好不得推断品牌 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L3-102 | 陷阱:预算缺失应追问,不编造 | 预算未知不再强制追问或拒绝；检查不编造金额，允许讨论方向。  | 会话与语义 |
| L3-103 | 陷阱:游戏缺分辨率应追问或明示推断,不静默编造 | 游戏分辨率未知不再固定追问；不得推断为已表达偏好。  | 会话与语义 |
| L3-104 | 对照:正常需求应结构化(8000 2K 黑神话) | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L3-105 | 显式品牌应照抄(A 卡 + AMD CPU) | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L4-201 | 已有 CPU 缺具体型号，必须追问 | 保留已有件与预算口径场景；只问确实影响下一步的信息，不强制旧阻断结果。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L4-202 | 已有 CPU 型号明确但预算口径不明 | 保留已有件与预算口径场景；只问确实影响下一步的信息，不强制旧阻断结果。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L4-203 | 当前目录独立最低价也超过一百元预算 | 本地最低价只能作为证据反馈；必须进入模型规划，不能程序提前返回市场无解。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L4-204 | 已有件型号不在目录，不能替换相似型号 | 目录外已有件允许查官网规格；不能偷换近似型号，不强制固定 data_unavailable。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L4-205 | 已有 AM5 CPU 和 DDR4 内存不能擅自更换 | 真实插槽/内存冲突必须保留；允许提出替换备选但不得修改用户已接受的保留件。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L4-206 | 已有准确 CPU 和 DDR5 内存，仅计算新增采购预算 | 保留场景；改用产品 planning 执行与交付核验，取消固定工具顺序/修复器限制，保留真实兼容性和预算约束。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L4-209 | 契约迁移：15K 2K 游戏,已有显卡，先补型号和预算口径 | 保留已有件与预算口径场景；只问确实影响下一步的信息，不强制旧阻断结果。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L4-210 | 契约迁移：3K 家用机,已有 CPU 和内存，先补型号和预算口径 | 保留已有件与预算口径场景；只问确实影响下一步的信息，不强制旧阻断结果。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L4-211 | 初筛追问已有件具体型号，不重复询问已知信息 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L4-212 | 初筛追问已有件的预算口径，不重复询问已知信息 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L4-213 | 游戏用途和分辨率已知，只追问已有显卡型号 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L4-214 | 已有件型号和采购口径已知，只追问预算金额 | 缺预算可先讨论；不将固定字段清单作为准入条件。  | 会话与语义 |
| L4-215 | 3K 家用办公：预算内交付或有证据的合理非交付 | 预算内交付与具体待解决分开统计，不把任意不交付都算正确。 固定完整型号/规格/报价快照，不只固定价格日期；新数据另跑配对组。 | 目录/规格/价格 |
| L5-301 | 补分辨率前修改预算，以新金额为准 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L5-302 | 补充缺失字段后不再追问可选游戏名 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L5-303 | 显卡价格不是整机预算 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L5-304 | 已有件型号和采购口径分轮补充 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L5-305 | 明确更正采购预算口径 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L5-306 | 中文金额和简短补充不丢用途 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L5-307 | 品牌偏好改变后其他条件保持 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L5-308 | 更正已有CPU型号，不能保留旧型号 | 保留用户输入与纠正语义；spec/clarify 断言迁移为服务端增量状态、来源、强度和必要追问检查。  | 会话与语义 |
| L5-309 | 办公改游戏后仅补问分辨率 | 办公改游戏应保留其他状态；是否追问分辨率由必要性决定。  | 会话与语义 |
| L5-310 | 用户撤回预算后应重新询问 | 预算撤销后保留未知且不复活；不强制立即重新询问。  | 会话与语义 |
