# 当前版本业务收尾与有限验收

> 后续用户要求已执行：[52 件缺价商品退库及右侧价格提示调整](../../../scripts/data/reviewed-intake/2026-09-15-retire-unpriced/README.md)，2026-09-16 完成主板规格、ITX 电源安装和散热未知表述收尾，2026-09-17 完成 SFX 电源发布、散热容量复核与三项真实模型冒烟（见下文补充收尾节）。当前有效目录为 177 件／125 条报价；下文 123／175 件是历史状态，冻结评测数据不改写。

本目标以 2026-09-15 用户最新指令为准，替代历史核心分数追平要求。历史报告和失败原文保留；不追加采样追分，不改变兼容性、必须条件或未知项判定。

**结论：本次业务收尾与有限验收完成。** 确认的会话补规格和未交付回复缺陷已修复，关键链路有离线及真实浏览器证据；模型能力、数据覆盖和检测环境限制按下文保留，不宣称完整模型质量验收或独立真人评审通过。

## 2026-09-16 补充收尾

用户随后取消了本目标原定的 12 次模型、3 次搜索、6 次网页读取总上限；取消只影响本轮人工验收额度，不改变单次 planning 的 8 次模型、24 次工具、3 次搜索、6 次读页产品保护。此次补充仍未调用真实模型、Embedding 或买手报价服务。

1. **主板规格**：正式发布 `mb-gb-b650-aorus-elite-ax.memory_speed_max_mts=8000`，同时保存 rev. 1.0/1.1 与 rev. 1.2 官网证据及 CPU/内存配置限制。当前目录实测该主板配 DDR5-5200 时 `MEMORY_SPEED=pass`。
2. **ITX 安装检查**：扩展既有 `FORM_FACTOR_SUPPORT`，纯 ITX 机箱同时校验电源形态和长度；较大机箱缺少电源仓、转接架及冷排联动字段时不推断。NR200P 与 Terra 保存 SFX/SFX-L、130 mm 官网资料；14 款在售电源的 ATX 形态和 140/160/180 mm 长度均已补齐。NR200P + MAG A650BN 实测为明确 fail，不再停在 unknown。
3. **散热未知与过度承诺**：`COOLER_THERMAL_CAPACITY` 非 pass 时，保存方案会删除或替换模型 issues、assumptions 与散热器理由中的“完全足够／绰绰有余／不影响安全性”等无证据保证；原始模型回复仍保留在运行轨迹。历史散热失败录制仍为 0/1，用于保留问题证据，不改写为通过。
4. **数据发布**：先后发布 `b2f60c4b-6657-5467-b32e-e8244c03c3cd` 与当前 `e2f061cb-429b-5f57-b5fa-d933f18c1f8c`。两批均先在独立数据库演练，再通过现有事务发布；123 件在售、123 条报价、报价快照和用户历史校验值保持不变。审核包见[主板与 ITX 规格](../../../scripts/data/reviewed-intake/2026-09-16-board-itx-fit/README.md)及[在售电源规格](../../../scripts/data/reviewed-intake/2026-09-16-active-psu-fit/README.md)。

验证范围：Go 生产包测试与 vet 通过；数据测试 167 项、Web 单测 63 项、类型检查、ESLint 与 API 类型检查通过。真实 Chromium 分别以桌面和手机运行首次交付、缺规格补证、CPU/预算修改与刷新历史三组用例，共 6/6 通过；使用真实 Next.js、产品 HTTP、PostgreSQL、生产规则和持久化，模型及网页 transport 为既有离线录制/夹具。第一次浏览器启动因测试 Origin 未同步而失败，修正环境后重跑通过，不计为业务缺陷。

全仓 `go test ./...` 与 `go vet ./...` 会编译被忽略的 `artifacts/firecrawl-comparison-20260914/source` 归档源码，并因该归档缺少配套文件失败；生产范围 `./cmd/... ./internal/... ./db` 全部通过。此次真实模型请求 0，模型 Token 0，模型耗时 0，买手及外部价格请求 0；账单金额无法取得，记为未知。官网补证保存 17 个具体型号来源、34 条字段证据；界面没有跨会话外部请求计数汇总，故总 HTTP 请求数不伪造为确定值。

当前仍保留的真实缺口是散热器解热能力、真实性能与静音实测，以及较大机箱的复杂电源仓/冷排联动；这些情况继续显示 unknown、warning 或待解决方案，不宣称全部兼容、性能保证或正式交付。

## 2026-09-17 补充收尾

本轮按用户指令完成 ITX 配置恢复、散热缺口处置与三项真实模型冒烟。当前正式 release：parts `b1e13ebd-71be-57e5-a1e9-755312ef6caa`、price `17ad835a-5794-522d-aee7-a2750faf18a8`、快照 10。

1. **ITX 路线（新增 SFX 电源）**：通过既有买手路线取得 2 款合格 SFX/SFX-L 电源并正式发布——`psu-cm-v850sfx-gold-white`（V SFX Gold 850W，SFX、100 mm，酷冷至尊京东自营 1199 元）与 `psu-asus-rog-loki-850p-white`（ROG Loki 850P，SFX-L、125 mm，华硕京东自营 1649 元）；目录 177 件／125 报价。两款的官网规格正文（Cooler Master、ROG 官网，2026-09-17 抓取）与买手报价（链接、卖家、row_key、观察日）均可追溯；利民 TR-SGFX650、FSP Dagger、DeepCool PS750G 证据不足明确暂缓。先独立数据库演练再正式发布，旧商品、报价、历史配置与需求校验值不变。发布后 `cmd/validate` 实测 NR200P／Terra × 2 款电源共 4 组 `FORM_FACTOR_SUPPORT` 全部 pass（sfx/100mm、sfx_l/125mm ≤ 130mm），电源形态与长度不再 fail 或 unknown；Terra 仅剩 AG400 风冷限高失败（150mm > 77mm），与电源无关。见[SFX 电源发布记录](../../../scripts/data/reviewed-intake/2026-09-17-sfx-psu/README.md)。
2. **散热容量处置（保持 unknown）**：对 10 款缺 `cooling_capacity_w` 的在售散热器逐款核查厂商官网（Thermalright×4、DeepCool×4、Arctic×2、Cooler Master×1），无一公布可映射的额定散热瓦数，全部保持 unknown、0 次发布；Noctua 两款官网反爬无法核实。已有数值（AG400 220W、AK620 260W、Dark Rock Pro 5 270W）保留。历史散热失败录制离线重放结论不变：保存内容无“完全足够／绰绰有余／保证温控”等表述。见[散热容量复核记录](../../../scripts/data/reviewed-intake/2026-09-17-cooler-capacity/README.md)。
3. **三项真实模型冒烟（各执行一次，不重跑）**：套件 `internal/planningeval/testdata/smoke-20260917-r1/`，Screening `deepseek-v4-flash-0731`、Builder `qwen3.8-max-0902`、retries=0，工具为离线夹具。结果存 `artifacts/smoke-live-20260917-r1/`：

   | 场景 | 结果 | 模型请求 | Tokens | 工具 | 搜索/读页 | 耗时 |
   | --- | --- | --- | --- | --- | --- | --- |
   | CPU 升级 | pass：保存 v2（5600→5700X），其他配件与预算/静音偏好保留，刷新恢复 | 3 | 36,669 | 2 | 0/0 | 30.4s |
   | 预算修改 | pass：保存 v2（7500→6500，总价 6483 元），其他需求与静音偏好保留 | 2 | 14,774 | 1 | 0/0 | 24.4s |
   | 散热未知 | 未达冻结预期（保存 proposal 而非 ready），失败结果保留 | 8 | 173,114 | 13 | 2/0 | 86.9s |

   合计 15 次真实模型请求、234,622 tokens、152.1 秒；外部搜索 0、外部网页读取 0；账单金额未知。散热场景按预声明预期判 fail，但业务保护全部成立：保存的 issues／assumptions／回复／配件理由中“完全足够／绰绰有余／保证温控／全部兼容／性能保证／正式交付”零命中（原始模型输出中的“绰绰有余”仅保留在运行轨迹，未进入保存内容）；散热器理由被替换为“散热能力缺少可核验规格，当前不能确认温控表现”；保存了 2 条可继续对话的待解决问题（4K 144fps 无实测数据、散热能力字段缺失）。模型输出“接近高刷目标”等表述未被系统采信为已验证结论，对应评估字段为 unknown。CPU 与预算场景均未重复询问已知信息、未新增确认步骤，v1 旧版本不变、刷新可恢复。
4. **离线验证**：Go 生产范围 `./cmd/... ./internal/... ./db` 测试与 vet 通过（39 包）；数据测试 167 项、Web 单测 63 项、tsc、ESLint、gofmt 通过；全仓 vet 仅被 `artifacts/firecrawl-comparison-20260914/source` 历史归档阻塞（undefined: numericEvidence），生产代码不受影响，未删除归档；golangci-lint 不在本机 PATH，仅 CI 可验。
5. **真实浏览器验证（离线夹具）**：按 2026-09-15 复现步骤执行 `TestRecordedBudgetBrowserServer`——真实 Next.js（3107，`GO_API_BASE_URL` 指向测试产品 API 18089）、产品 HTTP、独立 PostgreSQL、生产规则与持久化，模型为离线录制。第一次（`artifacts/browser-recorded-20260917-r1/`）因未启动 3107 Web 服务、浏览器无法交互而 30 分钟超时，环境性失败如实保留。第二次（`artifacts/browser-recorded-20260917-r2/`）以真实浏览器打开 `/__offline/start`、核对需求面板（7500 元／游戏／2K／必须保留显卡）并确认，页面状态由“待确认”变“已确认”，服务端收满 7 次离线协议响应；POST shutdown 后测试断言通过（262.5s）：v2 仅生成一次、历史 v1 未变、实际模型请求 0。同轮 run.py 默认 `-run .` 使全套 planningeval 一并运行，`TestHistoricalBaseAndBatchProductFlow` 因与浏览器测试共享评估库非空而失败；单独在干净库重跑该测试 1.7s 通过（`artifacts/historical-base-recheck-20260917/`），属测试顺序冲突而非业务缺陷。

## 2026-09-17 发布候选回退点

功能收尾冻结于本地 tag `release-candidate-20260917`（提交 `f4d05e9`，不推送远端）。备份位于 `var/backups/rc-20260917/`（不入库）：`pcbuilder-f4d05e9.dump`（pg_dump -Fc，已校验 44 张表数据）、`releases/`（磁盘不可变 release 目录）、`current.json`、`reviewed-intake/`（审核包，另在 Git 内有同内容）。`var/data/collections/` 原始采集不入备份。

回退步骤：`git checkout release-candidate-20260917` → 重建 `cmd/api`、`cmd/buildsvc` 二进制 → `docker exec postgres pg_restore -U pcbuilder -d pcbuilder --clean` 恢复 dump → 用备份覆盖 `var/data/releases/` 与 `var/data/current.json` → 重启常驻服务并核对 `/readyz` 与 release。

## 历史执行前调用登记（已取消）

本目标最初硬上限为模型 12 次、外部搜索 3 次、外部网页读取 6 次，所有失败尝试也计入；2026-09-16 用户已明确取消这些总上限。本段及下文 `0/12` 仅保留首次验收当时的历史口径，不再约束补充收尾。

历史终点为 `da852e9`：18 批合计 479 次真实模型请求、5,174,867 tokens，金额未知。它们属于本目标开始前的历史消耗，不计为本轮调用或本轮效果。当前固定模型为 Screening `deepseek-v4-flash-0731`、Builder `qwen3.8-max-0902`、Embedding `qwen3.7-text-embedding`；本轮离线不调用这些模型。

## 已核对的现状

- 初始工作区干净；已有直接 CPU 升级、上下文注入、需求纠正、方案持久化、自动交付及历史保护实现与测试。
- 正式数据最新为 175 件／123 报价、快照 9；ITX 发布包及不可变历史已核验。原 101 个暂缓候选中 1 个已发布，余 100 个维持逐项待证据记录。
- FIT V 内存仍缺可归档官网正文及购买链接，原 HTTP 406 已记录，不重试、不猜价。报价使用既定买手路线，本目标不新增采集或发布。
- 历史真实模型仍有重复追问、预算口径推断、重复背景、超预算选配和无来源性能表述。保存回答可验证软件修复，不能证明真实模型现在稳定成功。

## 本轮修复

复用 `register_candidate` 补充本地候选的会话规格：原编号、精确身份、已读取正文、逐字段摘录；仅补未知字段，保留本地价格，已知冲突拒绝。字段证据附候选编号、字段、来源 URL、正文摘录和采集时间。续聊从方案快照重新应用补充，当前目录已知事实优先。全局商品、已确认需求、旧版本均不静默改写。

未通过核验的草稿即使模型标签为 collect/clarify，也返回具体缺项和未交付回复；候选保留以便续聊。补充选中配件后旧核验失效，必须重新计算。未解决方案保留已检索本地备选。

## 验证结果

业务代码提交 `49250c9`，未推送远端。本轮按功能验证软件链路，不用历史总分或离线 oracle 成功率代替模型质量。

| 功能 | 本轮证据与实际范围 |
| --- | --- |
| 首次确认与自动交付 | 既有完整方案录制单测；`planning.spec.ts` 桌面与手机实际发送、编辑预算、确认、自动生成正式 v1，无额外方案确认 |
| 直接升级 CPU | `upgrade.spec.ts` 桌面与手机：同一句话触发检索和重新校验，5600 → 5700X、v2、4929.90 元；旧版不变，刷新保留。模型决策为明确的离线 oracle |
| 调预算且保留其他需求 | 新增浏览器场景直接从聊天把 7000 改为 6500，并自动选配；1080p 多轨素材事实、已撤销静音保持，不重新要求确认 |
| must / prefer、撤销、备选 | 既有 12 题机制集全通过，含 P2-002/P2-004/P2-010；浏览器把软静音改 must 后保存待解决方案，放宽后续聊交付；备选不生成版本 |
| 缺规格、补正文、重新核验 | 合成 `mb-offline-gap` 缺 `motherboard.memory_speed_max_mts` 时 MEMORY_SPEED=unknown，仅保留方案；随后真实执行 read_page → register_candidate → evaluate，使用合成页面明确的 3200 MT/s，生成 v3 |
| 来源追踪 | 桌面和手机展开来源，显示对应主板、支持内存频率、URL、正文摘录和日期；字段引用为 `supplement:<候选>:memory_speed_max_mts`，原始完整正文仍保存。搜索线索／本地目录／已读正文不混用 |
| 刷新、继续、旧版本 | 补证后刷新再发“按当前方案继续校验”，恢复会话规格，0 次新读页，保存 v4；v1 完整读模型相等，历史视图刷新仍只读。测试网关同时核对数据库商品规格未变，需求字段在补证前后相等 |
| 假交付保护 | 单测覆盖 ready/proposal/clarify/collect 携带未核验草稿时不能透传“全部兼容／性能保证／已正式交付”；缺价、冲突和必须条件仍阻止正式交付 |

**浏览器为实际 Chromium + Next.js + 产品 HTTP + PostgreSQL + 生产规则与工具实现，没有前端 route mock。** 模型及网页 transport 使用已有录制或明确合成输出，不是新真实模型验证；合成主板页面只证明补证机制，不冒充厂商事实。机制集还覆盖 A2A 编解码；新增浏览器网关在进程内接入 Runner，不单独证明真实跨进程模型请求。

### 检查与产物

- 全源码 `./cmd/... ./internal/... ./db` 在独立 PostgreSQL 完整迁移环境测试通过：`artifacts/closeout-all-db-20260915/`。最终规格格式检查加入后，planning/producthttp 再次独立数据库通过：`artifacts/closeout-final-planning-db-20260915/`。显式 live/浏览器门禁及未配置 Redis 测试环境的跳过项不计为已执行。
- 12 个既有机制场景全通过：`artifacts/closeout-mechanisms-20260915/`，23 条 Screening／40 条 Builder 离线响应、26 次工具执行。未修改套件输入、断言或历史分数。
- 前端 18 文件／62 项单测、类型检查、API 类型生成、相关 ESLint、Go build/vet、gofmt 与 `go mod verify` 通过。全仓 golangci-lint 报 22 条既有告警（15 errcheck、1 ineffassign、6 staticcheck），均在本轮未修改的代码行；以 `--new-from-rev=da852e9` 检查本轮差异为 **0 issues**，不宣称全仓 lint 已清零。
- 尝试了全源码独立数据库 `GOFLAGS=-race`：Windows ThreadSanitizer 在各测试进程初始化时报告内存分配失败（error code 87），未进入有效竞态检测，**不计通过**。日志无 DATA RACE 报告，但这不能证明没有竞态；证据 `artifacts/closeout-race-db-20260915/`。未以循环重试或修改检测参数掩盖环境错误；普通完整数据库回归结果独立保留。
- 桌面首次交付／必须条件：`closeout-browser-desktop-r3.log` 的 planning 场景通过；桌面 CPU 升级：`closeout-browser-desktop-r2.log` 的 upgrade 场景通过；桌面完整补证：`closeout-browser-supplement-r5.log` 的 desktop 场景通过（20.6 秒）。手机三场景 **3/3**：`artifacts/closeout-browser-mobile-r6.log`（47.3 秒），含截图。
- 本轮浏览器失败记录全部保留：首轮遗漏允许来源参数；随后一次首页回跳的轨迹同时记录开发热重载（未据此猜测修改业务导航）；补证夹具先错误序列化无 JSON 标签的内部 BuildDraft，再误把中文问题说明当字段路径断言；另外修正了服务未就绪、URL 来源定位及手机历史详情未展开的测试操作。未通过修改判分、生产来源检查或规格规则掩盖这些失败。桌面和手机最终对应场景均已有明确通过记录。
- 浏览器实际 API 快照另存 `artifacts/closeout-browser-observations.json`（从 r5 手机 trace 提取，该次仅末尾历史面板未展开断言失败，r6 已补验）。最终测试服务器正常 shutdown 并 PASS，专用数据库容器全部清理；3105／18087 临时服务关闭，Next 自动改写的 tsconfig 已恢复。

### 消耗

本目标真实模型 **0/12**、外部搜索 **0/3**、外部网页读取 **0/6**，Embedding／买手调用也为 0；新增项目模型 tokens、模型等待及外部请求耗时均为 0。没有账单查询能力，**实际账单金额未知**，不把历史 token 数当费用。本轮不追加真实验证，余量不用于追分。

单次完整补证浏览器场景包含 5 条 Screening、20 条 Builder 离线协议响应及 9 次工具执行，其中 1 次是离线网页 transport 读取；它们不是计费请求。桌面 20.6 秒、手机 15.1 秒包含 UI 操作，不是实际模型延迟。首次缺规格阶段 oracle 在既有 8 次上限内保存未解决结果，没有把 unknown 改为 pass。

机器可读额度、模型配置、历史起点、二进制及源码哈希：`artifacts/closeout-resource-use-20260915.json`。独立数据库启动、迁移和测试耗时分别见各目录 tests.log/manifest.json，不与模型耗时混算。

## 数据结论与剩余问题

本轮对正式库只读核对：175 件、快照 9（2026-09-15）、123 条报价，与 ITX 发布记录一致。报价覆盖为 CPU 27/28、主板16/21、显卡13/21、内存14/21、SSD13/23、电源14/20、机箱13/21、散热13/20；总计52件本快照无报价。没有回填旧报价、扩大采集或新增发布。

1. **模型语义仍有波动**：历史录制显示可选项重复追问、费用口径推断和重复背景条目；本轮确认上下文和续聊机制可用，未证明所有同义表达都能正确触发规划。用户可直接说明“保留其他要求并继续选配”，或编辑需求面板继续。
2. **真实规划和解释仍有缺口**：最近历史 L1-006 保留 0/1，`mb-gb-b650-aorus-elite-ax.memory_speed_max_mts`、`cooler-arctic-lf3-360.cooling_capacity_w` 未解决，原“接近144fps／散热绰绰有余”等表述没有相应证据。新补规格工具是否被真实模型主动有效使用没有新增实测；兼容性通过仍不能证明性能或噪声承诺。可继续补厂商资料、比较已有替代并重新校验，不能凭空补热功耗能力。
3. **数据覆盖按具体缺口继续**：100 个候选暂缓记录保留，FIT V 缺正文及购买链接继续暂缓；高端/静音/小机箱场景仍可能缺规格或价格。只把可靠正文用于会话 canonical 规格补充，正式商品发布仍走既有审核；页面提取的语义准确性仍需要模型正确识别具体型号、字段和单位。
4. **验证范围边界**：本轮没有新真实模型成功率、跨会话长期画像、真实网络抓取可靠性或独立真人评审结论；历史剪贴板桥接问题未重新验收，不报告修复。浏览器隔离环境的 Builder/Redis 健康探针不可用提示是测试依赖配置，不代表常驻生产依赖不就绪。

## 常驻服务与使用

2026-09-16 补充收尾后已重新构建并启动 API、Builder 和 Web。入口 `http://localhost:3000`；API 8082 `/readyz` 返回 ready，postgres/redis/buildsvc/auth 全部 ok，Builder agent card 与 Web 均返回 200。Builder 日志确认 planning + 固定 `qwen3.8-max-0902`，Screening 为固定 `deepseek-v4-flash-0731`、retries=0。当前数据库 parts release 为 `e2f061cb-429b-5f57-b5fa-d933f18c1f8c`。启动及健康检查没有模型探测或计费请求。

本机启动脚本、精确进程号、日志和二进制在 `var/run/business-closeout-20260916/`；`processes.json` 记录实际加载提交。共享 PostgreSQL、Redis 与账号容器保留；可选 ADK 调试 host 未启动。

复现浏览器（项目根目录，沿用现有启动器；另开终端运行 Next 与 Playwright）：

```bash
REQUIREMENT_BROWSER_WEB_URL=http://127.0.0.1:3105 PLANNING_BROWSER_ADDR=127.0.0.1:18087 GOFLAGS="-run=TestPlanningBrowserServer -v" python scripts/eval/planning/run.py --out artifacts/<新的验收目录> --go-tests ./internal/producthttp --test-timeout 30m
cd web
GO_API_BASE_URL=http://127.0.0.1:18087 NEXT_DIST_DIR=.next-closeout pnpm exec next dev --webpack --hostname 127.0.0.1 --port 3105
REQUIREMENTS_WEB_URL=http://127.0.0.1:3105 pnpm exec playwright test --config=playwright.requirements.config.ts planning.spec.ts upgrade.spec.ts supplement.spec.ts --project=desktop --project=mobile
```

先确认测试 API `/healthz` 为200再操作。结束 POST `http://127.0.0.1:18087/__offline/shutdown`，让启动器清理独立数据库；关闭对应 Next 进程并恢复其自动添加的 tsconfig 路径。不要用测试夹具修改真实目录或把合成规格发布。
