# Requirement v2 人工复核摘要（2026-09-22）

> 由 `scripts/eval/requirement/build_review_sheet.py` 从冻结 fixtures 生成；holdout 未运行，本表仅供产品方逐项确认金标。确认后如需修改，走 fixture 修订 + freeze-manifest 流程，不得为通过运行改标。

## 一、holdout 全量（9 例，锁定未运行）

### reducer / rd-holdout-productivity-titles

- split：`holdout`（session `rd-hold-1`）
- 用户输入：「主要用Premiere剪辑视频（操作：set=use_case.type；值 "productivity"；证据 stated；类别 fact；quote「Premiere剪辑」；set=use_case.titles；值 ["Premiere 视频剪辑"]；证据 stated；类别 fact；quote「Premiere剪辑」）」
- 理由：“主要用Premiere剪辑视频”给出 type 与 titles 两个 fact。

### reducer / rd-holdout-budget-basis

- split：`holdout`（session `rd-hold-2`）
- 用户输入：「预算按整机总价算（操作：set=budget_basis；值 "full_build"；证据 stated；quote「整机」）」
- 理由：复用旧内存时“按整机算”设置 budget_basis=full_build。

### readiness / rdy-holdout-owned-basis

- split：`holdout`（session `rdy-hold-1`）
- 用户输入：「（给定状态，见理由）」
- 状态：`incomplete`
- 缺失字段：`['budget_basis']`
- 阻塞冲突：`[]`
- 确认资格：False
- 理由：型号齐但 budget_basis 未知仍不完整；当前实现一致。

### readiness / rdy-holdout-general-ready

- split：`holdout`（session `rdy-hold-2`）
- 用户输入：「（给定状态，见理由）」
- 状态：`ready`
- 缺失字段：`[]`
- 阻塞冲突：`[]`
- 确认资格：True
- 有效默认：budget_flex=0.1（system_default）；size_pref="any"（system_default）；noise_pref="any"（system_default）；brand_pref.cpu="any"（system_default）；brand_pref.gpu="any"（system_default）；configuration_scope=["tower"]（system_default）
- 理由：general 无条件必填；默认 6 项。

### policy / pol-holdout-confirm-incomplete

- split：`holdout`（session `pol-hold-1`）
- 用户输入：「做视频剪辑，预算7500，全新买」
- 用户输入：「（confirm 请求）」
- Builder 准入：False（原因 `readiness_incomplete`）
- 禁止 veto：`['V4']`
- 理由：productivity 缺 titles 时 confirm API 必须以 readiness 拒绝；当前 planningProjection 忽略 missing，预期启动即 red。

### ui-contract / ui-holdout-productivity-ready

- split：`holdout`（session `ui-hold-1`）
- 用户输入：「用Blender渲染，预算8000，全新买」
- DTO 就绪块：- 状态：`ready`
- 确认状态：`unconfirmed`；build relation：`none`
- 理由：ready+unconfirmed+none；与 ui-ready-unconfirmed 不同会话模板。

### extraction / ex-holdout-budget-approx

- split：`holdout`（session `ex-hold-1`）
- 用户输入：「（无种子轮）」
- 用户输入：「主要做表格和上网，预算3000左右，主机全新」
- 预期操作：set=use_case.type；值 "productivity"；set=use_case.titles；值 ["Office 表格", "上网浏览"]；set=existing_parts；值 []；set=budget_cny；值 3000；证据 stated
- 预期 turn signals：asks_question=False requests_review=False requests_build=False ambiguous=False
- 预期状态：budget_cny=3000
- 预期缺失：`[]`
- 确认资格：True
- 理由：用户给出了具体金额 3000，“左右”的近似含义由默认 budget_flex 表达，预算不保持 unknown。

### extraction / ex-holdout-remove-budget

- split：`holdout`（session `ex-hold-2`）
- 用户输入：「打游戏用，1080p，预算7500，配件全新买」
- 用户输入：「预算不算了，先不限定」
- 预期操作：remove=budget_cny
- 预期 turn signals：asks_question=False requests_review=False requests_build=False ambiguous=False
- 预期状态：budget_cny={"status": "removed"}
- 预期缺失：`['budget_cny']`
- 确认资格：False
- 理由：“不算了”是 remove，不是忽略。

### conversations / cv-holdout-productivity

- split：`holdout`（session `cv-hold-1`）
- 用户输入：「主要用Premiere剪视频」
- 用户输入：「预算8000，全新买」
- 第 1 轮期望：预期操作：set=use_case.type；值 "productivity"；set=use_case.titles；值 ["Premiere 视频剪辑"]；预期缺失：`['budget_cny', 'existing_parts']`；确认资格：False
- 第 2 轮期望：预期操作：set=budget_cny；值 8000；set=existing_parts；值 []；预期缺失：`[]`；确认资格：True
- 最终：缺失 `[]`，确认资格 True，builder_must_run=False
- 理由：Premiere 流程：用途软件→预算→全新买，两轮到 ready。

## 二、关键 development 边界（供确认）

### conversations / cv-fps-progressive

- split：`development`（session `cv-dev-1`）
- 用户输入：「主要玩CS2和无畏契约，希望帧率高一点」
- 用户输入：「7500够吗？」
- 用户输入：「那就7500吧」
- 用户输入：「1080p就行」
- 用户输入：「配件全新买」
- 第 1 轮期望：预期操作：set=use_case.type；值 "gaming"；set=use_case.titles；值 ["CS2", "无畏契约"]；预期缺失：`['budget_cny', 'existing_parts', 'use_case.resolution']`；确认资格：False
- 第 2 轮期望：预期操作：无（不得产生任何 operation）；禁止操作：set budget_cny；预期 turn signals：asks_question=True requests_review=False requests_build=False ambiguous=False；预期缺失：`['budget_cny', 'existing_parts', 'use_case.resolution']`
- 第 3 轮期望：预期操作：set=budget_cny；值 7500；预期状态：budget_cny=7500；预期缺失：`['existing_parts', 'use_case.resolution']`
- 第 4 轮期望：预期操作：set=use_case.resolution；值 "1080p"；预期缺失：`['existing_parts']`
- 第 5 轮期望：预期操作：set=existing_parts；值 []；预期缺失：`[]`；确认资格：True
- 最终：缺失 `[]`，确认资格 True，builder_must_run=False
- 禁止 veto：`['V4']`
- 理由：人工模拟的 FPS/7500/全部新买流程；每轮只透露一部分，最终 ready 但聊天不得启动 Builder。

### extraction / ex-budget-accept-explicit

- split：`development`（session `ex-dev-3`）
- 用户输入：「（上下文）助手上一轮回复：「按7500元的预算继续可以吗？」」
- 用户输入：「可以，就按7500来」
- 预期操作：set=budget_cny；值 7500；证据 stated
- 预期 turn signals：asks_question=False requests_review=False requests_build=False ambiguous=False
- 预期状态：budget_cny=7500
- 预期缺失：`[]`
- 确认资格：True
- 理由：用户自己说出数字即 stated 证据；上一轮助手提问只是上下文。

### extraction / ex-bare-accept-needs-protocol

- split：`development`（session `ex-dev-4`）
- 用户输入：「（上下文）助手上一轮回复：「按7500元的预算继续可以吗？」」
- 用户输入：「可以」
- 预期操作：set=budget_cny；值 7500；证据 accepted_proposal
- 预期状态：budget_cny=7500
- 预期缺失：`[]`
- 确认资格：True
- 理由：存在同字段同值提案且用户明确接受时 evidence=accepted_proposal；当前无该协议，预期 red。

### extraction / ex-monitor-request

- split：`development`（session `ex-dev-9`）
- 用户输入：「帮我配台电脑，要带显示器」
- 预期操作：无（不得产生任何 operation）
- 禁止操作：set use_case.type
- 预期观察：原文含「显示器」
- 预期 turn signals：asks_question=False requests_review=False requests_build=True ambiguous=False
- 预期缺失：`['use_case.type', 'budget_cny', 'existing_parts']`
- 确认资格：False
- 理由：用途未明不猜测；显示器保留为 unresolved 观察，回复不得承诺覆盖。

### extraction / ex-fps-target-numeric

- split：`development`（session `ex-dev-11`）
- 用户输入：「帧数至少144」
- 预期操作：set=use_case.fps_target；值 144
- 预期 turn signals：asks_question=False requests_review=False requests_build=False ambiguous=False
- 预期状态：use_case.fps_target=144
- 预期缺失：`[]`
- 确认资格：True
- 理由：“至少144帧”是明确数值目标，映射可选字段 use_case.fps_target，不改 performance_goal。

### extraction / ex-budget-correction-same-turn

- split：`calibration`（session `ex-cal-7`）
- 用户输入：「预算6000吧，哦不对，7000」
- 预期操作：set=budget_cny；值 7000；证据 stated
- 预期 turn signals：asks_question=False requests_review=False requests_build=False ambiguous=False
- 预期状态：budget_cny=7000
- 预期缺失：`['use_case.type', 'existing_parts']`
- 确认资格：False
- 理由：“6000吧，哦不对，7000”是明确更正：以 7000 为当前预算 set，不记 conflict。

### extraction / ex-budget-conflict-family

- split：`calibration`（session `ex-cal-8`）
- 用户输入：「我爸说预算8000，我妈说不能超过6000」
- 预期操作：conflict=budget_cny
- 禁止操作：set budget_cny
- 预期 turn signals：asks_question=False requests_review=False requests_build=False ambiguous=False
- 预期状态：budget_cny={"status": "conflict"}
- 预期缺失：`['use_case.type', 'existing_parts']`
- 确认资格：False
- 理由：父母各自给出明确且互斥的预算约束，不能替用户取舍：记 conflict 并交确认页，不得择一写入。
